---
phase: 09-financial-wiring-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(gp)/entities/[id]/page.tsx
  - src/lib/computations/__tests__/waterfall.test.ts
  - src/components/features/deals/deal-dd-tab.tsx
  - src/components/features/deals/deal-overview-tab.tsx
  - src/components/features/lp/performance-charts.tsx
autonomous: true
requirements: [FIN-07, NOTIF-03]
gap_closure: true

must_haves:
  truths:
    - "Entity detail page shows a Plaid bank balance card when a Plaid connection exists for that entity"
    - "waterfall.test.ts in __tests__ folder compiles without TypeScript errors"
    - "eslint-disable any-type workarounds cleaned up where feasible (proper types replacing blanket any)"
  artifacts:
    - path: "src/app/(gp)/entities/[id]/page.tsx"
      provides: "Entity detail page with Plaid balance card"
      contains: "plaid"
    - path: "src/lib/computations/__tests__/waterfall.test.ts"
      provides: "Fixed waterfall test file"
  key_links:
    - from: "src/app/(gp)/entities/[id]/page.tsx"
      to: "/api/integrations/plaid/accounts"
      via: "useSWR fetch"
      pattern: "plaid/accounts"
---

<objective>
Clean up tech debt items from the v1.0 milestone audit: add Plaid balance card to entity detail page, fix waterfall test TypeScript error, and clean up eslint-disable any-type workarounds where possible.

Purpose: Close cosmetic tech debt and minor UI gaps flagged in the audit. These support the broader Phase 9 goal of polish and code quality.

Output: Entity detail page with bank balance visibility, clean test compilation, reduced eslint suppressions.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/api/integrations/plaid/accounts/route.ts (GET response):
```typescript
// When connected:
{
  connected: true,
  entityId: string,
  accounts: PlaidAccountData[],    // from src/lib/integrations/plaid.ts
  transactions: PlaidTransaction[] // last 50
}
// When not connected:
{
  connected: false,
  accounts: [],
  transactions: []
}
```

From src/lib/integrations/plaid.ts:
```typescript
export interface PlaidAccountData {
  accountId: string;
  name: string;
  officialName: string | null;
  type: string;
  subtype: string | null;
  currentBalance: number | null;
  availableBalance: number | null;
}
```

From src/lib/computations/waterfall.ts:
```typescript
export function computeWaterfall(
  tiers: WaterfallTierInput[],
  distributableAmount: number,
  totalContributed: number,
  totalDistributedPrior?: number,
  yearsOutstanding?: number,
  config?: WaterfallConfig,
  investorShares?: InvestorShare[]
): WaterfallResult;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Plaid balance card to entity detail page + fix waterfall test</name>
  <files>src/app/(gp)/entities/[id]/page.tsx, src/lib/computations/__tests__/waterfall.test.ts</files>
  <action>
**Plaid balance card on entity detail page:**

In `src/app/(gp)/entities/[id]/page.tsx`:

1. Add a SWR fetch for Plaid accounts: `const { data: plaidData } = useSWR(id ? `/api/integrations/plaid/accounts?entityId=${id}` : null, fetcher);`

2. In the "overview" tab section, add a "Bank Accounts" card (only rendered when `plaidData?.connected === true`). Card design:
   - Header: "Bank Accounts (Plaid)" with a small green "Connected" badge
   - For each account in `plaidData.accounts`: show account name, type, current balance (use `fmt()` from utils), and available balance
   - If no accounts: show "No accounts found"
   - If `plaidData?.connected === false` or `plaidData` is null: do NOT render the card at all (hide completely -- user connects via Settings > Integrations)

3. Place the card in the overview tab after the existing overview content, using the same card styling pattern as other cards on the page (rounded border, padding, shadow).

**Fix waterfall.test.ts TypeScript error:**

In `src/lib/computations/__tests__/waterfall.test.ts`:

The file uses `beforeEach` (line 48) which is not imported. TypeScript cannot find the name. Two options:
- Option A (preferred): Add `import { describe, it, expect, beforeEach } from "vitest";` at line 1 (add `beforeEach` to existing import).
- Option B: Remove `beforeEach` and inline the computation like the root-level test file does.

Use Option A -- just add `beforeEach` to the existing vitest import on line 1. This is the minimal fix.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npx tsc --noEmit 2>&1 | grep -c "waterfall" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Entity detail page overview tab shows Plaid bank balance card when connected
- Card hidden when no Plaid connection exists (no visual change for entities without Plaid)
- `__tests__/waterfall.test.ts` imports `beforeEach` from vitest -- zero TypeScript errors on that file
- Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up eslint-disable any-type workarounds</name>
  <files>src/components/features/deals/deal-dd-tab.tsx, src/components/features/deals/deal-overview-tab.tsx, src/components/features/lp/performance-charts.tsx</files>
  <action>
Clean up `eslint-disable @typescript-eslint/no-explicit-any` where possible. The audit flagged these files:

**1. `deal-dd-tab.tsx`:**
- Has blanket `/* eslint-disable @typescript-eslint/no-explicit-any */` at line 15
- `DealDDTabProps` uses `deal: any`
- Create a proper `Deal` interface with the fields actually used by this component (inspect the component body for accessed properties like `deal.id`, `deal.workstreams`, `deal.stage`, etc.)
- Replace `deal: any` with the typed interface
- Remove the blanket eslint-disable. If any remaining `any` usages are truly needed (e.g., API response parsing), use inline `// eslint-disable-next-line` only on those specific lines

**2. `deal-overview-tab.tsx`:**
- Has blanket `/* eslint-disable @typescript-eslint/no-explicit-any */` at line 20
- Same pattern: `deal: any` prop. Create a typed interface for the deal properties used in this component.
- Replace blanket disable with targeted inline disables only where unavoidable

**3. `performance-charts.tsx`:**
- Has 2 inline `// eslint-disable-next-line @typescript-eslint/no-explicit-any` on Recharts Tooltip formatter (lines 138, 222)
- These are EXPECTED per STATE.md decision: "Recharts v3 Tooltip formatter typed as any with eslint-disable -- Formatter<T,N> generic causes TS incompatibility"
- Leave these as-is. They are already properly scoped (inline, not blanket). No action needed.

**Important:** When creating typed interfaces for the deal prop, keep them pragmatic -- use the actual fields accessed, not a comprehensive Deal model. Use optional fields generously since the data comes from API responses. If some deeply nested fields are genuinely untyped (e.g., JSON metadata), use `Record<string, unknown>` instead of `any`.

If removing blanket eslint-disable causes more than 5 new lint errors in a file, keep the blanket disable and add a comment explaining why (e.g., "// Complex API response type -- see DATA-MODEL.md for full schema"). The goal is improvement, not perfection.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- deal-dd-tab.tsx: blanket eslint-disable removed or replaced with targeted disables, deal prop typed
- deal-overview-tab.tsx: blanket eslint-disable removed or replaced with targeted disables, deal prop typed
- performance-charts.tsx: inline Recharts any disables left as-is (documented as expected)
- Build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npx tsc --noEmit 2>&1 | grep waterfall` returns zero hits (no more TS errors)
3. `npx vitest run src/lib/computations/__tests__/waterfall.test.ts` -- all tests pass
4. Entity detail overview tab renders Plaid card when connected
5. `grep -c "eslint-disable @typescript-eslint/no-explicit-any" src/components/features/deals/deal-dd-tab.tsx src/components/features/deals/deal-overview-tab.tsx` shows reduction from blanket to targeted (or zero)
</verification>

<success_criteria>
- Plaid entity detail balance card shows live bank data when Plaid connected
- waterfall.test.ts TypeScript error fixed (zero TS errors on that file)
- eslint-disable any-type workarounds cleaned up where possible (deal tabs typed, Recharts left as-is)
- Zero build errors across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/09-financial-wiring-polish/09-02-SUMMARY.md`
</output>
