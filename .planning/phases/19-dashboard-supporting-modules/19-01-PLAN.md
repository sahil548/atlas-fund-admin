---
phase: 19-dashboard-supporting-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/dashboard/pipeline-summary/route.ts
  - src/app/api/dashboard/alerts/route.ts
  - src/lib/__tests__/phase19-dashboard-apis.test.ts
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "Pipeline summary API returns deals grouped by stage with count and aggregate value"
    - "Alerts API returns overdue capital calls, covenant breaches, and lease expirations within 90 days"
    - "Both APIs filter by firmId and handle empty result sets gracefully"
  artifacts:
    - path: "src/app/api/dashboard/pipeline-summary/route.ts"
      provides: "Deal pipeline summary grouped by stage"
      exports: ["GET"]
    - path: "src/app/api/dashboard/alerts/route.ts"
      provides: "Cross-module alert aggregation"
      exports: ["GET"]
    - path: "src/lib/__tests__/phase19-dashboard-apis.test.ts"
      provides: "Unit tests for pipeline grouping and alert filtering logic"
  key_links:
    - from: "src/app/api/dashboard/pipeline-summary/route.ts"
      to: "prisma.deal.groupBy"
      via: "Prisma groupBy query"
      pattern: "groupBy.*stage"
    - from: "src/app/api/dashboard/alerts/route.ts"
      to: "prisma.capitalCall + prisma.covenant + prisma.lease"
      via: "Promise.all parallel queries"
      pattern: "Promise\\.all"
---

<objective>
Build the two new dashboard API endpoints that power the morning briefing: pipeline summary (deals by stage with aggregate values) and alerts (overdue capital calls, covenant breaches, lease expirations). Include Wave 0 unit tests for the data transformation logic.

Purpose: These APIs are the data backbone for DASH-01 (pipeline funnel) and DASH-02 (needs-attention alerts) -- the dashboard UI in Plans 03/05 will consume them.
Output: Two API routes + test file with passing tests.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-supporting-modules/19-RESEARCH.md

<interfaces>
<!-- Existing API pattern from dashboard stats route -->
From src/app/api/dashboard/stats/route.ts:
```typescript
import { prisma } from "@/lib/prisma";
import { NextResponse } from "next/server";
import { getAuthUser } from "@/lib/auth";
// Uses getAuthUser() for firmId, returns NextResponse.json()
// Uses directFilter = firmId ? { firmId } : {} pattern
// Uses entityRelFilter = firmId ? { entity: { firmId } } : {} pattern
```

From prisma/schema.prisma (relevant enums):
```prisma
enum DealStage { SCREENING DUE_DILIGENCE IC_REVIEW CLOSING CLOSED DEAD }
enum CapitalCallStatus { DRAFT ISSUED PARTIALLY_FUNDED FUNDED CANCELLED }
enum CovenantStatus { COMPLIANT WARNING BREACH }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests + pipeline summary API</name>
  <files>src/lib/__tests__/phase19-dashboard-apis.test.ts, src/app/api/dashboard/pipeline-summary/route.ts</files>
  <behavior>
    - Test: groupPipelineStages maps grouped Prisma output to ordered stage array (SCREENING, DUE_DILIGENCE, IC_REVIEW, CLOSING)
    - Test: Missing stages in DB result get count=0 and totalValue=0
    - Test: DEAD and CLOSED deals are excluded from pipeline summary
    - Test: Null dealValue sums to 0 (not NaN)
  </behavior>
  <action>
    1. Create test file `src/lib/__tests__/phase19-dashboard-apis.test.ts` with tests for a pure `groupPipelineStages` function that takes raw Prisma groupBy output and returns the ordered stage array.
    2. Create `src/app/api/dashboard/pipeline-summary/route.ts` with GET handler:
       - Use `getAuthUser()` for firmId (same pattern as stats/route.ts)
       - Fall back to `req.nextUrl.searchParams.get("firmId")` if no auth user
       - Query: `prisma.deal.groupBy({ by: ["stage"], where: { firmId, status: { not: "DEAD" }, stage: { notIn: ["CLOSED", "DEAD"] } }, _count: { stage: true }, _sum: { dealValue: true } })`
       - Map results to ordered array: `[{ stage: "SCREENING", count, totalValue }, ...]`
       - Include all 4 active stages (SCREENING, DUE_DILIGENCE, IC_REVIEW, CLOSING) even if count=0
       - Return `{ stages: [...] }`
    3. Extract the pure mapping function to be testable (inline in the route file or as a helper).
    4. Run tests to confirm they pass.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx vitest run src/lib/__tests__/phase19-dashboard-apis.test.ts --reporter=verbose</automated>
  </verify>
  <done>Pipeline summary API returns { stages: [{ stage, count, totalValue }] } for all 4 active deal stages. Tests pass for stage ordering, missing stages, and null value handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alerts API endpoint</name>
  <files>src/app/api/dashboard/alerts/route.ts, src/lib/__tests__/phase19-dashboard-apis.test.ts</files>
  <behavior>
    - Test: categorizeAlerts groups items by type correctly (OVERDUE_CAPITAL_CALL, COVENANT_BREACH, LEASE_EXPIRY)
    - Test: Alert items include linkPath for navigation
    - Test: Counts object tallies each alert type
    - Test: Empty alert arrays return { alerts: [], counts: { overdueCapitalCalls: 0, covenantBreaches: 0, leaseExpiries: 0 } }
  </behavior>
  <action>
    1. Add tests to existing `phase19-dashboard-apis.test.ts` for a pure `buildAlerts` function that transforms raw Prisma query results into the alerts response shape.
    2. Create `src/app/api/dashboard/alerts/route.ts` with GET handler:
       - Use `getAuthUser()` for firmId with searchParams fallback
       - Run 3 parallel Prisma queries via Promise.all:
         (a) Overdue capital calls: `prisma.capitalCall.findMany({ where: { entity: { firmId }, status: "ISSUED", dueDate: { lt: today } }, include: { entity: { select: { id: true, name: true } } }, take: 20 })`
         (b) Covenant breaches: `prisma.covenant.findMany({ where: { creditAgreement: { asset: { entityAllocations: { some: { entity: { firmId } } } } }, currentStatus: "BREACH" }, include: { creditAgreement: { include: { asset: { select: { id: true, name: true } } } } }, take: 20 })`
         (c) Expiring leases: `prisma.lease.findMany({ where: { asset: { entityAllocations: { some: { entity: { firmId } } } }, currentStatus: "ACTIVE", leaseEndDate: { gte: today, lte: ninetyDaysFromNow } }, include: { asset: { select: { id: true, name: true } } }, take: 20 })`
       - Transform each into alert objects: `{ type, severity, title, entityId/assetId, linkPath, date }`
       - Overdue calls: severity="high", linkPath=`/entities/${entityId}`
       - Covenant breaches: severity="high", linkPath=`/assets/${assetId}`
       - Lease expiries: severity="medium", linkPath=`/assets/${assetId}`
       - Return `{ alerts: [...sortedByDate], counts: { overdueCapitalCalls, covenantBreaches, leaseExpiries } }`
    3. Run tests and build.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx vitest run src/lib/__tests__/phase19-dashboard-apis.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <done>Alerts API returns categorized alerts with navigation links and count summary. Tests pass. Build succeeds with zero type errors.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/phase19-dashboard-apis.test.ts` passes all tests
- `npm run build` succeeds with zero errors
- GET /api/dashboard/pipeline-summary?firmId=firm-1 returns JSON with stages array
- GET /api/dashboard/alerts?firmId=firm-1 returns JSON with alerts array and counts object
</verification>

<success_criteria>
- Pipeline summary groups active deals by stage with count and aggregate dealValue
- Alerts endpoint aggregates overdue capital calls, covenant breaches, and 90-day lease expirations
- Both endpoints handle empty data gracefully (no crashes, return empty arrays)
- All tests green, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-supporting-modules/19-01-SUMMARY.md`
</output>
