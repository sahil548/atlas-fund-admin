---
phase: 19-dashboard-supporting-modules
plan: 05
type: execute
wave: 3
depends_on: ["19-03", "19-04"]
files_modified:
  - src/app/(gp)/dashboard/page.tsx
  - src/components/features/dashboard/asset-allocation-chart.tsx
  - src/components/features/dashboard/portfolio-aggregates.tsx
  - src/lib/__tests__/phase19-dashboard-assembly.test.ts
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
must_haves:
  truths:
    - "Dashboard is the definitive morning briefing: summary bar, alerts, funnel, entity cards, portfolio charts, activity feed -- all on one page"
    - "Asset allocation chart is a clean single-ring donut (not the broken 3-dimension nested pie)"
    - "Activity feed is at the bottom of the dashboard, full-width, with type and entity filtering"
    - "All dashboard sections work in both light and dark mode"
    - "The entire dashboard loads without console errors on localhost"
  artifacts:
    - path: "src/app/(gp)/dashboard/page.tsx"
      provides: "Complete restructured dashboard page"
      min_lines: 60
    - path: "src/components/features/dashboard/asset-allocation-chart.tsx"
      provides: "Redesigned simple donut chart"
      contains: "PieChart"
    - path: "src/lib/__tests__/phase19-dashboard-assembly.test.ts"
      provides: "Assembly verification tests for final dashboard wiring"
  key_links:
    - from: "src/app/(gp)/dashboard/page.tsx"
      to: "src/components/features/dashboard/activity-feed-section.tsx"
      via: "import ActivityFeedSection"
      pattern: "ActivityFeedSection"
    - from: "src/components/features/dashboard/asset-allocation-chart.tsx"
      to: "/api/dashboard/asset-allocation"
      via: "SWR fetch (same endpoint, simplified rendering)"
      pattern: "useSWR.*asset-allocation"
---

<objective>
Final dashboard assembly: wire the ActivityFeedSection into the dashboard page, redesign the asset allocation chart from a broken 3-dimension nested pie to a clean single-ring donut, and verify the complete dashboard end-to-end with a human checkpoint. Include assembly verification tests.

Purpose: This plan completes the dashboard as the GP's morning briefing surface. The chart redesign fixes the broken visualization, and the human verification ensures everything looks correct visually.
Output: Complete, polished dashboard page with all 5 sections + redesigned chart + tests.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-dashboard-supporting-modules/19-RESEARCH.md
@.planning/phases/19-dashboard-supporting-modules/19-03-SUMMARY.md
@.planning/phases/19-dashboard-supporting-modules/19-04-SUMMARY.md

<interfaces>
<!-- ActivityFeedSection (from Plan 04) -->
```typescript
// Self-contained component, no props needed -- fetches its own data via SWR
export function ActivityFeedSection(): JSX.Element;
```

<!-- AssetAllocationChart current interface (to be simplified) -->
```typescript
// Fetches from /api/dashboard/asset-allocation
// Current response: { innerRing: [...], outerRing: [...] }
// outerRing items: { name: "REAL_ESTATE"|..., value: number, equityValue: number, debtValue: number }
// We only need outerRing for the simplified donut
```

<!-- PortfolioAggregates current layout -->
```typescript
// Currently renders: AssetAllocationChart, TopBottomPerformers, CapitalDeploymentTracker, RecentActivityFeed
// RecentActivityFeed will be REMOVED from PortfolioAggregates (replaced by ActivityFeedSection)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire activity feed into dashboard + redesign asset allocation chart + assembly tests</name>
  <files>src/app/(gp)/dashboard/page.tsx, src/components/features/dashboard/asset-allocation-chart.tsx, src/components/features/dashboard/portfolio-aggregates.tsx, src/lib/__tests__/phase19-dashboard-assembly.test.ts</files>
  <action>
    1. Redesign `src/components/features/dashboard/asset-allocation-chart.tsx`:
       - Replace the broken double-ring nested pie chart with a clean single-ring donut chart
       - Use only the `outerRing` data from `/api/dashboard/asset-allocation` (asset class breakdown by fair value)
       - Remove the inner ring (participation structure) entirely
       - Remove the opacity-based capital instrument encoding (debt/equity shading)
       - Single Pie component: `<Pie data={data.outerRing} dataKey="value" cx="50%" cy="50%" outerRadius={110} innerRadius={60} paddingAngle={2} />`
       - Add clean label rendering: show asset class name + percentage on segments > 5%
       - Keep ASSET_CLASS_HEX color mapping
       - Custom tooltip: show asset class label + `fmt(value)` + percentage
       - Simplified legend below chart: single row of colored dots + asset class names
       - Remove the 3-section legend (outer ring, inner ring, shading)
       - Dark mode: ensure chart container has dark:bg-gray-900 and legend text uses dark:text-gray-300
       - Center label inside donut: "Total" + fmt(sum of all values) using Recharts label prop or custom positioned text
       - Chart height: 250px (slightly smaller than current 300px)

    2. Update `src/components/features/dashboard/portfolio-aggregates.tsx`:
       - Remove the RecentActivityFeed import and rendering from PortfolioAggregates
       - PortfolioAggregates now only renders 3 items: AssetAllocationChart, TopBottomPerformers, CapitalDeploymentTracker
       - Change grid from `grid-cols-2` to a layout that works for 3 items: keep `grid-cols-2 gap-4` but let the third item (CapitalDeploymentTracker) span full width below, or use `grid-cols-1 lg:grid-cols-3 gap-4` if the items fit
       - Remove the `recentActivity` prop usage

    3. Wire ActivityFeedSection into `src/app/(gp)/dashboard/page.tsx`:
       - Add import: `import { ActivityFeedSection } from "@/components/features/dashboard/activity-feed-section"`
       - Add the activity feed section at the BOTTOM of the page (per locked decision: "Activity feed at the bottom of the page, full-width"):
         ```
         {/* Activity Feed -- full-width at bottom */}
         <section>
           <SectionErrorBoundary>
             <ActivityFeedSection />
           </SectionErrorBoundary>
         </section>
         ```
       - Ensure the final page layout order is:
         1. PageHeader
         2. SummaryBar
         3. NeedsAttentionPanel
         4. DealPipelineFunnel
         5. Entity Cards grid
         6. Portfolio Overview (TopBottomPerformers + CapitalDeployment + redesigned AssetAllocation)
         7. ActivityFeedSection (full-width)

    4. Create `src/lib/__tests__/phase19-dashboard-assembly.test.ts` with assembly verification tests:
       - Test: Verify dashboard page file imports all 7 expected dashboard sections (grep for import statements: SummaryBar, NeedsAttentionPanel, DealPipelineFunnel, EntityCard, PortfolioAggregates, ActivityFeedSection)
       - Test: Verify asset-allocation-chart.tsx does NOT contain "innerRing" rendering (the broken nested pie pattern is removed)
       - Test: Verify portfolio-aggregates.tsx does NOT import RecentActivityFeed (replaced by ActivityFeedSection)
       - Test: Verify dashboard page.tsx does NOT contain "LPComparison" (removed per locked decision)
       - These are file-content assertion tests (grep-as-test pattern), which reliably verify wiring without needing DOM rendering.

    5. Run tests and build to verify everything compiles.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx vitest run src/lib/__tests__/phase19-dashboard-assembly.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <done>Dashboard has all 7 sections wired. Asset allocation chart is a clean single-ring donut. Activity feed sits at bottom full-width. RecentActivityFeed removed from PortfolioAggregates. Assembly tests pass. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 19 dashboard and supporting modules</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 19 output: dashboard restructure (summary bar, alerts, pipeline funnel, compact entity cards, donut chart, activity feed) and supporting modules (report preview, report grouping, entity detail Reports tab, integrations status, notification preferences).
  </action>
  <verify>Human confirms all sections render correctly in browser at localhost:3000</verify>
  <done>User approves the dashboard as the morning briefing surface and confirms supporting module polish items work correctly.</done>
  <what-built>
    Complete Phase 19 dashboard restructure and supporting modules:
    - Dashboard: summary bar (NAV, IRR, TVPI, deals, dry powder) at top
    - Needs attention panel (overdue calls, breaches, lease expirations)
    - Deal pipeline funnel (horizontal, clickable stages)
    - Compact entity cards (2-row, 4-6 per row, quick action icons)
    - Redesigned asset allocation donut chart (replaces broken 3D nested pie)
    - Full-width activity feed with type chips and entity filter
    - Top/Bottom performers and Capital deployment retained
    - LP Comparison removed
    - Reports page: preview modal + grouped-by-entity history
    - Entity detail pages: Reports tab showing entity-specific reports
    - Settings > Integrations: green/red status dots + last sync time
    - Settings > Notifications: email/SMS toggle + digest frequency
    - AI config test connection verified (pre-existing)
    - Zero window.confirm() calls confirmed
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/dashboard
    2. Verify summary bar at top shows Total NAV, IRR, TVPI, Active Deals, Dry Powder
    3. Below summary: "Needs Attention" panel (may show alerts or "All clear" depending on seed data)
    4. Below alerts: horizontal pipeline funnel with stage counts -- click a stage to verify it navigates to /deals filtered
    5. Below funnel: compact entity cards (should fit 4+ per row on wide screen). Hover over icon buttons to see tooltips (View Entity, Create Capital Call, Generate Report). Click one to verify navigation.
    6. Below cards: portfolio section with clean donut chart (NOT the old broken nested rings), top/bottom performers, capital deployment
    7. At bottom: activity feed with type chip toggles and entity dropdown. Click different chips to filter. Click "Load more" if available.
    8. Verify NO "LP Comparison" section appears
    9. Toggle dark mode -- verify all sections have dark backgrounds and readable text
    10. Open http://localhost:3000/reports -- click "Preview" on a report to verify modal opens with PDF iframe. Verify reports are grouped by entity then period.
    11. Navigate to an entity detail page (click an entity card on dashboard). Verify a "Reports" tab exists. Click it to see entity-specific reports.
    12. Open http://localhost:3000/settings -- go to Integrations tab, verify green/red dots and last sync timestamps. Go to Notifications tab, verify email/SMS toggles and save button.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/phase19-dashboard-assembly.test.ts` passes all tests
- `npm run build` passes with zero errors
- `npm test` passes all existing + new tests
- Dashboard loads at localhost:3000/dashboard with all 7 sections visible
- Asset allocation chart is a simple donut (no nested rings or opacity encoding)
- Activity feed filters work (type chips and entity dropdown)
- Reports preview modal works
- Entity detail page has Reports tab
- Settings integrations and notifications tabs are functional
- Dark mode renders correctly across all sections
</verification>

<success_criteria>
- The dashboard is the GP's definitive morning briefing: one page shows pipeline status, alerts, entity metrics, portfolio analytics, and activity stream
- All DASH requirements (01-04) are visually verifiable on the dashboard page
- All SUPP requirements (01-06) are verifiable in reports, entity detail, and settings pages
- Human approval confirms the dashboard looks correct and all interactions work
</success_criteria>

<output>
After completion, create `.planning/phases/19-dashboard-supporting-modules/19-05-SUMMARY.md`
</output>
