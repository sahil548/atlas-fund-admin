---
phase: 21-initial-manual-walkthrough-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/walkthroughs/v3.0-gp-baseline.md
  - .planning/walkthroughs/v3.0-lp-baseline.md
  - .planning/walkthroughs/SESSION-NOTES.md
  - .planning/ROADMAP.md
  - .planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md
autonomous: false
requirements:
  - MAN-01
  - MAN-02

must_haves:
  truths:
    - "`.planning/walkthroughs/v3.0-gp-baseline.md` exists and is populated with the user's GP-side observations plus Claude's triage for every observation"
    - "`.planning/walkthroughs/v3.0-lp-baseline.md` exists and is populated with the user's LP-side observations (from both Michael Chen and Tom Wellington sessions) plus Claude's triage for every observation"
    - "Every observation in both baseline files carries an explicit triage decision (Urgent → v3.0 / Already scoped → Phase N / Defer → v3.1+) with a Triage Reason that references the 3-part rubric"
    - "Every Urgent item has a concrete Proposed Destination — either a Phase 22-27 plan/section that absorbs it or an entry in `.planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md`"
    - "Phases 22-27 that receive urgent items have those items recorded in their ROADMAP.md phase section (in-place update) so downstream planners see them when those phases get planned"
    - "All 4 validation layers from `21-VALIDATION.md` pass (file existence, schema, triage sanity, ROADMAP cross-reference)"
  artifacts:
    - path: ".planning/walkthroughs/v3.0-gp-baseline.md"
      provides: "GP walkthrough script + user's captured observations + Claude's triage + triage summary tables"
      contains: "Pre-Walkthrough Setup, 11 GP Domain sections, Overall Impressions, Triage Summary (Urgent / Already Scoped / Defer)"
    - path: ".planning/walkthroughs/v3.0-lp-baseline.md"
      provides: "LP walkthrough script (Michael Chen pass + Tom Wellington pass) + captured observations + Claude's triage + triage summary tables"
      contains: "Pre-Walkthrough Setup, Pass 1 (CalPERS, 7 LP domains), Pass 2 (Wellington), Triage Summary"
    - path: ".planning/walkthroughs/SESSION-NOTES.md"
      provides: "Pre-session environment notes (branch merge status, seed freshness, known gaps the user should expect)"
      contains: "feat/edit-delete-across-entities merge status, BUG-01/02/03 reproduction guidance, LP seed caveat (no true single-vehicle LP)"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 22-27 sections with in-place 'Additions from walkthrough' notes for any urgent items folded into those phases"
      contains: "Phase 21 marked complete in Progress table; 'Additions from walkthrough' subsections under destination phases"
    - path: ".planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md"
      provides: "Urgent items that don't fit Phases 22-27 land here for Phase 28 pickup"
      contains: "Table of urgent items with Source, Observation, Rubric Justification"
  key_links:
    - from: ".planning/walkthroughs/v3.0-gp-baseline.md"
      to: ".planning/walkthroughs/v3.0-gp-baseline.md#triage-summary"
      via: "Every Observation block in the domain sections has a matching row in the Triage Summary tables"
      pattern: "Each domain observation resolves to exactly one row in one of the three triage tables"
    - from: ".planning/walkthroughs/v3.0-gp-baseline.md (Urgent → v3.0 rows)"
      to: ".planning/ROADMAP.md (Phase 22-27 sections) OR .planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md"
      via: "Wave 4 in-place folding step"
      pattern: "Every 'Urgent → v3.0' item with Destination=Phase N appears either as a note under that phase in ROADMAP.md OR as a row in 28-BACKLOG.md"
    - from: "user's solo GP click-through session"
      to: ".planning/walkthroughs/v3.0-gp-baseline.md (capture blocks)"
      via: "User types observations directly into the capture blocks during the solo session; Claude does not observe in real time"
      pattern: "Capture blocks change from placeholder `[capture block — user fills in here]` to populated Observation N entries"
---

<objective>
Phase 21 captures a first-person baseline of how the Atlas app feels today — before any v3.0 changes begin — by running two walkthrough sessions (GP as James Kim, then LP as Michael Chen + Tom Wellington) against a freshly-seeded dev build. Claude's job in this plan is the scaffolding (walkthrough scripts embedded in the two baseline files, a pre-session notes file, and the triage framework) and the post-session synthesis (applying the locked 3-part rubric to every captured observation, then folding urgent items into the downstream Phase 22-27 sections in-place OR into a Phase 28 follow-up backlog).

Purpose: Shape Phases 22-27 priorities with real-world friction signal, not planning assumptions. Also acts as a forcing function to catch anything the off-GSD v2.1 work introduced that the retroactive audit missed.

Output: Two populated walkthrough baseline files, a session notes file, ROADMAP.md updates folding urgent items into destination phases, and (if any urgent items don't fit P22-27) a Phase 28 backlog file.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/UI-GUIDE.md
@.planning/phases/21-initial-manual-walkthrough-baseline/21-CONTEXT.md
@.planning/phases/21-initial-manual-walkthrough-baseline/21-RESEARCH.md
@.planning/phases/21-initial-manual-walkthrough-baseline/21-VALIDATION.md
@CLAUDE.md
@.claude/rules/coding-patterns.md

<interfaces>
<!-- Key reference material the executor will use verbatim. Pulled from 21-RESEARCH.md so the executor does not need to re-derive. -->

**Triage Rubric (LOCKED, from 21-CONTEXT.md § Triage rubric).** An item is `Urgent → v3.0` only if ALL THREE are true:
  1. Materially blocks a user completing a real workflow (not aesthetic)
  2. Fixable inside existing Phase 22-27 scope OR a small Phase 28 follow-up (no new phase needed)
  3. Does NOT introduce new feature surface

Failing any one test → `Defer → v3.1+` with a one-line reason citing the failed test.

**Per-observation capture schema (from 21-RESEARCH.md § Capture Template Design).** Every observation block contains:
  - `**Page / Action:**` — not blank
  - `**What I saw:**` — not blank
  - Severity checkbox (exactly one of `[x] Blocker` / `[x] Annoying` / `[x] Nitpick`)
  - `**Claude Triage:**` checkbox (exactly one of `[x] Urgent → v3.0` / `[x] Already scoped → Phase N` / `[x] Defer → v3.1+`)
  - `**Triage Reason:**` — must name which rubric test(s) pass/fail
  - `**Proposed Destination:**` — one of `Phase 22` / `Phase 23` / `Phase 24` / `Phase 25` / `Phase 26` / `Phase 27` / `Phase 28-backlog` / `v3.1-backlog`

**LP user selection (LOCKED by 21-RESEARCH.md § LP Seed-Data Inventory — Karen Miller is NOT seeded):**
  - Pass 1 (multi-vehicle): `user-lp-calpers` (Michael Chen, CalPERS, 5 vehicles, active capital calls + distributions)
  - Pass 2 ("simpler" LP): `user-lp-wellington` (Tom Wellington, Wellington Family Office, 3 vehicles — note: no true single-vehicle LP exists in seed; this is the closest)

**March-5 bug re-verification hooks (from 21-RESEARCH.md § March-5 Bug Reproduction Details):**
  - BUG-01: DD tab 0%/NOT_STARTED on post-DD deals → `src/components/features/deals/deal-dd-tab.tsx`
  - BUG-02: Pipeline pass rate 300% → `src/app/(gp)/deals/page.tsx`
  - BUG-03: IC Memo stuck "Generating..." spinner → `src/components/features/deals/deal-overview-tab.tsx`
  If any bug reproduces → Urgent → Phase 22 (FIN-08). If none reproduce → note "could not reproduce" and BUG status flips to "resolved" in Phase 22 scope.

**GP domain order (LOCKED, from 21-CONTEXT.md):** Dashboard → Deal Desk → Assets → Entities → Capital Activity+Waterfall+Transactions (collapsed into one domain per 21-RESEARCH.md § Routes in CONTEXT.md Order) → Cap Table (sub-section of Entities) → Directory/CRM → Tasks → Documents → Meetings → AI command bar (sampled) → Settings.

**LP domain order (LOCKED, from 21-CONTEXT.md, adjusted for actual routes):** Dashboard (`/lp-dashboard`) → Capital Account (`/lp-account`, inserted per 21-RESEARCH.md) → Portfolio (`/lp-portfolio`) → Notices & Activity (`/lp-activity`) → Documents (`/lp-documents`) → Settings/Notifications (`/lp-settings`) → Profile (`/lp-profile`).

**Full baseline file skeletons:** See `21-RESEARCH.md` § "Baseline File Skeleton (GP + LP)" — use those skeletons verbatim as the starting structure for both files. The executor should not invent a new format.

**Validation commands (from 21-VALIDATION.md Layers 1-4):** Copy the exact grep/ls/test commands from that file into the Wave 4 validation step.
</interfaces>
</context>

<tasks>

<!-- ======================= WAVE 0 (autonomous scaffolding) ======================= -->

<task type="auto">
  <name>Wave 0 — Task 1: Create walkthroughs directory and pre-session notes</name>
  <files>
    .planning/walkthroughs/SESSION-NOTES.md
  </files>
  <action>
Create the `.planning/walkthroughs/` directory (it does not exist yet — `ls .planning/walkthroughs/` returns "No such file or directory" as of planning time) and write `SESSION-NOTES.md` inside it with pre-session context the user needs to see before starting either walkthrough.

Commands:
```bash
mkdir -p .planning/walkthroughs
```

Then write `SESSION-NOTES.md` with these sections (plain English, non-developer tone per CLAUDE.md — Sahil is not a developer):

1. **"What this file is"** — one short paragraph explaining this is a pre-flight checklist for the Phase 21 walkthroughs.

2. **"Before you start either session"** — a numbered checklist:
   - Run `npx prisma db seed` to reset demo data to a known state
   - Start `npm run dev` on port 3000
   - Open http://localhost:3000 in a browser
   - Capture the current git commit hash: run `git rev-parse --short HEAD` and write it in the session header
   - Capture session date and browser name in the session header

3. **"Branch merge status — feat/edit-delete-across-entities"** — explain in plain English:
   - v2.1 ROADMAP.md mentions a 6-commit unmerged branch `feat/edit-delete-across-entities` that contains additional CRUD edit/delete wiring
   - As of planning time, this branch does NOT appear locally (confirmed via `git branch -a | grep edit-delete` returning nothing)
   - **What this means for the walkthrough:** If you try to edit or delete something on pages like `/assets/[id]` > income/expenses or `/entities/[id]` > cap-table and the controls are missing or broken, it may be because those changes live on an unmerged branch rather than being a bug. Note it as an observation anyway — the triage will sort it out.
   - Before the GP session, run `git branch -a | grep edit-delete` and paste the output into the session header. If it prints nothing, the branch is not pulled down; treat the tested build as "main only."

4. **"March-5 bug re-verification"** — list the three bugs verbatim from 21-RESEARCH.md § March-5 Bug Reproduction Details (BUG-01 DD tab 0%/NOT_STARTED, BUG-02 pipeline pass rate 300%, BUG-03 IC Memo stuck spinner) with plain-English reproduction steps. Tell the user: "These are baked into the GP walkthrough script — you'll be prompted to check them in-flow. Flag in the capture block whether each bug still reproduces or appears resolved."

5. **"LP user selection caveat"** — plain-English callout:
   - The CONTEXT.md originally mentioned "Karen Miller" as the multi-vehicle LP, but research confirmed Karen Miller is not in the seed data.
   - Pass 1 will use Michael Chen (CalPERS, 5 vehicles)
   - Pass 2 will use Tom Wellington (Wellington Family Office, 3 vehicles) — note: there is no true single-vehicle LP in the seed, so Pass 2 is a "simpler LP" sanity pass, not a strict single-vehicle test.
   - The user switcher in the local dev app (mock UserProvider) is how you change LP identity — no login flow.

6. **"BUG-03 prerequisite"** — note that IC Memo generation may require an AI API key configured in Settings > AI Config; if none is set, skip BUG-03 check rather than misreading a missing-key error as a stuck spinner.

7. **"DD tab test data prerequisite"** — to exercise BUG-01 the seed must have at least one deal past DUE_DILIGENCE stage (IC_REVIEW or CLOSING). If none exist post-seed, the script instructs you to advance one deal using "Send to IC Review" before checking the DD tab.

Keep tone plain English. No code blocks beyond the literal shell commands. No React/TypeScript jargon. Sahil should be able to read this top-to-bottom in two minutes and know exactly what to do.
  </action>
  <verify>
    <automated>test -d .planning/walkthroughs && test -f .planning/walkthroughs/SESSION-NOTES.md && [ -s .planning/walkthroughs/SESSION-NOTES.md ] && grep -q "feat/edit-delete-across-entities" .planning/walkthroughs/SESSION-NOTES.md && grep -q "Michael Chen" .planning/walkthroughs/SESSION-NOTES.md && grep -q "Tom Wellington" .planning/walkthroughs/SESSION-NOTES.md && grep -q "BUG-01" .planning/walkthroughs/SESSION-NOTES.md && echo "SESSION-NOTES.md OK"</automated>
  </verify>
  <done>
`.planning/walkthroughs/` directory exists. `SESSION-NOTES.md` exists, is non-empty, and references all 7 required sections: what-this-is, pre-flight checklist, branch merge status note, 3 March-5 bugs, LP user selection caveat, BUG-03 AI key prerequisite, BUG-01 post-DD deal prerequisite. File reads as plain English (no React/TypeScript jargon).
  </done>
</task>

<task type="auto">
  <name>Wave 0 — Task 2: Scaffold GP baseline file with full walkthrough script</name>
  <files>
    .planning/walkthroughs/v3.0-gp-baseline.md
  </files>
  <action>
Create `.planning/walkthroughs/v3.0-gp-baseline.md` by using the skeleton in `21-RESEARCH.md` § "Baseline File Skeleton (GP + LP)" as the literal starting structure, then expanding each domain's "What to look at" section per the GP Route Inventory (21-RESEARCH.md § GP Route Inventory) and the v2.1 High-Risk Surface Map (21-RESEARCH.md § v2.1 High-Risk Surface Map).

**CRITICAL — do not change the structure or ordering from the research skeleton. Your job is to populate each domain with the specific click-paths from the route inventory.**

Required top-level sections, in order:
1. File header (title, session type, user ID `user-jk`, status checklist)
2. Pre-Walkthrough Setup (session date / commit hash / browser / seed version fields — point to SESSION-NOTES.md)
3. Domain 1: Dashboard (`/dashboard`)
4. Domain 2: Deal Desk (`/deals` list + `/deals/[id]` detail — MUST include explicit BUG-02 check on list page stat cards, BUG-01 check on a post-DD deal's DD tab, BUG-03 check on Overview tab IC memo, AND FIN-01 check via clicking a meeting link in the activity feed)
5. Domain 3: Assets (`/assets` list + `/assets/[id]` detail — income/expenses/performance tabs with CRUD-01/02/03 stress-test probes: inline edit + delete on each tab)
6. Domain 4: Vehicles/Entities (`/entities` list with CRUD-04 create/edit/delete probe + `/entities/[id]` detail, covering overview, capital, cap-table [CRUD-08, UX-01..04], waterfall [CRUD-05/06/07, WF-22], fundraising, compliance, operations tabs)
7. Domain 5: Capital Activity + Waterfall + Transactions (collapsed per 21-RESEARCH.md § Routes in CONTEXT.md Order — single domain covering `/transactions` with calls tab [CRUD-09/10/12, UX-10 Fund All button], distributions tab [CRUD-11, WF-20/21/23], waterfall tab [waterfall calculator])
8. Domain 6: Directory/CRM (`/directory` with investors [CRUD-14/15/16], companies, contacts, team, sideLetters [CRUD-13] tabs + `/investors/[id]` activity [UX-05/06/07])
9. Domain 7: Tasks (`/tasks`)
10. Domain 8: Documents (`/documents`)
11. Domain 9: Meetings (`/meetings` — MUST explicitly prompt user to click a meeting and report the result; this is the FIN-01 re-verification moment)
12. Domain 10: AI Command Bar (Cmd+K — sample only per CONTEXT.md § Coverage depth)
13. Domain 11: Settings (`/settings` — 10 tabs: firm, users, integrations, gp, ai, dealdesk, decisions, notifications, permissions, service-providers — sample, do not change AI keys)
14. Overall Impressions (4 free-text prompts per skeleton)
15. Triage Summary with three empty tables: Urgent → v3.0, Already Scoped, Defer → v3.1+ (Claude fills in during Wave 3)

Within each domain section, use this format per 21-RESEARCH.md § Existing UI-GUIDE.md Workflow Format:
- **Go to:** `http://localhost:3000/<route>`
- **What to look at:** numbered list of observable checks (plain English, e.g., "Do the stat cards at the top load and show numbers?")
- **v2.1 stress-test:** (only where a CRUD/WF/UX item applies to this domain) — numbered list of specific probes tied to requirement IDs (e.g., "Try editing an income entry inline [CRUD-01]")
- **Capture block:** an explicit placeholder `<!-- capture block — user fills in here with Observation 1, Observation 2, ... following the per-observation template -->` followed by one pre-populated example Observation block showing the schema so the user knows what to fill in.

Example per-observation block (pre-populate one as an example in Domain 1 Dashboard so user has a template to copy):

```
### Observation 1

**Page / Action:** [e.g., /dashboard — top stat cards]
**What I saw:** [e.g., "Total AUM card is blank for 3 seconds before a number appears"]

**Severity (your gut call):**
- [ ] Blocker — I can't complete the task at all
- [ ] Annoying — I can complete it but it's confusing or wrong-looking
- [ ] Nitpick — minor cosmetic or wording issue

<!-- Claude fills in below during the follow-up triage session -->
**Claude Triage:**
- [ ] Urgent → v3.0 — fix before shipping
- [ ] Already scoped → Phase [N] — walkthrough confirms the known issue
- [ ] Defer → v3.1+ — real problem but not blocking v3.0

**Triage Reason:** [Which of the 3 rubric tests this passes or fails, and why]
**Proposed Destination:** [Phase 22 / Phase 23 / Phase 24 / Phase 25 / Phase 26 / Phase 27 / Phase 28-backlog / v3.1-backlog]
```

Tone: plain English throughout per CLAUDE.md ("Click the blue button that says New Deal" not "Invoke the CreateDealModal"). Non-developer language. Arrow-chain navigation format from UI-GUIDE.md (`[page] → "[element]" → [what happens]`). Replace "Verify:" with "Look for:".

Bug-check prompts must be explicit and identify the bug ID inline, e.g.:
> 3. At the top of the deals list, look for a "Pass Rate" or "IC Pass Rate" number. If you see anything over 100% (like "300%"), that's **BUG-02** still reproducing — note it in the capture block. If the number looks sane (0-100%), BUG-02 appears resolved — note that too.

FIN-01 prompt in Domain 2 must be:
> Click any meeting link that appears in the deal's Activity tab. Either it loads a page with meeting details (FIN-01 is unexpectedly already fixed), or you get a 404 / blank page (expected — FIN-01 is the Phase 22 work to build this page). Note what you see.

FIN-01 prompt in Domain 9 must be:
> Click any meeting card in the list. Same as above — a page loads OR you hit a 404. Note it.

Do NOT wait for user approval on wording — 21-CONTEXT.md § Claude's Discretion explicitly grants Claude latitude on "Exact checklist markdown format and per-domain question wording."

File must be committed with placeholders for user-typed observations but otherwise complete.
  </action>
  <verify>
    <automated>test -f .planning/walkthroughs/v3.0-gp-baseline.md && [ -s .planning/walkthroughs/v3.0-gp-baseline.md ] && grep -q "Domain 1: Dashboard" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "Domain 11: Settings" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "BUG-01" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "BUG-02" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "BUG-03" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "FIN-01" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "Triage Summary" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "CRUD-01" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "Proposed Destination:" .planning/walkthroughs/v3.0-gp-baseline.md && echo "GP baseline scaffold OK"</automated>
  </verify>
  <done>
`v3.0-gp-baseline.md` exists with: file header, Pre-Walkthrough Setup section, all 11 GP domain sections in locked order (Dashboard → Deal Desk → Assets → Entities → Capital Activity+Waterfall+Transactions → Directory → Tasks → Documents → Meetings → AI command bar → Settings), explicit in-flow checks for BUG-01/02/03 and FIN-01 in the correct domains, v2.1 stress-test subsections referencing CRUD-01..17, WF-20..26, UX-01..10 where applicable, at least one pre-populated example Observation block showing the full per-observation schema, Overall Impressions section, and empty Triage Summary tables. Tone is plain English (no "invoke", "instantiate", "revalidate", etc.).
  </done>
</task>

<task type="auto">
  <name>Wave 0 — Task 3: Scaffold LP baseline file with full walkthrough script (both passes)</name>
  <files>
    .planning/walkthroughs/v3.0-lp-baseline.md
  </files>
  <action>
Create `.planning/walkthroughs/v3.0-lp-baseline.md` using the skeleton in `21-RESEARCH.md` § "Baseline File Skeleton (GP + LP)" adapted for two sequential passes.

**CRITICAL corrections applied to the skeleton (per 21-RESEARCH.md § LP Seed-Data Inventory):**
- Pass 1 is Michael Chen (`user-lp-calpers`), NOT Karen Miller — Karen is not seeded.
- Pass 2 is Tom Wellington (`user-lp-wellington`), labeled "simpler LP" NOT "single-vehicle" — no true single-vehicle LP exists.

Required top-level sections, in order:
1. File header (title, session type = LP-side two passes, status checklist with 3 checkboxes: Pass 1 complete / Pass 2 complete / Triage done)
2. Pre-Walkthrough Setup (reference SESSION-NOTES.md for seed + branch notes; session date / commit / browser fields for each pass)
3. **Pass 1 — Multi-Vehicle LP (CalPERS — Michael Chen, `user-lp-calpers`)** with this structure:
   - User-switcher instructions (plain English: "Use the user dropdown in local dev to switch to Michael Chen")
   - What this tests: 5 vehicles, active capital calls (funded + pending), distributions, broad aggregation stress
   - LP Domain 1: Dashboard (`/lp-dashboard`) — CalPERS has ~$165M committed per research; numbers should be plausible for an institutional investor
   - LP Domain 2: Capital Account (`/lp-account`) — inserted per 21-RESEARCH.md (between Dashboard and Portfolio); period statement line items, color-coded
   - LP Domain 3: Portfolio (`/lp-portfolio`) — pro-rata asset exposure across 5 vehicles, asset class badges
   - LP Domain 4: Notices & Activity (`/lp-activity`) — capital calls with Funded/Pending statuses, distributions with breakdowns, sort order
   - LP Domain 5: Documents (`/lp-documents`) — GP-shared documents, entity names, categories
   - LP Domain 6: Settings / Notifications (`/lp-settings`) — notification preferences; note per 21-RESEARCH.md there is no standalone notifications page, it lives inside settings
   - LP Domain 7: Profile (`/lp-profile`) — profile info should match Michael Chen / CalPERS, contact and tax ID fields
   - Pass 1 Overall Impressions (3 free-text prompts: clear picture of exposure, hardest info to find, what's missing)
4. **Pass 2 — Simpler LP (Wellington Family Office — Tom Wellington, `user-lp-wellington`)** with this structure:
   - User-switcher instructions
   - What this tests: 3 vehicles, lighter data footprint, family-office POV (not institutional)
   - **Explicit caveat in the section intro:** "There is no truly single-vehicle LP seeded. Wellington is the closest to a 'simpler' LP. This pass is a sanity check, not a single-vehicle test."
   - Brief repeat of the same 7 LP domains as Pass 1, but with narrower prompts focused on: (a) does the data correctly switch to Wellington's 3 vehicles (not CalPERS's 5)? (b) does anything that worked for CalPERS break for Wellington? (c) does the portal feel appropriate for a family office vs. an institution?
   - Pass 2 Overall Impressions (2 free-text prompts)
5. Triage Summary (three empty tables: Urgent → v3.0, Already Scoped, Defer → v3.1+)

Each LP domain must include:
- **Go to:** `http://localhost:3000/<route>`
- **What to look at:** numbered list of observable checks (plain English)
- **Capture block:** placeholder + one pre-populated example Observation block using the exact per-observation template from 21-RESEARCH.md § Capture Template Design (same template as GP file — Page/Action, What I saw, Severity, Claude Triage, Triage Reason, Proposed Destination)

**Do NOT include** per CONTEXT.md § LP walkthrough scope (out of scope):
- Email/SMS notification flow deep-dive
- K-1 acknowledgment flow deep-dive
- SERVICE_PROVIDER role walkthrough (deferred per CONTEXT.md § Deferred Ideas)

Tone: plain English, non-developer. Use "sign in as" (user-switcher language) not "authenticate as". Example: "Open the user dropdown in the top-right corner of the app and select 'Michael Chen' — you should see 'MC' in the top corner after switching."
  </action>
  <verify>
    <automated>test -f .planning/walkthroughs/v3.0-lp-baseline.md && [ -s .planning/walkthroughs/v3.0-lp-baseline.md ] && grep -q "Michael Chen" .planning/walkthroughs/v3.0-lp-baseline.md && grep -q "Tom Wellington" .planning/walkthroughs/v3.0-lp-baseline.md && ! grep -q "Karen Miller" .planning/walkthroughs/v3.0-lp-baseline.md && grep -q "user-lp-calpers" .planning/walkthroughs/v3.0-lp-baseline.md && grep -q "user-lp-wellington" .planning/walkthroughs/v3.0-lp-baseline.md && grep -q "LP Domain 2: Capital Account" .planning/walkthroughs/v3.0-lp-baseline.md && grep -q "simpler LP" .planning/walkthroughs/v3.0-lp-baseline.md && grep -q "Triage Summary" .planning/walkthroughs/v3.0-lp-baseline.md && echo "LP baseline scaffold OK"</automated>
  </verify>
  <done>
`v3.0-lp-baseline.md` exists with: file header, Pre-Walkthrough Setup, Pass 1 (Michael Chen) with all 7 LP domains (including `/lp-account` inserted between Dashboard and Portfolio), Pass 1 Overall Impressions, Pass 2 (Tom Wellington) with explicit "simpler LP not single-vehicle" caveat and abbreviated domain revisit, Pass 2 Overall Impressions, empty Triage Summary tables. Karen Miller is NOT mentioned. Both user IDs `user-lp-calpers` and `user-lp-wellington` appear. At least one pre-populated example Observation block per pass shows the per-observation schema. Plain English throughout.
  </done>
</task>

<!-- ======================= WAVE 1 (human checkpoint — GP walkthrough) ======================= -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Wave 1 — GP Walkthrough Session (human execution)</name>
  <files>
    .planning/walkthroughs/v3.0-gp-baseline.md
  </files>
  <action>
Human execution step — the user opens the app at http://localhost:3000 (signed in as James Kim, `user-jk`, GP_ADMIN) and works through the 11-domain GP walkthrough script embedded in `.planning/walkthroughs/v3.0-gp-baseline.md`. The user types observations into the capture blocks using the per-observation template (Page/Action + What I saw + Severity). Claude does NOT observe in real time — this is a "solo-then-return" checkpoint per 21-CONTEXT.md § Capture format + session cadence. Claude's role in this task is to pause execution, provide the copy-paste-ready instructions in `<how-to-verify>` below, and wait for the user's resume signal. Triage fields are intentionally left blank — those are filled in Wave 3. See `<how-to-verify>` for the full pre-flight + execution + resume instructions.
  </action>
  <what-built>
Three files are now staged in `.planning/walkthroughs/`:
- `SESSION-NOTES.md` — pre-flight checklist + branch/bug/LP caveats
- `v3.0-gp-baseline.md` — full GP walkthrough script (11 domains, empty capture blocks)
- `v3.0-lp-baseline.md` — full LP walkthrough script (2 passes, empty capture blocks)

The GP script is ready for you to execute. It embeds in-flow checks for the 3 March-5 bugs (BUG-01 DD tab, BUG-02 pass rate, BUG-03 IC memo spinner) and for FIN-01 (meeting detail 404).
  </what-built>
  <how-to-verify>
**Step 1 — Pre-flight (5 minutes):**
1. Open a terminal in the project folder
2. Run: `cat .planning/walkthroughs/SESSION-NOTES.md` — read it top to bottom
3. Run: `npx prisma db seed` — wait for it to finish (you'll see "Seeding complete" or similar)
4. Run: `git rev-parse --short HEAD` — copy the short commit hash
5. Run: `git branch -a | grep edit-delete` — copy whatever it prints (likely nothing, which is fine)
6. Run: `npm run dev` — leave this running in the terminal
7. Open http://localhost:3000 in your browser
8. Confirm you see "JK" (James Kim) in the top-right corner. If not, use the user switcher to select James Kim.

**Step 2 — Fill the session header of `v3.0-gp-baseline.md`:**
- Paste the commit hash into the "Commit hash" field
- Fill session date and browser name
- Paste the `git branch -a | grep edit-delete` output into the "Branch status" field (or write "not present" if empty)

**Step 3 — Execute the GP walkthrough (45-75 minutes):**
Work top to bottom through Domains 1 through 11 in `v3.0-gp-baseline.md`. At each domain:
- Follow the "Go to" URL
- Work through the "What to look at" numbered list
- Complete any "v2.1 stress-test" probes
- **Write your observations into the capture block using the per-observation template.** Add as many Observation N blocks as you need (one per distinct thing you noticed).
- Fill in the Severity checkbox (one of Blocker / Annoying / Nitpick). **Leave the Claude Triage / Triage Reason / Proposed Destination fields blank — Claude fills those in Wave 3.**

**Critical bug checks — be explicit about each:**
- In Domain 2 (Deal Desk): did you see a pass rate over 100% on the deals list? (BUG-02)
- In Domain 2: on a post-DD deal's DD tab, were workstreams stuck at 0%/NOT_STARTED? (BUG-01)
- In Domain 2: did the IC memo generate successfully, stay stuck spinning, or was there no AI key configured (skip)? (BUG-03)
- In Domain 2 AND Domain 9: when you clicked a meeting link, did you get a working page, a 404, or something else? (FIN-01)

**Step 4 — Fill in "Overall Impressions" at the end of the GP file.**

**Step 5 — Save the file and report back.** When you resume, Claude will move to Wave 2 (LP walkthrough).

If something is unclear about a domain, write "skip — unclear" and move on. Do not block yourself on ambiguity.
  </how-to-verify>
  <verify>
    <automated>test -s .planning/walkthroughs/v3.0-gp-baseline.md && grep -c "Observation" .planning/walkthroughs/v3.0-gp-baseline.md | awk '$1 >= 5 {print "at least 5 observations captured"; exit 0} {print "WARN: fewer than 5 observations — confirm user completed walkthrough"; exit 1}'</automated>
  </verify>
  <done>
The user has completed the GP walkthrough. `v3.0-gp-baseline.md` has session header populated (date/commit/browser), at least 5 Observation blocks across the 11 domains with Page/Action + What I saw + Severity filled in, explicit notes on BUG-01/02/03 status (reproduced or not), explicit note on FIN-01 status (404 or working), and Overall Impressions answered. Triage fields remain blank (intentionally — Wave 3 work).
  </done>
  <resume-signal>Type "gp done" or describe any blockers hit during the walkthrough.</resume-signal>
</task>

<!-- ======================= WAVE 2 (human checkpoint — LP walkthrough) ======================= -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Wave 2 — LP Walkthrough Session (human execution, both passes)</name>
  <files>
    .planning/walkthroughs/v3.0-lp-baseline.md
  </files>
  <action>
Human execution step (two sequential passes) — the user switches the mock UserProvider to Michael Chen (`user-lp-calpers`, CalPERS, 5 vehicles) and works through Pass 1 of `.planning/walkthroughs/v3.0-lp-baseline.md`, then switches to Tom Wellington (`user-lp-wellington`, Wellington Family Office, 3 vehicles — the closest "simpler LP" in the seed, per 21-RESEARCH.md § LP Seed-Data Inventory) and works through Pass 2. The user types observations into the capture blocks in each LP domain. Karen Miller is NOT in the seed — that's why Pass 1 uses Michael Chen. Claude does NOT observe in real time. Claude's role in this task is to pause execution, provide the copy-paste-ready instructions in `<how-to-verify>` below, and wait for the user's resume signal. Out-of-scope areas (email/SMS notifications, K-1 acknowledgment, SERVICE_PROVIDER role) are explicitly excluded per 21-CONTEXT.md § LP walkthrough scope. Triage fields remain blank — filled in Wave 3. See `<how-to-verify>` for detailed step-by-step instructions.
  </action>
  <what-built>
The GP walkthrough is complete and captured in `v3.0-gp-baseline.md`. Now you execute the LP-side walkthrough using `v3.0-lp-baseline.md`, which has two passes: Michael Chen (CalPERS, 5 vehicles) then Tom Wellington (Wellington Family Office, 3 vehicles).
  </what-built>
  <how-to-verify>
**Step 1 — Fresh seed between sessions:**
1. Stop or restart `npm run dev` if it's been running for a while
2. Run: `npx prisma db seed` again — fresh state for the LP session
3. Run: `git rev-parse --short HEAD` — fill the LP file's session header

**Step 2 — Pass 1 (Michael Chen, CalPERS):**
1. In the browser at http://localhost:3000, find the user switcher dropdown (top-right corner in local dev)
2. Select **Michael Chen** (`user-lp-calpers`). You should see "MC" in the corner after switching, and the sidebar should change to the LP navigation (My Overview, Capital Account, LP Portfolio, etc.) — not the GP sidebar.
3. Work through Pass 1's 7 LP domains top to bottom:
   - LP Domain 1: Dashboard (`/lp-dashboard`) — CalPERS committed ~$165M; sanity-check the numbers
   - LP Domain 2: Capital Account (`/lp-account`)
   - LP Domain 3: Portfolio (`/lp-portfolio`) — 5 vehicles
   - LP Domain 4: Notices & Activity (`/lp-activity`) — capital calls + distributions
   - LP Domain 5: Documents (`/lp-documents`)
   - LP Domain 6: Settings (`/lp-settings`)
   - LP Domain 7: Profile (`/lp-profile`)
4. Add Observation blocks (same per-observation template as the GP file) for anything that felt off. Fill Severity; leave Triage fields blank for Claude.
5. Fill in Pass 1 Overall Impressions.

**Step 3 — Pass 2 (Tom Wellington, simpler LP):**
1. Switch users — select **Tom Wellington** (`user-lp-wellington`). Confirm "TW" appears in the corner and the sidebar still shows LP navigation.
2. Revisit each of the 7 LP domains briefly. Focus on:
   - Did the data switch from 5 vehicles (CalPERS) to 3 vehicles (Wellington)? If CalPERS data persists, that's a bug worth flagging.
   - Anything that worked for CalPERS but not Wellington?
   - Does the portal feel appropriate for a smaller family office?
3. Add Observation blocks for anything notable.
4. Fill in Pass 2 Overall Impressions.

**Step 4 — Save and report back.** When you resume, Claude will move to Wave 3 (triage).

**Out of scope (do not dig into these unless they crash on you):**
- Email / SMS notification flow
- K-1 acknowledgment flow
- SERVICE_PROVIDER role — not tested in Phase 21

If you cannot switch to an LP user (user switcher missing, sidebar won't change, blank page), note it as a Blocker observation in Pass 1 and stop — that's a higher-priority finding than finishing the walkthrough.
  </how-to-verify>
  <verify>
    <automated>test -s .planning/walkthroughs/v3.0-lp-baseline.md && grep -c "Observation" .planning/walkthroughs/v3.0-lp-baseline.md | awk '$1 >= 4 {print "at least 4 observations captured across both passes"; exit 0} {print "WARN: fewer than 4 observations — confirm user completed both passes"; exit 1}'</automated>
  </verify>
  <done>
The user has completed both LP passes. `v3.0-lp-baseline.md` has session header populated, at least 4 Observation blocks across both passes (Pass 1 CalPERS + Pass 2 Wellington) with Page/Action + What I saw + Severity filled, both Pass 1 and Pass 2 Overall Impressions answered, and explicit note on whether user-switching correctly changed the data shown. Triage fields remain blank.
  </done>
  <resume-signal>Type "lp done" or describe any blockers hit during either pass.</resume-signal>
</task>

<!-- ======================= WAVE 3 (autonomous triage) ======================= -->

<task type="auto">
  <name>Wave 3 — Task: Apply triage rubric to every observation, populate triage tables</name>
  <files>
    .planning/walkthroughs/v3.0-gp-baseline.md
    .planning/walkthroughs/v3.0-lp-baseline.md
  </files>
  <action>
Both baseline files now have the user's observations filled in. Your job is to apply the LOCKED 3-part triage rubric to every Observation block and populate the Triage Summary tables at the bottom of each file.

**Triage Rubric (LOCKED — do NOT modify):** An observation is `Urgent → v3.0` only if ALL THREE are true:
1. Materially blocks a user completing a real workflow (not aesthetic)
2. Fixable inside existing Phase 22-27 scope OR a small Phase 28 follow-up (no new phase needed)
3. Does NOT introduce new feature surface

Failing any one test → `Defer → v3.1+` with a one-line reason citing the failed test.

**Process for each Observation block (applied to both GP and LP files):**

1. Read the observation's Page/Action + What I saw + Severity.
2. Apply the 3 rubric tests in order. For each test, answer yes/no in your head.
3. Check exactly one Claude Triage box:
   - `[x] Urgent → v3.0` — ALL 3 tests pass
   - `[x] Already scoped → Phase N` — the observation is a known issue already mapped to a downstream phase requirement (FIN-01, FIN-02, RBAC-01, etc.)
   - `[x] Defer → v3.1+` — any test fails
4. Fill `Triage Reason:` with explicit rubric language, e.g.:
   - Urgent: "Passes all 3 rubric tests: (1) blocks workflow because user cannot open a meeting from the activity feed, (2) Phase 22 FIN-01 scope covers this exactly, (3) not a new feature surface — it's a missing page for already-linked entity."
   - Already scoped: "Matches FIN-08 Phase 22 scope — March-5 BUG-02 pass rate >100% still reproduces; in-flow check confirms still-open bug."
   - Defer: "Fails rubric test 1 — cosmetic only, loading spinner color mildly off-brand; does not materially degrade workflow."
5. Fill `Proposed Destination:` with one of the allowed values: `Phase 22`, `Phase 23`, `Phase 24`, `Phase 25`, `Phase 26`, `Phase 27`, `Phase 28-backlog`, `v3.1-backlog`.

**Mapping guide for Proposed Destination (use to pick the right phase):**
- Meeting detail 404 / waterfall refactor / second-fund validation / March-5 bugs → **Phase 22** (FIN-01, FIN-02, FIN-04, FIN-08)
- Documentation gaps / waterfall conventions doc / VERIFICATION.md retrofit / traceability → **Phase 23** (FIN-03, FIN-05, FIN-06, FIN-07)
- Role/permission enforcement / LP hitting GP data / admin routes accessible to GP_TEAM → **Phase 24** (RBAC-01..05)
- Long list performance / scroll-to-bottom exhaustion / "where are older items" → **Phase 25** (PAGE-01..04)
- White-screen crashes / broken states that take down page → **Phase 26** (ERR-01..03)
- Regression risk / no automated test → **Phase 27** (E2E-01..06)
- Urgent-but-doesn't-fit-P22-27 → **Phase 28-backlog** (lands in the Phase 28 follow-up batch via Wave 4)
- Cosmetic / new-feature-ish / future-work → **v3.1-backlog** (recorded in baseline file, not folded anywhere else)

**Override rule:** If the user rated an observation `[x] Blocker` but you triage `Defer → v3.1+`, the Triage Reason MUST name the failed rubric test explicitly. Cosmetic-but-annoying complaints that the user labeled Blocker out of frustration should be downgraded in the Triage Reason (e.g., "User severity Blocker appears to reflect frustration with appearance rather than workflow block; fails rubric test 1 — task still completable.").

**After every observation is triaged, populate the three Triage Summary tables at the end of each file:**

Table 1 — Urgent → v3.0:
| # | Page/Action | Observation | Severity | Destination | Triage Reason |
(one row per Urgent observation)

Table 2 — Already Scoped (walkthrough confirms):
| # | Page/Action | Observation | Phase | Requirement |
(one row per Already-scoped observation, with requirement ID like FIN-01)

Table 3 — Defer → v3.1+:
| # | Page/Action | Observation | Severity | Reason |
(one row per Deferred observation, with one-line rubric-based reason)

Do this for BOTH `v3.0-gp-baseline.md` and `v3.0-lp-baseline.md`.

**Sanity checks (Layer 3 of 21-VALIDATION.md) — run before declaring Wave 3 done:**
- Any Urgent item without a specific Phase N or Phase 28-backlog destination? → re-evaluate.
- Any Urgent item whose destination phase has no matching requirement in REQUIREMENTS.md? → re-evaluate (likely belongs in Phase 28-backlog instead).
- Any Defer item rated Blocker without an explicit rubric-test failure in the reason? → re-write the reason.
- Zero urgent items total across both files? → possible but suspicious. Include a note in the Triage Summary intro acknowledging this and flag for the user's review in Wave 4.

Do NOT introduce new observations in Wave 3 — only triage what the user captured. If an observation is ambiguous, add a parenthetical "(Claude note: ambiguous — recommending verification at Phase 28 final walkthrough)" in the Triage Reason and default to `Defer → v3.1+` unless clearly workflow-blocking.
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md; do obs=$(grep -c "^### Observation" "$f" || echo 0); triaged=$(grep -cE "\[x\] (Urgent → v3\.0|Already scoped|Defer → v3\.1\+)" "$f" || echo 0); reason_count=$(grep -c "\*\*Triage Reason:\*\*" "$f" || echo 0); blank_reason=$(grep -cE "\*\*Triage Reason:\*\* *$" "$f" || echo 0); dest_count=$(grep -c "\*\*Proposed Destination:\*\*" "$f" || echo 0); blank_dest=$(grep -cE "\*\*Proposed Destination:\*\* *$" "$f" || echo 0); echo "$f: observations=$obs triaged_boxes=$triaged reason_fields=$reason_count blank_reasons=$blank_reason dest_fields=$dest_count blank_dests=$blank_dest"; if [ "$blank_reason" -gt 1 ] || [ "$blank_dest" -gt 1 ]; then echo "FAIL: $f has blank Triage Reason or Proposed Destination fields beyond the single template"; exit 1; fi; done; grep -q "## Triage Summary" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "## Triage Summary" .planning/walkthroughs/v3.0-lp-baseline.md && echo "Wave 3 triage OK"'</automated>
  </verify>
  <done>
Every Observation block in both baseline files has exactly one Claude Triage box checked, a non-empty Triage Reason referencing the 3-part rubric, and a Proposed Destination pointing at a specific phase (22-27), `Phase 28-backlog`, or `v3.1-backlog`. Both files have populated Triage Summary tables (Urgent / Already Scoped / Defer). No "Blocker + Defer" mismatch exists without an explicit rubric-failure reason. Layer 3 sanity flags addressed or acknowledged.
  </done>
</task>

<!-- ======================= WAVE 4 (autonomous fold-in + validate) ======================= -->

<task type="auto">
  <name>Wave 4 — Task: Fold urgent items into downstream phases (in-place) and run full validation</name>
  <files>
    .planning/ROADMAP.md
    .planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md
    .planning/walkthroughs/v3.0-gp-baseline.md
    .planning/walkthroughs/v3.0-lp-baseline.md
  </files>
  <action>
For every row in either file's "Urgent → v3.0" Triage Summary table, carry the item into its destination. This is the "fold urgent items into downstream phases" step required by ROADMAP.md Phase 21 success criterion 5.

**Key constraint (per planning_shape_guidance):** Phases 22-27 have NO plan files yet (only ROADMAP.md sections). "Folding into the plan" means appending an in-place note to the ROADMAP.md phase section — NOT pre-creating plan files. Only Phase 21 has a plan file; all downstream phases remain unplanned until their own plan-phase runs.

**Process:**

1. **Read the Urgent tables** in both `v3.0-gp-baseline.md` and `v3.0-lp-baseline.md`.

2. **For each urgent row, route to destination:**

   **If Destination is Phase 22, 23, 24, 25, 26, or 27:**
   - Open `.planning/ROADMAP.md`
   - Locate the `### Phase N: ...` heading
   - Within that phase's section (between the heading and the next `###` heading), append (or update if already present) a subsection titled exactly:
     ```
     **Additions from Phase 21 walkthrough:**
     - [ ] {short item description} — source: {GP|LP} baseline, {severity}, rubric reasoning: {one line}
     ```
   - If the subsection already exists from a prior urgent item, append the new bullet rather than duplicating the subsection.
   - This subsection must sit BEFORE the "Plans: TBD" line so a future planner running `/gsd:plan-phase {N}` sees it.

   **If Destination is `Phase 28-backlog`:**
   - Ensure `.planning/phases/28-final-walkthrough-signoff/` directory exists (`mkdir -p` it if not).
   - Create `.planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md` if not present, with a header and an empty table (columns: `# | Source | Page/Action | Observation | Severity | Rubric Reasoning`).
   - Append a row for this urgent item with source = `gp` or `lp`, page/action + observation + severity copied from the triage table, rubric reasoning copied from Triage Reason.
   - If 28-BACKLOG.md already exists from a prior urgent item this session, append rather than recreate.

3. **Also update ROADMAP.md Progress table:**
   - Find the `| 21. Initial Manual Walkthrough (Baseline) | v3.0 | 0/TBD | Not started | — |` row.
   - Change it to `| 21. Initial Manual Walkthrough (Baseline) | v3.0 | 1/1 | Complete | 2026-04-16 |` (use today's date from environment — 2026-04-16 per planning context).

4. **Also update ROADMAP.md Phase 21 section's Plans line:**
   - Change `**Plans:** TBD (likely 1 plan: ...)` to `**Plans:** 1 plan complete (21-01-PLAN.md — walkthrough scaffolding + execution + triage + downstream fold-in)`.

5. **Run the full 4-layer validation from 21-VALIDATION.md:**

   Layer 1 (file existence):
   ```bash
   ls .planning/walkthroughs/v3.0-gp-baseline.md
   ls .planning/walkthroughs/v3.0-lp-baseline.md
   [ -s .planning/walkthroughs/v3.0-gp-baseline.md ]
   [ -s .planning/walkthroughs/v3.0-lp-baseline.md ]
   ```

   Layer 2 (schema) — for each file, confirm field counts match observation counts and no empty Triage Reason / Proposed Destination fields remain:
   ```bash
   for f in .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md; do
     echo "=== $f ==="
     echo "Observations: $(grep -c '^### Observation' $f)"
     echo "Page/Action fields: $(grep -c '\*\*Page / Action:\*\*' $f)"
     echo "What I saw fields: $(grep -c '\*\*What I saw:\*\*' $f)"
     echo "Claude Triage fields: $(grep -c '\*\*Claude Triage:\*\*' $f)"
     echo "Triage Reason fields: $(grep -c '\*\*Triage Reason:\*\*' $f)"
     echo "Proposed Destination fields: $(grep -c '\*\*Proposed Destination:\*\*' $f)"
     echo "Empty Triage Reason: $(grep -cE '\*\*Triage Reason:\*\* *$' $f)"
     echo "Empty Proposed Destination: $(grep -cE '\*\*Proposed Destination:\*\* *$' $f)"
   done
   ```
   Pass criteria: Observations count == Page/Action == What I saw == Claude Triage == Triage Reason == Proposed Destination (allowing +1 for any pre-populated example block). Empty counts ≤ 1 (the example block) per file.

   Layer 3 (triage sanity audit) — already executed during Wave 3; confirm no unresolved flags by re-reading your triage output.

   Layer 4 (ROADMAP cross-reference):
   ```bash
   # Every Phase N destination in either Urgent table must exist as a ### Phase N heading in ROADMAP.md
   destinations=$(grep -hoE "Phase 2[2-8]" .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md | sort -u)
   echo "$destinations" | while read -r phase; do
     grep -q "### $phase:" .planning/ROADMAP.md && echo "$phase OK" || echo "FAIL: $phase not found in ROADMAP.md"
   done
   ```
   Pass criteria: every Urgent destination resolves to a `### Phase N:` heading in ROADMAP.md. Any `Phase 28-backlog` destination must resolve to a row in `.planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md`.

6. **Write the validation results directly into each baseline file** as a final section titled `## Validation (Phase 21 gate)` summarizing Layer 1-4 outcomes. Example:
   ```
   ## Validation (Phase 21 gate)

   - Layer 1 (existence + non-empty): PASS — both files present, both non-empty
   - Layer 2 (schema): PASS — {N} observations, all fields populated (no blank Triage Reason or Proposed Destination)
   - Layer 3 (triage sanity): PASS — no Blocker+Defer mismatches; all Urgent items have concrete destinations
   - Layer 4 (ROADMAP cross-reference): PASS — all Urgent Phase N destinations resolve in ROADMAP.md, all Phase 28-backlog items resolve in 28-BACKLOG.md
   ```

If any layer fails, fix the underlying issue (re-triage, move to correct destination, update ROADMAP) before declaring done. Do NOT mark the phase complete with failing validation.

**Edge cases:**
- Zero urgent items across both files: that's possible (especially if the app is in good shape). Record "No urgent items surfaced — all observations deferred to v3.1+ or already scoped" in the Validation section. Do NOT fabricate urgent items. Layer 3 flags this as "suspicious — review before declaring done"; address by confirming in the Validation section that the zero-urgent outcome is real and not a missed triage.
- Observation points to a phase that doesn't have a matching requirement (e.g., Urgent item destined for Phase 25 but the observation isn't about pagination): re-triage and move to the correct phase or to Phase 28-backlog.
- BUG-01/02/03 marked "could not reproduce" in the walkthrough: triage as `Already scoped → Phase 22` with reasoning "walkthrough evidence suggests BUG-N resolved; Phase 22 FIN-08 re-verification task will formalize closure." Add an "Additions from Phase 21 walkthrough" note under Phase 22 flagging "walkthrough suggests BUG-N already resolved — formalize in FIN-08 re-verify task."
  </action>
  <verify>
    <automated>bash -c 'set -e; echo "=== Layer 1 ==="; test -s .planning/walkthroughs/v3.0-gp-baseline.md && echo "GP OK" && test -s .planning/walkthroughs/v3.0-lp-baseline.md && echo "LP OK"; echo "=== Layer 2 ==="; for f in .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md; do empty_reason=$(grep -cE "\*\*Triage Reason:\*\* *$" "$f" || echo 0); empty_dest=$(grep -cE "\*\*Proposed Destination:\*\* *$" "$f" || echo 0); if [ "$empty_reason" -gt 1 ] || [ "$empty_dest" -gt 1 ]; then echo "FAIL: $f has blank fields"; exit 1; fi; done; echo "schema OK"; echo "=== Layer 4 ==="; grep -q "## Validation (Phase 21 gate)" .planning/walkthroughs/v3.0-gp-baseline.md && grep -q "## Validation (Phase 21 gate)" .planning/walkthroughs/v3.0-lp-baseline.md && echo "validation sections present"; if grep -qE "Phase 28-backlog" .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md 2>/dev/null; then test -f .planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md || (echo "FAIL: 28-backlog items exist but 28-BACKLOG.md missing"; exit 1); echo "28-BACKLOG.md present"; else echo "no 28-backlog items — 28-BACKLOG.md not required"; fi; if grep -qE "Phase 2[2-7]" .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md 2>/dev/null; then grep -q "Additions from Phase 21 walkthrough" .planning/ROADMAP.md || (echo "FAIL: Urgent items destined for P22-27 but no Additions subsection in ROADMAP.md"; exit 1); fi; grep -q "| 21\. Initial Manual Walkthrough (Baseline) | v3\.0 | 1/1 | Complete" .planning/ROADMAP.md && echo "ROADMAP progress row updated" && echo "=== ALL 4 LAYERS PASS ==="'</automated>
  </verify>
  <done>
Every Urgent item from both triage tables has been folded into its destination: Phase 22-27 items appear under an "Additions from Phase 21 walkthrough" subsection in the corresponding ROADMAP.md phase section; Phase 28-backlog items appear as rows in `.planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md`. ROADMAP.md Progress table shows Phase 21 as `1/1 Complete`. Both baseline files carry a `## Validation (Phase 21 gate)` section stating Layers 1-4 pass. No urgent items are orphaned (all have a destination; all destinations resolve). If zero urgent items surfaced, the Validation section explicitly confirms that outcome is intended, not missed.
  </done>
</task>

</tasks>

<verification>
Run the full 4-layer validation pipeline from `21-VALIDATION.md`:

```bash
# Layer 1 — file existence + non-empty
ls .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md
[ -s .planning/walkthroughs/v3.0-gp-baseline.md ] && [ -s .planning/walkthroughs/v3.0-lp-baseline.md ]

# Layer 2 — schema (every observation has full per-observation schema)
for f in .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md; do
  obs=$(grep -c '^### Observation' $f)
  reason=$(grep -c '\*\*Triage Reason:\*\*' $f)
  dest=$(grep -c '\*\*Proposed Destination:\*\*' $f)
  empty_reason=$(grep -cE '\*\*Triage Reason:\*\* *$' $f)
  empty_dest=$(grep -cE '\*\*Proposed Destination:\*\* *$' $f)
  echo "$f: obs=$obs reason=$reason dest=$dest empty_reason=$empty_reason empty_dest=$empty_dest"
done

# Layer 3 — triage sanity: eyeball each Urgent row for rubric-test citation; each Defer row for one-line reason
grep -A 1 "\[x\] Urgent → v3.0" .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md | grep "Triage Reason" | head -20

# Layer 4 — ROADMAP cross-reference: every Urgent Phase N destination in the triage tables maps to a ### Phase N heading in ROADMAP.md
for phase in $(grep -hoE "Phase 2[2-8]" .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md | sort -u); do
  grep -q "### $phase:" .planning/ROADMAP.md && echo "$phase OK" || echo "FAIL $phase"
done

# ROADMAP Progress row updated
grep -q "| 21\. Initial Manual Walkthrough (Baseline) | v3\.0 | 1/1 | Complete" .planning/ROADMAP.md

# 28-backlog check (only if any 28-backlog items exist)
if grep -q "Phase 28-backlog" .planning/walkthroughs/v3.0-gp-baseline.md .planning/walkthroughs/v3.0-lp-baseline.md; then
  test -f .planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md
fi
```

All 5 ROADMAP.md Phase 21 success criteria must be demonstrably TRUE:
1. GP baseline exists + populated ✓ (Layer 1 + Layer 2)
2. LP baseline exists + populated ✓ (Layer 1 + Layer 2)
3. Every comment triaged with explicit reasoning ✓ (Layer 2 + Layer 3)
4. Urgent items fold into existing Phase 22-27 or Phase 28 backlog ✓ (Layer 4 + ROADMAP Additions subsections OR 28-BACKLOG.md rows)
5. Phases 22-27 updated in-place with absorbed items ✓ (ROADMAP.md "Additions from Phase 21 walkthrough" subsections per phase)
</verification>

<success_criteria>
The phase is complete when ALL of the following are demonstrably TRUE:

1. `.planning/walkthroughs/v3.0-gp-baseline.md` exists, is populated with the user's GP-side observations (minimum 5 Observation blocks across the 11 domains), each observation has Claude's full triage (Claude Triage checkbox, Triage Reason citing rubric, Proposed Destination pointing at a phase or backlog), and the Triage Summary tables at the bottom are populated.

2. `.planning/walkthroughs/v3.0-lp-baseline.md` exists, is populated with the user's LP-side observations from both Pass 1 (Michael Chen) and Pass 2 (Tom Wellington) — minimum 4 Observation blocks total across both passes — each with Claude's full triage, and Triage Summary tables populated.

3. The 3-part triage rubric from CONTEXT.md is applied explicitly (each Urgent item's Triage Reason names the passing rubric tests; each Defer item's reason names the failing rubric test).

4. Every Urgent item has either (a) landed as an "Additions from Phase 21 walkthrough" bullet in the corresponding Phase 22-27 section of `.planning/ROADMAP.md`, OR (b) been appended to `.planning/phases/28-final-walkthrough-signoff/28-BACKLOG.md` as a row.

5. `.planning/ROADMAP.md` Progress table row for Phase 21 shows `1/1 Complete` with today's date.

6. All 4 validation layers from `21-VALIDATION.md` pass (file existence + non-empty; schema; triage sanity; ROADMAP cross-reference). The Validation section in each baseline file states the outcome.

7. MAN-01 requirement satisfied by the populated GP baseline file; MAN-02 requirement satisfied by the populated LP baseline file.
</success_criteria>

<output>
After completion, create `.planning/phases/21-initial-manual-walkthrough-baseline/21-01-SUMMARY.md` following the standard summary template. The summary must include:

- **What was built:** List the 5 artifacts produced (SESSION-NOTES.md, v3.0-gp-baseline.md, v3.0-lp-baseline.md, ROADMAP.md updates, 28-BACKLOG.md if any).
- **Triage outcome counts:**
  - GP file: {N} observations → {U} urgent, {S} already-scoped, {D} deferred
  - LP file: {N} observations → {U} urgent, {S} already-scoped, {D} deferred
- **Urgent items folded into downstream phases:** table of `Destination Phase | Item Count | Notes`
- **Phase 28 backlog count:** how many items landed in 28-BACKLOG.md
- **March-5 bug status from walkthrough:** for each of BUG-01/02/03, state reproduced / not reproduced / not tested
- **FIN-01 status from walkthrough:** meeting detail 404 still reproduces (yes / no / not tested)
- **Surprises / unexpected findings:** anything the walkthrough surfaced that was not predicted by RESEARCH.md
- **Downstream impact:** which Phase 22-27 plans will see scope additions when they are planned
- **Update to STATE.md:** record Phase 21 complete, summarize urgent-vs-deferred counts, point to the two baseline files
</output>
