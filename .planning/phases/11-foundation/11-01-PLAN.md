---
phase: 11-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/empty-state.tsx
  - src/components/ui/table-skeleton.tsx
  - src/components/ui/page-header.tsx
  - src/components/ui/section-panel.tsx
  - src/lib/utils.ts
  - src/components/ui/stat-card.tsx
  - src/lib/__tests__/foundation.test.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-04, FOUND-05, FOUND-06, FOUND-07, FOUND-08]

must_haves:
  truths:
    - "EmptyState component renders with icon, title, description, CTA button for true-empty, and 'Clear filters' link for filtered-empty"
    - "TableSkeleton renders exactly 5 skeleton rows with N columns using animate-pulse"
    - "PageHeader renders title-only for list pages and breadcrumb trail for detail pages, with an actions slot on the right"
    - "SectionPanel wraps content in the standard white card pattern with optional title and noPadding mode"
    - "formatDate() outputs 'Mar 8, 2026' format, formatRelativeTime() outputs '3h ago' for recent times"
    - "fmt() remains unchanged; duplicate formatCurrency definitions are identified for removal in Plan 02"
    - "StatCard has proper dark: classes for dark mode parity"
  artifacts:
    - path: "src/components/ui/empty-state.tsx"
      provides: "EmptyState component with true-empty and filtered-empty variants"
      exports: ["EmptyState"]
    - path: "src/components/ui/table-skeleton.tsx"
      provides: "TableSkeleton component with fixed 5-row default"
      exports: ["TableSkeleton"]
    - path: "src/components/ui/page-header.tsx"
      provides: "PageHeader with breadcrumbs, title, subtitle, and actions slot"
      exports: ["PageHeader"]
    - path: "src/components/ui/section-panel.tsx"
      provides: "SectionPanel wrapping white card pattern"
      exports: ["SectionPanel"]
    - path: "src/lib/utils.ts"
      provides: "formatDate, formatDateShort, formatRelativeTime added alongside existing fmt/pct/cn"
      exports: ["fmt", "pct", "cn", "formatDate", "formatDateShort", "formatRelativeTime"]
    - path: "src/lib/__tests__/foundation.test.ts"
      provides: "Unit tests for all 4 new components and 3 new formatters"
      min_lines: 80
  key_links:
    - from: "src/components/ui/empty-state.tsx"
      to: "lucide-react"
      via: "InboxIcon default import"
      pattern: "import.*lucide-react"
    - from: "src/components/ui/page-header.tsx"
      to: "next/link"
      via: "breadcrumb href links"
      pattern: "import.*Link.*next/link"
    - from: "src/components/ui/section-panel.tsx"
      to: "src/lib/utils.ts"
      via: "cn() for className merging"
      pattern: "import.*cn.*utils"
---

<objective>
Create the 4 new shared UI components (EmptyState, TableSkeleton, PageHeader, SectionPanel), add date/currency formatter functions to utils.ts, fix StatCard dark mode, and create the Wave 0 test scaffold covering all new components and formatters.

Purpose: These components and formatters are the foundation that all subsequent migration plans (02-05) depend on. Building and testing them first ensures the migration sweeps in later plans have stable, verified building blocks.

Output: 6 new/modified source files + 1 test file, all passing `npx vitest run src/lib/__tests__/foundation.test.ts`
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation/11-CONTEXT.md
@.planning/phases/11-foundation/11-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs -->

From src/lib/utils.ts (KEEP EXISTING, ADD TO):
```typescript
export function fmt(n: number): string { /* currency formatter - DO NOT MODIFY */ }
export function pct(n: number): string { /* percentage formatter - DO NOT MODIFY */ }
export function cn(...classes: (string | false | null | undefined)[]): string { /* class merger - DO NOT MODIFY */ }
```

From src/components/ui/confirm-dialog.tsx (REFERENCE ONLY):
```typescript
interface ConfirmDialogProps {
  open: boolean;
  onClose: () => void;
  onConfirm: () => void;
  title: string;
  message: string;
  confirmLabel?: string;
  variant?: "primary" | "danger";
  loading?: boolean;
}
export function ConfirmDialog(props: ConfirmDialogProps): JSX.Element;
```

From src/components/ui/stat-card.tsx (MODIFY for dark mode):
```typescript
export function StatCard({ label, value, sub, trend, small }: {
  label: string; value: string; sub?: string; trend?: number; small?: boolean;
}): JSX.Element;
// Currently has: bg-white, text-gray-900, text-gray-500
// Missing: dark:bg-gray-900, dark:text-gray-100, dark:text-gray-400, dark:border-gray-700
```

From src/components/ui/modal.tsx (used by ConfirmDialog):
```typescript
// Modal already has dark mode styles - reference for consistency
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test scaffold and formatter functions</name>
  <files>src/lib/__tests__/foundation.test.ts, src/lib/utils.ts</files>
  <behavior>
    - formatDate(new Date(2026, 2, 8)) returns "Mar 8, 2026"
    - formatDate(null) returns em-dash
    - formatDate(undefined) returns em-dash
    - formatDate("invalid") returns em-dash
    - formatDateShort(new Date(2026, 2, 8)) returns "Mar 8"
    - formatRelativeTime for 30 seconds ago returns "just now"
    - formatRelativeTime for 45 minutes ago returns "45m ago"
    - formatRelativeTime for 3 hours ago returns "3h ago"
    - formatRelativeTime for 1 day ago returns "yesterday"
    - formatRelativeTime for 5 days ago returns "5d ago"
    - formatRelativeTime for 14 days ago falls back to formatDate
    - fmt(0) still returns "$0" (existing - regression check)
    - fmt(1500000) still returns "$1.5M" (existing - regression check)
  </behavior>
  <action>
    1. Create `src/lib/__tests__/foundation.test.ts` with test scaffolds for:
       - formatDate(), formatDateShort(), formatRelativeTime() (from behavior list above)
       - Placeholder describe blocks for EmptyState, TableSkeleton, PageHeader, SectionPanel (will be filled in Task 2)
       - Regression tests for existing fmt() and pct() functions

    2. Add to `src/lib/utils.ts` (AFTER existing fmt/pct/cn -- do NOT modify them):
       - `formatDate(d: string | Date | null | undefined): string` -- uses `new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })` with try/catch returning em-dash on failure. Use native Intl.DateTimeFormat (no date-fns per research recommendation).
       - `formatDateShort(d: string | Date | null | undefined): string` -- same but without year
       - `formatRelativeTime(isoStr: string): string` -- relative time for activity feeds (just now, Xm ago, Xh ago, yesterday, Xd ago, falls back to formatDate for 7+ days)

    IMPORTANT: Use `new Date(year, month, day)` constructor in tests (local time) to avoid UTC timezone off-by-one issues documented in pitfall 5 of the research.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx vitest run src/lib/__tests__/foundation.test.ts</automated>
  </verify>
  <done>All formatter tests pass. formatDate("Mar 8, 2026"), formatDateShort("Mar 8"), formatRelativeTime("3h ago") all produce expected output. Existing fmt/pct unchanged and regression tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 shared UI components and fix StatCard dark mode</name>
  <files>src/components/ui/empty-state.tsx, src/components/ui/table-skeleton.tsx, src/components/ui/page-header.tsx, src/components/ui/section-panel.tsx, src/components/ui/stat-card.tsx, src/lib/__tests__/foundation.test.ts</files>
  <action>
    Create 4 new components following the exact patterns from 11-RESEARCH.md:

    1. **EmptyState** (`src/components/ui/empty-state.tsx`):
       - "use client" directive
       - Props: icon (ReactNode, defaults to InboxIcon from lucide-react), title (string, required), description (string, optional), action ({ label, onClick }, optional), filtered (boolean, optional), onClearFilters (callback, optional)
       - When `filtered=false` (true-empty): show icon + title + description + CTA button
       - When `filtered=true` (filtered-empty): show icon + title + "Clear filters" link (no CTA)
       - Dark mode classes on all text/icon elements: text-gray-600 dark:text-gray-300 for title, text-gray-400 dark:text-gray-500 for description, text-gray-300 dark:text-gray-600 for icon wrapper
       - CTA button uses existing Button component from ./button

    2. **TableSkeleton** (`src/components/ui/table-skeleton.tsx`):
       - NO "use client" needed (no hooks)
       - Props: columns (number, required), rows (number, defaults to 5 -- locked decision)
       - Renders `<tr>` rows (NOT wrapped in tbody -- caller wraps), each with N `<td>` cells containing `animate-pulse` divs
       - First column skeleton wider (w-32), last column narrower (w-12), middle columns (w-20)
       - Dark mode: bg-gray-100 dark:bg-gray-800 for skeleton bars, border-gray-100 dark:border-gray-800 for row borders

    3. **PageHeader** (`src/components/ui/page-header.tsx`):
       - "use client" directive (uses Link from next/link)
       - Props: title (string), subtitle (string, optional), breadcrumbs (Array<{ label, href? }>, optional), actions (ReactNode, optional)
       - Breadcrumbs render as linked trail with "/" separators; last item is current page (no link)
       - Title: text-lg font-bold text-gray-900 dark:text-gray-100 (standardized heading)
       - Subtitle: text-xs text-gray-500 dark:text-gray-400
       - Actions slot: flex container on the right side
       - Breadcrumb text: text-xs text-gray-400, hover text-gray-600 dark:hover:text-gray-300

    4. **SectionPanel** (`src/components/ui/section-panel.tsx`):
       - NO "use client" needed (pure wrapper)
       - Props: title (string, optional), headerRight (ReactNode, optional), children (ReactNode), className (string, optional), noPadding (boolean, optional)
       - Wrapper: bg-white dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-700
       - Header row (when title or headerRight): flex between, px-4 py-3, border-b border-gray-100 dark:border-gray-800
       - Header title: text-sm font-semibold text-gray-900 dark:text-gray-100
       - Content: p-4 unless noPadding=true
       - Uses cn() from @/lib/utils for className merging

    5. **Fix StatCard dark mode** (`src/components/ui/stat-card.tsx`):
       - Add dark: classes to existing StatCard (currently missing ALL dark mode):
       - bg-white -> bg-white dark:bg-gray-900
       - border-gray-200 -> border-gray-200 dark:border-gray-700
       - text-gray-500 -> text-gray-500 dark:text-gray-400
       - text-gray-900 -> text-gray-900 dark:text-gray-100
       - text-emerald-600 -> text-emerald-600 dark:text-emerald-400
       - text-red-600 -> text-red-600 dark:text-red-400

    6. **Add component tests** to `src/lib/__tests__/foundation.test.ts`:
       - For FOUND-03: Add a test that reads all .tsx/.ts source files in src/ (excluding test files and node_modules) and asserts no raw `confirm(` calls exist (grep-as-test pattern). This test will initially FAIL (expected -- confirms the migration need). Mark it with `.skip` and add a comment: "Enable after Plan 02 completes confirm() migration"
       - For components: Add basic render tests if jsdom is configured, otherwise add structural tests that verify exports exist

    IMPORTANT: Every new component MUST have paired dark: classes for every light-mode color. Follow the exact dark mode pattern: text-gray-900 dark:text-gray-100, bg-white dark:bg-gray-900, border-gray-200 dark:border-gray-700, bg-gray-50 dark:bg-gray-800.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx vitest run src/lib/__tests__/foundation.test.ts && npm run build</automated>
  </verify>
  <done>All 4 new components exist with proper exports and dark mode classes. StatCard has dark mode fixed. Test file covers formatters and component existence. Build passes with zero errors.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/foundation.test.ts` -- all tests green
2. `npm run build` -- zero TypeScript errors
3. All 4 new component files exist in src/components/ui/
4. utils.ts exports formatDate, formatDateShort, formatRelativeTime alongside unchanged fmt/pct/cn
5. StatCard has dark: classes on all color properties
</verification>

<success_criteria>
- 4 new shared components created and exported (EmptyState, TableSkeleton, PageHeader, SectionPanel)
- 3 new formatter functions added to utils.ts (formatDate, formatDateShort, formatRelativeTime)
- StatCard dark mode fixed
- Wave 0 test scaffold created with passing tests for formatters
- Build passes with zero errors
- All components have full dark mode parity (dark: classes on every color/background/border)
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation/11-01-SUMMARY.md`
</output>
