---
phase: 11-foundation
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/(gp)/settings/page.tsx
  - src/app/(gp)/tasks/page.tsx
  - src/app/(gp)/deals/page.tsx
  - src/app/(gp)/directory/page.tsx
  - src/app/(gp)/transactions/page.tsx
  - src/app/(gp)/meetings/page.tsx
  - src/app/(gp)/documents/page.tsx
  - src/app/(gp)/assets/[id]/page.tsx
  - src/components/features/deals/deal-overview-tab.tsx
  - src/components/features/deals/deal-dd-tab.tsx
  - src/components/features/deals/deal-documents-tab.tsx
  - src/components/features/deals/workstream-detail-panel.tsx
  - src/components/features/deals/deal-closing-tab.tsx
  - src/components/features/deals/deal-ic-review-tab.tsx
  - src/components/features/accounting/entity-accounting-tab.tsx
  - src/components/features/accounting/trial-balance-view.tsx
  - src/components/features/side-letters/side-letter-rules-panel.tsx
  - src/lib/ai-service.ts
autonomous: true
requirements: [FOUND-06, FOUND-07]

must_haves:
  truths:
    - "All dates display as 'Mar 8, 2026' format consistently across every page -- no raw toLocaleDateString() calls remain"
    - "All currency values use fmt() from utils.ts -- no duplicate local formatCurrency() definitions"
    - "Activity feeds and recent timestamps use formatRelativeTime() for relative display"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "Canonical formatDate, formatDateShort, formatRelativeTime (from Plan 01)"
      exports: ["formatDate", "formatDateShort", "formatRelativeTime"]
  key_links:
    - from: "src/app/(gp)/settings/page.tsx"
      to: "src/lib/utils.ts"
      via: "import formatDate"
      pattern: "import.*formatDate.*utils"
    - from: "src/components/features/deals/deal-dd-tab.tsx"
      to: "src/lib/utils.ts"
      via: "import formatDate"
      pattern: "import.*formatDate.*utils"
    - from: "src/components/features/accounting/trial-balance-view.tsx"
      to: "src/lib/utils.ts"
      via: "import fmt (replaces local formatCurrency)"
      pattern: "import.*fmt.*utils"
---

<objective>
Replace ~25 raw toLocaleDateString() calls with formatDate() from utils.ts and remove 3 duplicate local formatCurrency() definitions, consolidating to fmt() from utils.ts. Every date and currency value in the app comes from the canonical formatters.

Purpose: Consistent formatting is the foundation of a professional app. Dates like "3/8/2026" vs "Mar 8, 2026" vs "2026-03-08" appearing on different pages creates a sloppy impression. Currency duplicates risk divergent behavior.

Output: ~18 files modified with formatter imports replaced, zero raw toLocaleDateString() calls remaining in GP page files
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation/11-CONTEXT.md
@.planning/phases/11-foundation/11-RESEARCH.md
@.planning/phases/11-foundation/11-01-SUMMARY.md

<interfaces>
<!-- Formatter functions from Plan 01 -->
From src/lib/utils.ts:
```typescript
// EXISTING (DO NOT MODIFY)
export function fmt(n: number): string;  // "$1.5M", "$0", "$450K"
export function pct(n: number): string;  // "15.0%"
export function cn(...classes: (string | false | null | undefined)[]): string;

// NEW (from Plan 01)
export function formatDate(d: string | Date | null | undefined): string;  // "Mar 8, 2026" or em-dash
export function formatDateShort(d: string | Date | null | undefined): string;  // "Mar 8" or em-dash
export function formatRelativeTime(isoStr: string): string;  // "3h ago", "yesterday", etc.
```

<!-- Files with duplicate formatCurrency to REMOVE -->
- src/components/features/accounting/trial-balance-view.tsx line ~66: local formatCurrency() -- DELETE, import fmt from utils
- src/components/features/side-letters/side-letter-rules-panel.tsx line ~103: local formatCurrency() -- DELETE, import fmt from utils
- src/lib/ai-service.ts line ~106: local formatCurrency helper -- CONVERT to use fmt from utils
- src/components/features/deals/close-deal-modal.tsx: has formatCurrencyInput() -- KEEP (input field formatting, different purpose)
- src/lib/pdf/shared-styles.ts: has formatCurrency() -- KEEP (used by PDF tests, separate context)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate formatCurrency definitions and migrate date formatting in GP pages</name>
  <files>src/app/(gp)/settings/page.tsx, src/app/(gp)/tasks/page.tsx, src/app/(gp)/deals/page.tsx, src/app/(gp)/directory/page.tsx, src/app/(gp)/transactions/page.tsx, src/app/(gp)/meetings/page.tsx, src/app/(gp)/documents/page.tsx, src/app/(gp)/assets/[id]/page.tsx, src/components/features/accounting/trial-balance-view.tsx, src/components/features/side-letters/side-letter-rules-panel.tsx, src/lib/ai-service.ts</files>
  <action>
    **Part A: Remove duplicate formatCurrency() definitions (FOUND-07)**

    1. **trial-balance-view.tsx** (~line 66): Delete the local `formatCurrency` function. Add `fmt` to the existing import from `@/lib/utils` (or add the import if not present). Replace all `formatCurrency(value)` calls in this file with `fmt(value)`.

    2. **side-letter-rules-panel.tsx** (~line 103): Delete the local `formatCurrency` function. Add `fmt` to the import from `@/lib/utils`. Replace all `formatCurrency(value)` calls with `fmt(value)`.

    3. **ai-service.ts** (~line 106): Find the local formatCurrency helper. If it's a simple currency formatter, replace with `fmt` from utils. If it has AI-specific formatting needs (e.g., full precision for AI prompts), keep it but rename to avoid confusion (e.g., `formatCurrencyFull`).

    DO NOT touch:
    - `src/components/features/deals/close-deal-modal.tsx` (formatCurrencyInput is input-field formatting, different purpose)
    - `src/lib/pdf/shared-styles.ts` (PDF context, used by PDF-specific tests)

    **Part B: Migrate date formatting in GP pages (FOUND-06)**

    For each page file listed in the research (see file list), read the file, find all `new Date(x).toLocaleDateString(...)` or `toLocaleDateString()` calls, and replace with `formatDate(x)` from `@/lib/utils`.

    Research-identified locations (~25 calls across these files):
    - settings/page.tsx: 3 calls
    - tasks/page.tsx: 2 calls
    - deals/page.tsx: 1 call
    - directory/page.tsx: 1 call
    - transactions/page.tsx: 4 calls
    - meetings/page.tsx: 2 calls
    - documents/page.tsx: 2 calls
    - assets/[id]/page.tsx: 2 calls

    For each replacement:
    - `{new Date(item.createdAt).toLocaleDateString()}` becomes `{formatDate(item.createdAt)}`
    - `{new Date(item.createdAt).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })}` becomes `{formatDate(item.createdAt)}`
    - Add `formatDate` to the import from `@/lib/utils` (may already import fmt/pct/cn)

    For activity feed or recent-timestamp contexts, use `formatRelativeTime()` instead of `formatDate()` when the original showed relative time.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>3 duplicate formatCurrency definitions removed, ~14 toLocaleDateString calls replaced with formatDate in GP page files. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate date formatting in deal feature components</name>
  <files>src/components/features/deals/deal-overview-tab.tsx, src/components/features/deals/deal-dd-tab.tsx, src/components/features/deals/deal-documents-tab.tsx, src/components/features/deals/workstream-detail-panel.tsx, src/components/features/deals/deal-closing-tab.tsx, src/components/features/deals/deal-ic-review-tab.tsx, src/components/features/accounting/entity-accounting-tab.tsx</files>
  <action>
    Migrate remaining toLocaleDateString() calls in deal feature components and accounting tab.

    Research-identified locations (~13 calls across these files):
    - deal-overview-tab.tsx: 2 calls
    - deal-dd-tab.tsx: 4 calls
    - deal-documents-tab.tsx: 1 call
    - workstream-detail-panel.tsx: 2 calls
    - deal-closing-tab.tsx: 1 call
    - deal-ic-review-tab.tsx: 3 calls
    - entity-accounting-tab.tsx: 1 call

    For each file:
    1. Read the file
    2. Find all `new Date(x).toLocaleDateString(...)` patterns
    3. Replace with `formatDate(x)` from `@/lib/utils`
    4. Add `formatDate` to the import from `@/lib/utils`
    5. If the file already imports from `@/lib/utils`, add `formatDate` to the existing import

    After all replacements, verify no raw toLocaleDateString() calls remain in GP source files:
    ```bash
    grep -rn "toLocaleDateString" src/app/ src/components/ --include="*.tsx" --include="*.ts" | grep -v "__tests__" | grep -v "node_modules" | grep -v "pdf/"
    ```
    This should return zero results (or only results in files we intentionally skipped like PDF).

    IMPORTANT: Some date displays may use `toLocaleString()` (with time) rather than `toLocaleDateString()`. Leave those alone unless they clearly should be date-only. Focus on `toLocaleDateString()` calls only.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx vitest run src/lib/__tests__/foundation.test.ts && npm run build</automated>
  </verify>
  <done>All ~25 toLocaleDateString() calls across GP pages and deal components replaced with formatDate(). Zero raw toLocaleDateString() calls remain outside PDF context. All 3 duplicate formatCurrency() definitions removed. Tests pass. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "toLocaleDateString" src/app/ src/components/ --include="*.tsx" --include="*.ts" | grep -v "__tests__" | grep -v "pdf/"` returns zero results
2. `grep -rn "function formatCurrency" src/ --include="*.tsx" --include="*.ts" | grep -v "pdf/" | grep -v "close-deal-modal" | grep -v "__tests__"` returns zero results (no duplicates remain)
3. `npx vitest run src/lib/__tests__/foundation.test.ts` -- all formatter tests pass
4. `npm run build` -- zero errors
</verification>

<success_criteria>
- Zero raw toLocaleDateString() calls remain in GP page files and deal components (PDF context excluded)
- Zero duplicate formatCurrency() definitions remain (close-deal-modal's formatCurrencyInput and PDF's formatCurrency are intentionally kept)
- All dates display as "Mar 8, 2026" format
- All currency values flow through fmt() from utils.ts
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation/11-04-SUMMARY.md`
</output>
