---
phase: 11-foundation
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - src/app/(gp)/deals/page.tsx
  - src/app/(gp)/assets/page.tsx
  - src/app/(gp)/entities/page.tsx
  - src/app/(gp)/transactions/page.tsx
  - src/app/(gp)/directory/page.tsx
  - src/app/(gp)/tasks/page.tsx
  - src/app/(gp)/documents/page.tsx
  - src/app/(gp)/meetings/page.tsx
  - src/app/(gp)/analytics/page.tsx
  - src/app/(gp)/reports/page.tsx
  - src/app/(gp)/accounting/page.tsx
  - src/app/(gp)/settings/page.tsx
  - src/app/(gp)/dashboard/page.tsx
autonomous: false
requirements: [FOUND-04, FOUND-05, FOUND-08]

must_haves:
  truths:
    - "Page titles, subtitles, and action buttons look identical across all GP list pages -- no one-off heading styles"
    - "Section cards use the same white card wrapper pattern everywhere -- no ad-hoc card borders"
    - "All new and modified components look correct in dark mode -- no white-on-white or black-on-black issues"
  artifacts:
    - path: "src/app/(gp)/assets/page.tsx"
      provides: "PageHeader replaces ad-hoc heading, SectionPanel wraps table"
      contains: "PageHeader"
    - path: "src/app/(gp)/entities/page.tsx"
      provides: "PageHeader replaces ad-hoc heading, SectionPanel wraps table"
      contains: "PageHeader"
    - path: "src/app/(gp)/deals/page.tsx"
      provides: "PageHeader replaces ad-hoc heading"
      contains: "PageHeader"
  key_links:
    - from: "src/app/(gp)/assets/page.tsx"
      to: "src/components/ui/page-header.tsx"
      via: "import PageHeader"
      pattern: "import.*PageHeader.*page-header"
    - from: "src/app/(gp)/assets/page.tsx"
      to: "src/components/ui/section-panel.tsx"
      via: "import SectionPanel"
      pattern: "import.*SectionPanel.*section-panel"
---

<objective>
Adopt PageHeader and SectionPanel across all GP list pages to standardize headings and card wrappers, then verify dark mode parity across all modified components.

Purpose: This is the final polish pass -- after Plans 01-04 built components and migrated formatters/confirm dialogs/skeletons/empty states, this plan makes every page look consistent with standardized headings and card wrappers, then validates the entire phase visually in dark mode.

Output: ~13 page files updated with PageHeader/SectionPanel, visual verification of dark mode across all changes
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation/11-CONTEXT.md
@.planning/phases/11-foundation/11-RESEARCH.md
@.planning/phases/11-foundation/11-01-SUMMARY.md
@.planning/phases/11-foundation/11-02-SUMMARY.md
@.planning/phases/11-foundation/11-03-SUMMARY.md
@.planning/phases/11-foundation/11-04-SUMMARY.md

<interfaces>
<!-- Components from Plan 01 -->
From src/components/ui/page-header.tsx:
```typescript
interface BreadcrumbItem {
  label: string;
  href?: string;
}
interface PageHeaderProps {
  title: string;
  subtitle?: string;
  breadcrumbs?: BreadcrumbItem[];
  actions?: React.ReactNode;
}
export function PageHeader(props: PageHeaderProps): JSX.Element;
```

From src/components/ui/section-panel.tsx:
```typescript
interface SectionPanelProps {
  title?: string;
  headerRight?: React.ReactNode;
  children: React.ReactNode;
  className?: string;
  noPadding?: boolean;
}
export function SectionPanel(props: SectionPanelProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt PageHeader and SectionPanel across all GP list pages</name>
  <files>src/app/(gp)/deals/page.tsx, src/app/(gp)/assets/page.tsx, src/app/(gp)/entities/page.tsx, src/app/(gp)/transactions/page.tsx, src/app/(gp)/directory/page.tsx, src/app/(gp)/tasks/page.tsx, src/app/(gp)/documents/page.tsx, src/app/(gp)/meetings/page.tsx, src/app/(gp)/analytics/page.tsx, src/app/(gp)/reports/page.tsx, src/app/(gp)/accounting/page.tsx, src/app/(gp)/settings/page.tsx, src/app/(gp)/dashboard/page.tsx</files>
  <action>
    Replace ad-hoc heading patterns with PageHeader on all GP list pages. Per the user's locked decision: list pages get title only (no breadcrumbs), detail pages get breadcrumb trail. This task covers LIST pages only.

    For each page:
    1. Read the file
    2. Find the heading section -- typically a `<div>` with `<h1>` or `<h2>` and action buttons
    3. Replace with `<PageHeader title="Page Name" subtitle="optional count or description" actions={<existing action buttons>} />`
    4. Move existing action buttons (create button, search input, filter dropdowns) into the `actions` prop as a React fragment
    5. Import PageHeader from "@/components/ui/page-header"

    **Expected heading patterns to find and replace:**
    ```tsx
    // BEFORE (common patterns across pages):
    <div className="flex items-center justify-between mb-4">
      <h1 className="text-xl font-bold text-gray-900">Assets</h1>
      <Button onClick={() => setShowCreate(true)}>+ Add Asset</Button>
    </div>

    // AFTER:
    <PageHeader
      title="Assets"
      subtitle={`${allAssets.length} assets`}
      actions={<Button onClick={() => setShowCreate(true)}>+ Add Asset</Button>}
    />
    ```

    **Subtitle guidance (Claude's discretion per CONTEXT.md):**
    - Pages with record counts: show count (e.g., "12 deals", "5 entities")
    - Pages without meaningful counts: no subtitle
    - Dashboard: no subtitle

    **Where to wrap SectionPanel:**
    After replacing the heading, look for the main content area (table or card grid). If it's already wrapped in `<div className="bg-white rounded-xl border border-gray-200 ...">`, replace that wrapper div with `<SectionPanel noPadding>` (noPadding for tables since they manage their own cell padding).

    Apply SectionPanel to the primary content area of each page where the white card pattern exists. Not every page needs SectionPanel -- some use stat cards or other layouts. Only wrap sections that currently have the ad-hoc white card pattern.

    Import SectionPanel from "@/components/ui/section-panel".

    **Pages to skip SectionPanel on:**
    - Dashboard (has its own complex layout with multiple stat cards)
    - Analytics (may have chart-specific wrappers)
    - Settings (has its own tab-based layout)

    Only adopt SectionPanel where it cleanly replaces an existing `bg-white rounded-xl border` wrapper without requiring layout restructuring.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>All GP list pages use PageHeader for headings. Pages with white card wrapper divs use SectionPanel. Heading styles are consistent across all pages. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dark mode parity across all Phase 11 changes</name>
  <files>N/A -- visual verification only</files>
  <action>
    This is a human verification checkpoint. Claude starts the dev server if not running, then presents the testing checklist to the user. No code changes in this task.

    All Phase 11 changes across Plans 01-05 need visual dark mode verification:
    - 4 new shared components (EmptyState, TableSkeleton, PageHeader, SectionPanel)
    - StatCard dark mode fix
    - ConfirmDialog migration (7 sites)
    - Skeleton loading on all list pages
    - EmptyState on all list pages
    - Date/currency formatter consolidation
    - PageHeader/SectionPanel adoption across all GP pages

    Steps for the user:
    1. Start dev server: `npm run dev`
    2. Open browser to http://localhost:3000
    3. Go to Settings page and toggle Dark Mode ON
    4. Visit /deals -- verify PageHeader, skeleton loading (refresh page fast), EmptyState (narrow filters to get no results)
    5. Visit /assets -- verify PageHeader, SectionPanel, skeleton, EmptyState
    6. Visit /entities -- verify PageHeader, SectionPanel, skeleton, EmptyState
    7. Visit /transactions -- verify PageHeader, skeleton, EmptyState
    8. Visit /directory -- verify PageHeader, skeleton, EmptyState
    9. Visit /tasks -- verify PageHeader, skeleton, EmptyState
    10. Visit /documents -- verify PageHeader, EmptyState
    11. Visit /meetings -- verify PageHeader, EmptyState
    12. Visit /settings -- trigger a destructive action (e.g., try to delete a pipeline stage) to verify ConfirmDialog appears styled correctly in dark mode
    13. Check that StatCard components on dashboard look correct in dark mode
    14. Toggle back to Light Mode and quickly scan the same pages to ensure nothing broke

    What to look for:
    - Text should be light (gray-100) on dark backgrounds (gray-900)
    - Borders should be visible (gray-700) not invisible
    - Skeleton pulse bars should be visible (gray-800 on gray-900 background)
    - EmptyState icons and text should be readable
    - ConfirmDialog modal should have dark background and readable text
    - No elements that disappear against the background
  </action>
  <verify>User confirms dark mode looks correct by typing "approved"</verify>
  <done>User has visually verified that all Phase 11 components render correctly in both light and dark mode across all GP pages. No white-on-white or black-on-black artifacts found.</done>
</task>

</tasks>

<verification>
1. `npm run build` -- zero errors
2. All GP list pages use PageHeader component (consistent heading style)
3. Content areas with white card pattern use SectionPanel
4. Dark mode visual check passes across all pages
5. `npx vitest run` -- full test suite green
</verification>

<success_criteria>
- Every GP list page uses PageHeader for its heading (consistent text-lg font-bold)
- Content sections with the white card pattern use SectionPanel
- Dark mode looks correct on all pages -- no visual artifacts
- Full vitest suite passes
- Build passes with zero errors
- User approves dark mode visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation/11-05-SUMMARY.md`
</output>
