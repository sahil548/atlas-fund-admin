---
phase: 12-ai-configuration-document-intake
plan: 05
type: execute
wave: 4
depends_on: ["12-02", "12-04"]
files_modified: []
autonomous: false
requirements: [AICONF-01, AICONF-02, AICONF-03, AICONF-04, AICONF-05, DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "All 8 phase requirements verified through manual end-to-end testing"
    - "Tenant AI key management works from settings"
    - "Per-user AI toggle works in Users & Access tab"
    - "Personal AI key override works in profile page"
    - "Document upload triggers automatic extraction for supported categories"
    - "Extraction preview side panel allows review and apply"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for all Phase 12 requirements.

Purpose: Verify that all 8 requirements (AICONF-01 through AICONF-05, DOC-01 through DOC-03) work correctly together as a complete system. This is the final checkpoint before the phase can be marked complete.

Output: Verified phase completion or a list of issues to address.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ai-configuration-document-intake/12-CONTEXT.md
@.planning/phases/12-ai-configuration-document-intake/12-01-SUMMARY.md
@.planning/phases/12-ai-configuration-document-intake/12-02-SUMMARY.md
@.planning/phases/12-ai-configuration-document-intake/12-03-SUMMARY.md
@.planning/phases/12-ai-configuration-document-intake/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and smoke test</name>
  <files></files>
  <action>
Run the full build to ensure no TypeScript errors exist across the entire codebase after all Phase 12 changes:

```bash
npm run build
```

If the build fails, fix any TypeScript errors before proceeding to the human verification checkpoint.

Also verify database state:
- Run `npx prisma validate` to confirm schema is valid
- Optionally run `npx prisma studio` to spot-check that User model has aiEnabled field and Document model has extractionStatus field
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>Full build passes with zero TypeScript errors. Prisma schema validates. All Phase 12 code compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of all Phase 12 requirements</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for approval. The complete AI Configuration and Document Intake system includes:
- Tenant AI key management (AICONF-01) -- already existed, verified
- Per-user AI toggle in Settings > Users and Access (AICONF-02)
- Personal AI key override in /profile (AICONF-03)
- AI config fallback chain: user key then tenant key then none (AICONF-04)
- SERVICE_PROVIDER AI disabled by default (AICONF-05)
- Automatic document extraction on upload for CIM/lease/credit/K-1 (DOC-01)
- Extracted data linked to parent deal/asset/entity (DOC-02)
- Extraction status badges + review side panel with apply workflow (DOC-03)

Verification steps for the user:

Start the dev server: `npm run dev` and open http://localhost:3000

Test AICONF-01 (Tenant AI key):
1. Go to /settings then AI Configuration tab
2. Verify you can set/update a tenant-wide API key (this was already built -- just verify it still works)

Test AICONF-02 (Per-user AI toggle):
1. Go to /settings then Users and Access tab
2. Verify "AI Access" column exists with toggle switches
3. Toggle AI off for a GP_TEAM user -- should see toast confirmation
4. Verify LP_INVESTOR rows show "N/A" (no toggle)

Test AICONF-03 (Personal AI key override):
1. Click your user avatar/initials in the top bar -- should navigate to /profile
2. Verify "AI Settings" section appears
3. Set a personal provider/model/API key -- save
4. Verify status indicator changes to "Using: Your key"
5. Clear the personal key -- status should change to "Using: Firm default" (if tenant key exists) or "No key configured"

Test AICONF-04 (Fallback chain):
1. With both personal and tenant keys set: Profile shows "Using: Your key"
2. Remove personal key: Profile shows "Using: Firm default"
3. Remove tenant key too: Profile shows "No key configured"

Test AICONF-05 (SERVICE_PROVIDER default):
1. In Users and Access tab, check SERVICE_PROVIDER user -- AI toggle should be OFF by default

Test DOC-01 (Automatic extraction):
1. Navigate to any deal detail then Documents tab
2. Upload a PDF with category "Financial" (a CIM or credit doc)
3. Upload should return immediately (no waiting)
4. Document should show "Processing" badge, then "Extracted" after a few seconds
5. If no AI key configured, document uploads normally without extraction

Test DOC-02 (Auto-linking):
1. Verify the extracted document is linked to the deal (dealId populated)
2. Open the extraction panel -- fields should show deal-related data

Test DOC-03 (Status + preview panel):
1. Verify status badges show correctly: Processing (amber), Extracted (green), Failed (red)
2. Click an "Extracted" document -- side panel should slide open from right
3. Panel shows extracted fields with checkboxes and editable inputs
4. Edit a value, select some fields, click "Apply Selected"
5. Verify applied fields are saved (re-open panel to confirm)
6. For a FAILED document: verify retry button appears and works

Resume signal: Type "approved" if all tests pass, or describe any issues found.
  </action>
  <verify>User confirms all 8 requirements pass manual testing</verify>
  <done>All AICONF and DOC requirements verified working end-to-end. Phase 12 complete.</done>
</task>

</tasks>

<verification>
All 8 phase requirements tested end-to-end via manual browser testing.
</verification>

<success_criteria>
- All 5 AICONF requirements verified working
- All 3 DOC requirements verified working
- No TypeScript build errors
- No console errors during testing
- Phase 12 success criteria from ROADMAP.md all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-configuration-document-intake/12-05-SUMMARY.md`
</output>
