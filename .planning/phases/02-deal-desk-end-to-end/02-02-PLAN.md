---
phase: 02-deal-desk-end-to-end
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/features/deals/create-deal-wizard.tsx
  - src/components/features/deals/inline-edit-field.tsx
  - src/components/features/deals/kill-deal-modal.tsx
  - src/lib/deal-stage-engine.ts
  - src/app/api/deals/[id]/route.ts
  - src/app/(gp)/deals/[id]/page.tsx
  - src/app/(gp)/deals/page.tsx
  - src/app/api/deals/route.ts
autonomous: true
requirements:
  - DEAL-01
  - DEAL-05

must_haves:
  truths:
    - "Create Deal wizard shows inline validation errors under each field when required fields are missing"
    - "Create Deal wizard shows a toast summary of all validation errors"
    - "Wizard Step 1 includes Participation Structure dropdown"
    - "Wizard Step 1 includes Deal Source as optional field"
    - "Wizard Step 2 requires at least 1 document"
    - "Deal lead defaults to current user"
    - "Counterparty field supports inline company creation"
    - "InlineEditField saves reliably on blur and Enter without double-save"
    - "InlineEditField shows error toast on save failure"
    - "Textarea Enter inserts newline, only blur saves"
    - "Kill deal requires a reason (dropdown + free text)"
    - "Killed deals shown in separate Dead Deals view"
    - "Revive deal returns to original stage"
  artifacts:
    - path: "src/components/features/deals/create-deal-wizard.tsx"
      provides: "Enhanced wizard with validation, participation structure, deal source, counterparty inline creation"
    - path: "src/components/features/deals/inline-edit-field.tsx"
      provides: "Reliable inline edit with double-save prevention"
    - path: "src/components/features/deals/kill-deal-modal.tsx"
      provides: "Kill deal modal with reason dropdown and free text"
  key_links:
    - from: "create-deal-wizard.tsx"
      to: "/api/deals POST"
      via: "form submit with participationStructure field"
      pattern: "participationStructure"
    - from: "kill-deal-modal.tsx"
      to: "/api/deals/[id] PATCH"
      via: "kill action with reason payload"
      pattern: "action.*KILL"
    - from: "deal-stage-engine.ts reviveDeal"
      to: "prisma.deal.update"
      via: "restore previous stage"
      pattern: "previousStage"
---

<objective>
Polish deal creation wizard with validation and new fields, fix inline edit reliability, implement counterparty inline creation, and build kill/revive deal flow with required reasons.

Purpose: DEAL-01 (screening stage polish) and DEAL-05 (inline edit reliability) are the foundation for a trustworthy deal desk. Kill/revive is a critical pipeline workflow gap.
Output: Enhanced wizard with inline validation, participation structure, counterparty creation, reliable inline editing, kill/revive with reasons.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md
@.planning/phases/02-deal-desk-end-to-end/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/features/deals/create-deal-wizard.tsx:
- Props: { open: boolean; onClose: () => void }
- identity state: { name, assetClass, capitalInstrument, targetCheckSize, dealLeadId, gpName, counterparty, source, entityId }
- validateStep1() only checks name + assetClass currently -- needs expansion
- Existing patterns: useSWR for users/companies/entities, FileUpload component, toast for errors

From src/components/features/deals/inline-edit-field.tsx:
- Props: { label, value, field, dealId, type?, placeholder? }
- save() on blur and Enter, cancel() on Escape
- PUT /api/deals/[id] with { [field]: editValue }

From src/lib/deal-stage-engine.ts:
- killDeal(dealId): moves to DEAD, logs activity
- No kill reason params, no revive functionality

From prisma/schema.prisma (after Plan 01):
- Deal.killReason: String?
- Deal.killReasonText: String?
- Deal.previousStage: DealStage?
- Deal.participationStructure: ParticipationStructure? (already exists in enum)

From src/app/api/deals/route.ts (POST):
- Creates deal with identity fields
- participationStructure field exists in Prisma but wizard may not send it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Create Deal Wizard with validation, new fields, counterparty creation, and document requirement</name>
  <files>
    src/components/features/deals/create-deal-wizard.tsx,
    src/app/api/deals/route.ts
  </files>
  <action>
  Modify the CreateDealWizard to implement all DEAL-01 decisions:

  1. **Add Participation Structure dropdown to Step 1** (after Capital Instrument):
     - Options: "GP / Direct" (DIRECT_GP), "LP / Passive" (LP_STAKE_SILENT_PARTNER), "Co-GP / Co-Invest / JV" (CO_INVEST_JV_PARTNERSHIP)
     - Optional field (no validation required)

  2. **Add Deal Source field** (verify existing `source` in identity state sends to API):
     - If source field already exists in the form, ensure it's visible and connected
     - If not visible, add a text input or dropdown with common sources: Referral, Direct, Broker, Other

  3. **Deal lead defaults to current user**:
     - Import useUser from providers
     - On mount, if users list loaded and dealLeadId is empty, set identity.dealLeadId to current user's ID

  4. **Validation errors -- both inline AND toast**:
     - Step 1 validateStep1(): check name (required), assetClass (required) -- only required fields per user decision
     - Add a `fieldErrors` state: `Record<string, string>`
     - Show inline error message (red text below input) for each failing field
     - Show toast summary: `toast.error("Missing required fields: Deal Name, Asset Class")`
     - Clear individual field errors when user types/selects

  5. **Step 2: require at least 1 document**:
     - When clicking "Create Deal" or "Create & Screen", check docs.length >= 1
     - If 0 docs, show toast: "At least one document is required"
     - Do NOT block Step 1 -> Step 2 navigation

  6. **Counterparty inline creation** (promote from stretch goal per WARNING 4):
     - In the counterparty select, add an "Add New Company" option at the bottom
     - When selected, show inline fields: company name (required), type (default COUNTERPARTY)
     - POST to /api/companies to create, then auto-select the new company as counterparty
     - Pattern: same as entity creation inline in close modal

  7. **API route update**: Ensure POST /api/deals route accepts `participationStructure` in the Zod schema if not already there.

  Do NOT change the 2-step structure, the two-path flow (Create Deal / Create & Screen), or remove any existing fields.
  Use `const toast = useToast()` (never destructure). Follow existing form patterns.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - Step 1 shows inline error under Deal Name when empty and Next clicked
  - Toast summary fires with all missing fields
  - Participation Structure dropdown present in Step 1
  - Deal lead defaults to current user
  - Step 2 blocks submission with toast if 0 documents
  - Counterparty "Add New Company" inline creation works
  - Build passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix InlineEditField reliability</name>
  <files>src/components/features/deals/inline-edit-field.tsx</files>
  <action>
  Fix edge cases in inline-edit-field.tsx (DEAL-05):

  1. **Prevent double-save**: Add a `savingRef` (useRef<boolean>) to guard against concurrent save calls. If savingRef.current is true, skip the save.

  2. **Blur after Enter**: When Enter triggers save(), the blur event fires immediately after. Add a `justSavedRef` flag. In handleSave: set justSavedRef = true, do the save, then setTimeout(() => justSavedRef = false, 100). In handleBlur: if justSavedRef is true, skip the save (just exit edit mode).

  3. **Error feedback**: If the PUT fails (non-2xx), show a toast error and keep edit mode open so user doesn't lose input. Import useToast. Use: `const msg = typeof data.error === "string" ? data.error : "Failed to save"; toast.error(msg);` (prevents React crash from object errors).

  4. **Textarea Enter behavior**: For textarea type, Enter should NOT save (it inserts a newline). Only blur saves for textarea. Check if `type === "textarea"` in the keydown handler and skip the save-on-Enter logic. Optionally support Ctrl+Enter to save.

  Use `const toast = useToast()` (never destructure).
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - InlineEditField does not double-save on Enter+blur sequence
  - Error toast shown on save failure (no React crash from object errors)
  - Textarea Enter inserts newline, blur saves
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Kill/revive deal with reasons + Dead Deals UI</name>
  <files>
    src/components/features/deals/kill-deal-modal.tsx,
    src/lib/deal-stage-engine.ts,
    src/app/api/deals/[id]/route.ts,
    src/app/(gp)/deals/[id]/page.tsx,
    src/app/(gp)/deals/page.tsx
  </files>
  <action>
  **A. Create KillDealModal** (new file):
  - Props: { open, onClose, dealName, onConfirm(reason: string, reasonText: string), loading }
  - Dropdown with options: Pricing, Risk, Timing, Sponsor, Other
  - Free text textarea for explanation (optional)
  - Reason dropdown is required -- submit button disabled until selected
  - Use existing Modal component pattern from ui/modal.tsx

  **B. Update deal-stage-engine.ts killDeal()**:
  - Change signature: `killDeal(dealId: string, killReason: string, killReasonText?: string)`
  - Before setting DEAD: read current deal.stage, store as previousStage
  - Save killReason, killReasonText, previousStage on the deal record
  - Activity metadata should include { killReason, killReasonText }

  **C. Add reviveDeal() to deal-stage-engine.ts**:
  - Read deal.previousStage, set deal.stage back to that value
  - Clear killReason, killReasonText, previousStage (set to null)
  - Log DealActivity with type "DEAL_REVIVED" and metadata { revivedToStage }

  **D. Update PATCH /api/deals/[id]/route.ts**:
  - KILL action: validate body with KillDealSchema, pass killReason and killReasonText to killDeal()
  - Add REVIVE action: call reviveDeal(dealId), return updated deal

  **E. Update deal detail page (deals/[id]/page.tsx)**:
  - Replace existing ConfirmDialog for kill with KillDealModal
  - When deal is DEAD, show "Revive Deal" button (secondary style, not danger)
  - Show kill reason + free text on the DEAD banner if present

  **F. Update pipeline page (deals/page.tsx)**:
  - Dead deals already shown in separate modal -- verify this matches user decision (not collapsed column)
  - Add kill reason badge to dead deal cards in the modal
  - Add closing checklist progress (%) on deal cards in the Closing column: fetch closing items count vs COMPLETE count, show as "X% complete" badge

  **G. Closing warn-on-incomplete (WARNING 5)**:
  - In the close deal flow (wherever the close action is triggered), check if any ClosingChecklist items have status != COMPLETE
  - If incomplete items exist, show a ConfirmDialog: "X of Y closing items are incomplete. Close anyway?"
  - Allow override -- do NOT block closing

  Use `const toast = useToast()` (never destructure). Follow existing coding patterns.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - Kill deal modal requires reason selection
  - Kill stores reason, text, and previous stage
  - Revive button appears on dead deals, restores previous stage
  - Dead deal cards show kill reason
  - Closing column cards show checklist progress %
  - Closing warns on incomplete items but allows override
  - Build passes with zero errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Create Deal wizard: click Next with empty name => inline error + toast
- Create Deal wizard: participation structure dropdown visible
- Create Deal wizard: counterparty "Add New Company" inline creation works
- InlineEditField: rapid Enter+blur does not produce double API calls
- Kill deal from detail page => modal with reason dropdown => deal goes to DEAD with reason stored
- Revive from DEAD state => returns to previous stage
- Close deal with incomplete items => warning dialog, allow override
</verification>

<success_criteria>
DEAL-01: Wizard shows inline validation + toast, includes participation structure, deal source, counterparty creation, deal lead defaults to current user, Step 2 requires documents.
DEAL-05: InlineEditField saves reliably without double-fires, error feedback on failure, textarea newlines work.
Kill/revive: Kill requires reason, revive restores previous stage, pipeline UI reflects both. Closing warns on incomplete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-02-SUMMARY.md`
</output>
