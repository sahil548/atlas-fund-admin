---
phase: 02-deal-desk-end-to-end
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/features/deals/deal-closing-tab.tsx
  - src/components/features/deals/close-deal-modal.tsx
  - src/components/features/deals/deal-entity-section.tsx
  - src/lib/deal-stage-engine.ts
  - src/lib/closing-templates.ts
  - src/app/api/deals/[id]/closing/route.ts
  - src/app/api/deals/[id]/route.ts
  - src/app/(gp)/deals/[id]/page.tsx
  - prisma/schema.prisma
autonomous: true
requirements:
  - DEAL-02
  - DEAL-03
  - DEAL-04

must_haves:
  truths:
    - "Closing checklist supports custom items (GP can add beyond template)"
    - "Closing checklist items support per-item file attachments"
    - "Closing warns on incomplete items but allows override (does not block)"
    - "Deal close creates Asset with all metadata carried over (AI-extracted data, linked docs, counterparty)"
    - "After close, user is auto-redirected to the new asset page"
    - "Asset shows 'Originated from: [Deal Name]' link back to deal"
    - "Entity creation available at any deal stage (DealEntitySection always visible)"
    - "Deal can be linked to multiple entities via junction table"
    - "Multi-entity allocation % visible on deal detail (not just in close modal)"
  artifacts:
    - path: "src/components/features/deals/deal-closing-tab.tsx"
      provides: "Enhanced closing tab with custom items, file attachments"
    - path: "src/lib/deal-stage-engine.ts"
      provides: "closeDeal with full metadata carryover and multi-entity support"
    - path: "prisma/schema.prisma"
      provides: "DealEntity junction table, ClosingChecklist fileUrl field, Asset.sourceDealId"
  key_links:
    - from: "deal-closing-tab.tsx"
      to: "/api/deals/[id]/closing POST/PATCH"
      via: "create custom items, update with file attachments"
      pattern: "closing"
    - from: "deal-stage-engine.ts closeDeal"
      to: "prisma.asset.create"
      via: "full metadata carryover including AI data"
      pattern: "asset.*create"
    - from: "deal-entity-section.tsx"
      to: "/api/deals/[id]/entities"
      via: "junction table CRUD for multi-entity"
      pattern: "dealEntit"
---

<objective>
Complete the closing workflow end-to-end with checklist enhancements, implement deal-to-asset transition with full metadata carryover, and enable multi-entity deal participation with entity formation at any stage.

Purpose: DEAL-02 (closing workflow), DEAL-03 (deal-to-asset), and DEAL-04 (entity formation) are the core of the GP daily workflow. Closing a deal and getting a properly-formed asset is the primary value proposition.
Output: Enhanced closing checklist with attachments and custom items, seamless deal-to-asset transition, multi-entity deal support with junction table.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/deal-stage-engine.ts closeDeal():
- Accepts dealId, opts: { costBasis, fairValue?, entryDate?, force?, allocations: CloseAllocation[] }
- Creates Asset with: name, assetClass, capitalInstrument, participationStructure, sector, costBasis, fairValue, entryDate
- Creates AssetEntityAllocation per allocation
- Links deal documents to asset via prisma.document.updateMany
- Does NOT carry over: AI-extracted data (screeningResult), counterparty, gpName, description, thesisNotes, investmentRationale

From src/components/features/deals/deal-closing-tab.tsx:
- Fetches closing checklist via GET /api/deals/[id]/closing
- Initialize via POST with { action: "INITIALIZE" }
- Update via PATCH with { id, status/assigneeId/dueDate/notes }
- No custom item creation, no file attachments

From src/components/features/deals/deal-entity-section.tsx:
- Uses deal.entityId (single FK) to show one linked entity
- link/unlink/create+link via PUT /api/deals/[id] with { entityId }

From prisma/schema.prisma:
- Deal has entityId (single FK to Entity) — needs junction table
- ClosingChecklist: id, dealId, title, status, assigneeId, dueDate, notes — no fileUrl
- Asset: no sourceDealId field
- AssetEntityAllocation: assetId, entityId, allocationPercent, costBasis

From src/lib/closing-templates.ts:
- 8 template items focused on transactional closing mechanics
- User wants Claude to review and adjust for transactional focus (not DD items)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema changes for multi-entity deals, closing attachments, and asset source tracking</name>
  <files>
    prisma/schema.prisma,
    src/app/api/deals/[id]/entities/route.ts,
    src/lib/closing-templates.ts
  </files>
  <action>
  **Schema changes in prisma/schema.prisma:**

  1. **DealEntity junction table** (new model):
     ```
     model DealEntity {
       id                String  @id @default(cuid())
       dealId            String
       entityId          String
       allocationPercent Float?
       role              String? // e.g. "Lead Entity", "Co-Investor"
       createdAt         DateTime @default(now())

       deal   Deal   @relation(fields: [dealId], references: [id], onDelete: Cascade)
       entity Entity @relation(fields: [entityId], references: [id])

       @@unique([dealId, entityId])
       @@index([dealId])
       @@index([entityId])
     }
     ```
  2. Add `dealEntities DealEntity[]` relation to Deal model (keep existing entityId for backward compat)
  3. Add `dealEntities DealEntity[]` relation to Entity model

  4. **ClosingChecklist additions**:
     - Add `fileUrl String?` (for per-item file attachment via Vercel Blob)
     - Add `fileName String?` (display name of attached file)
     - Add `isCustom Boolean @default(false)` (distinguish template vs custom items)

  5. **Asset source tracking**:
     - Add `sourceDealId String?` to Asset model
     - Add relation: `sourceDeal Deal? @relation("AssetSourceDeal", fields: [sourceDealId], references: [id])`
     - Add `sourceAssets Asset[] @relation("AssetSourceDeal")` to Deal model

  6. **New API route: /api/deals/[id]/entities/route.ts**:
     - GET: return all DealEntity records with entity details (name, type, formation status)
     - POST: create DealEntity with { entityId, allocationPercent?, role? }. Validate entity belongs to same firm.
     - DELETE: remove DealEntity by entityId in query params

  7. **Update closing-templates.ts**: Review and adjust items to focus on transactional closing mechanics. Replace "Legal Due Diligence Completion" (that's a DD item) with something more appropriate like "Legal Opinion / Closing Deliverables". Keep the overall structure.

  After schema changes: `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION="yes" npx prisma db push --force-reset && npx prisma generate && npx prisma db seed`
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npx prisma generate && npm run build</automated>
  </verify>
  <done>
  - DealEntity junction table exists in schema
  - ClosingChecklist has fileUrl, fileName, isCustom fields
  - Asset has sourceDealId field
  - /api/deals/[id]/entities route handles GET/POST/DELETE
  - Closing templates adjusted for transactional focus
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Closing tab enhancements + deal-to-asset carryover + multi-entity UI + auto-redirect</name>
  <files>
    src/components/features/deals/deal-closing-tab.tsx,
    src/components/features/deals/close-deal-modal.tsx,
    src/components/features/deals/deal-entity-section.tsx,
    src/lib/deal-stage-engine.ts,
    src/app/api/deals/[id]/closing/route.ts,
    src/app/api/deals/[id]/route.ts,
    src/app/(gp)/deals/[id]/page.tsx
  </files>
  <action>
  **Part A: Closing tab enhancements (DEAL-02)**

  In deal-closing-tab.tsx:
  1. **Add custom item button**: "Add Custom Item" button below checklist that opens inline form (title input + Add button). POSTs to /api/deals/[id]/closing with { action: "ADD_CUSTOM", title: "..." }.
  2. **Per-item file attachment**: In expanded item section, add a file upload input (use FileUpload component pattern). Upload via FormData to the closing PATCH endpoint with the file. Store fileUrl and fileName on the ClosingChecklist record.
  3. **Show attached file**: When item has fileUrl, show filename link with download icon.

  In /api/deals/[id]/closing/route.ts:
  4. **POST handler**: Add "ADD_CUSTOM" action that creates a new ClosingChecklist item with isCustom=true, status=NOT_STARTED.
  5. **PATCH handler**: Accept file upload (FormData) for file attachment. Use Vercel Blob upload pattern (same as document upload). Save fileUrl and fileName on the item.

  **Part B: Deal-to-asset metadata carryover (DEAL-03)**

  In deal-stage-engine.ts closeDeal():
  6. **Carry over additional fields**: When creating the Asset, also populate:
     - `sourceDealId: dealId` (link back to deal)
     - `description: deal.description`
     - `counterparty: deal.counterparty` (note: need to add counterparty field to Asset if missing — check schema first. If not in schema, store in Asset metadata or skip with comment.)
     - Fetch deal.screeningResult and store AI-extracted data as JSON metadata on Asset if a metadata JSON field exists. If not, document the gap.
  7. **Return assetId** in the close response so the client can redirect.

  In deals/[id]/page.tsx handleCloseDeal():
  8. **Auto-redirect**: After successful close, use `router.push(/assets/${data.asset.id})` to redirect to the new asset page.
  9. **Show toast**: "Deal closed -- redirecting to asset..."

  **Part C: Multi-entity deal support (DEAL-04)**

  In deal-entity-section.tsx:
  10. **Rewrite to use DealEntity junction table** instead of single entityId FK:
      - Fetch linked entities via GET /api/deals/[id]/entities
      - Show all linked entities with their allocation % and entity type badge
      - "Add Entity" button: opens picker, POSTs to /api/deals/[id]/entities
      - "Remove" button per entity: DELETEs from junction table
      - Inline entity creation: same pattern as before but creates entity then adds to junction
      - Show formation status badge (FORMING, FORMED, etc.) per entity
  11. **Always visible**: Ensure DealEntitySection renders at every deal stage (it's in the overview tab already — verify it's not conditionally hidden)

  In close-deal-modal.tsx:
  12. **Pre-populate from DealEntity records**: Instead of using deal.entityId, fetch DealEntity records and pre-populate allocations from them. If allocations already set on DealEntity records, use those percentages.

  Use `const toast = useToast()` (never destructure). Use `useFirm()` for firmId. Use `parseBody(req, schema)` for API validation.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - Closing tab: "Add Custom Item" creates new checklist items
  - Closing tab: each item can have a file attached, file link shown
  - closeDeal carries AI data, description, counterparty, sourceDealId to Asset
  - After close, auto-redirect to new asset page
  - DealEntitySection shows multiple entities with allocation %
  - Entities can be added/removed at any stage
  - Formation status badge shown per entity
  - CloseDealModal pre-populates from DealEntity junction records
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Create a custom closing checklist item => appears in list with isCustom flag
- Attach file to closing item => file link visible
- Close deal => asset created with sourceDealId set, metadata carried over
- After close => auto-redirect to /assets/[newAssetId]
- Link multiple entities to deal at any stage => all visible with allocation %
- Close modal pre-populates from junction table allocations
</verification>

<success_criteria>
DEAL-02: Closing checklist fully functional with custom items, file attachments, template items focused on transactional mechanics, warn-but-allow-override on incomplete items.
DEAL-03: Deal close creates Asset with full metadata including AI data and source deal link, auto-redirect to asset page.
DEAL-04: Multi-entity deal support via junction table, entity creation at any stage, formation status visible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-02-SUMMARY.md`
</output>
