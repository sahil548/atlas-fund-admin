---
phase: 02-deal-desk-end-to-end
plan: 07
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - src/app/(gp)/assets/[id]/page.tsx
  - src/components/features/assets/asset-originated-from.tsx
  - src/app/api/assets/[id]/route.ts
  - src/app/(gp)/deals/page.tsx
  - src/app/(gp)/analytics/page.tsx
  - src/app/api/analytics/pipeline/route.ts
  - src/lib/routes.ts
autonomous: true
requirements:
  - ASSET-01
  - DEAL-10

must_haves:
  truths:
    - "Asset detail page shows 'Originated from: [Deal Name]' with clickable link"
    - "Asset detail displays all data carried from the deal (cost basis, fair value, AI-extracted metadata)"
    - "Asset type-specific tabs auto-populate from deal DD data"
    - "Deals page shows enhanced summary cards above pipeline board"
    - "Dedicated analytics page exists at /analytics with pipeline value by stage"
    - "Time-in-stage tracking shows average days per stage"
    - "Deal velocity / throughput metrics shown"
    - "Analytics page registered in routes.ts"
  artifacts:
    - path: "src/app/(gp)/assets/[id]/page.tsx"
      provides: "Enhanced asset detail with source deal link and AI intelligence"
    - path: "src/components/features/assets/asset-originated-from.tsx"
      provides: "Originated from banner component"
    - path: "src/app/(gp)/analytics/page.tsx"
      provides: "Dedicated pipeline analytics page"
    - path: "src/app/api/analytics/pipeline/route.ts"
      provides: "Pipeline analytics data API"
  key_links:
    - from: "assets/[id]/page.tsx"
      to: "/api/assets/[id] GET"
      via: "fetch asset with sourceDeal include"
      pattern: "sourceDeal"
    - from: "analytics/page.tsx"
      to: "/api/analytics/pipeline GET"
      via: "fetch pipeline metrics"
      pattern: "analytics.*pipeline"
    - from: "routes.ts"
      to: "analytics/page.tsx"
      via: "route registry entry"
      pattern: "analytics"
---

<objective>
Polish asset detail pages with source deal attribution and AI intelligence, then build pipeline analytics with enhanced summary cards and a dedicated analytics page.

Purpose: ASSET-01 depends on deal-to-asset carryover from Plan 03. Once deals create assets with metadata, asset pages need to display that data. Pipeline analytics gives the GP team visibility into deal flow health and bottlenecks.
Output: Polished asset detail with "Originated from" link and AI data, enhanced deals page summary cards, dedicated analytics page with pipeline metrics.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md
@.planning/phases/02-deal-desk-end-to-end/02-03-SUMMARY.md

<interfaces>
<!-- Dependencies from Plan 03 (deal-to-asset) -->

From prisma/schema.prisma Asset (after Plan 01):
- sourceDealId: String? -- links to source deal
- sourceDeal: Deal? @relation("AssetSourceDeal")
- Existing: name, assetClass, capitalInstrument, participationStructure, sector, costBasis, fairValue, moic, irr
- Type-specific: equityDetails, creditDetails, realEstateDetails, fundLPDetails

From src/app/api/assets/[id]/route.ts GET:
- Returns asset with includes: entityAllocations, documents, valuations, tasks, leases, creditAgreements
- Need to add: sourceDeal include

From prisma/schema.prisma Deal model:
- stage: DealStage
- createdAt, updatedAt
- activities: DealActivity[] (each has activityType, createdAt)
- Stage transitions logged as DealActivity with activityType like "STAGE_CHANGE"

From src/app/(gp)/deals/page.tsx:
- Pipeline kanban board with columns per stage
- Existing stat cards above board (deal counts, conversion rates)
- Already has Dead Deals separate view
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asset detail polish with source deal link and AI intelligence</name>
  <files>
    src/app/api/assets/[id]/route.ts,
    src/components/features/assets/asset-originated-from.tsx,
    src/app/(gp)/assets/[id]/page.tsx
  </files>
  <action>
  **A. Update GET /api/assets/[id]:**
  - Add to Prisma include: `sourceDeal: { select: { id: true, name: true, assetClass: true, stage: true, screeningResult: { select: { score: true, summary: true, strengths: true, risks: true, memo: true } }, dealMetadata: true } }`

  **B. Create AssetOriginatedFrom component** (new file):
  - Props: `{ dealId: string; dealName: string }`
  - Renders a subtle info banner: "Originated from: [Deal Name]" with a Next.js Link to `/deals/${dealId}`
  - Styled: bg-indigo-50 dark:bg-indigo-900/20, border-indigo-200, small text, arrow icon
  - Use cn() for conditional classes

  **C. Enhance asset detail page (assets/[id]/page.tsx):**

  Read the existing file first, then add:

  1. **"Originated from" banner**: If asset.sourceDeal exists, render AssetOriginatedFrom at the top (below breadcrumb, above header)

  2. **AI Deal Intelligence section**: If sourceDeal.screeningResult exists, add an expandable card section:
     - AI Score badge
     - Executive summary from memo (truncated)
     - Key strengths and risks as bullet lists
     - "View deal for full analysis" link

  3. **Type-specific tabs**: Verify existing type-specific sections render correctly when detail records exist:
     - Real Estate: propertyType, sqft, occupancy, noi, capRate
     - Credit: instrument, principal, rate, maturity, ltv
     - Equity: instrument, ownership, revenue, ebitda
     - Fund LP: gpName, commitment, calledAmount, distributions
     - If no type-specific record, show placeholder: "No type-specific details. Add via manual entry."

  4. **Layout polish**: Ensure clean dashboard feel:
     - Header: name, badges (asset class, status, instrument)
     - Key metrics row: cost basis, fair value, MOIC, IRR (use fmt/pct utils)
     - Entity allocations section
     - Documents section

  Do NOT restructure what already works well -- only add new features and polish rough edges.
  Use `const toast = useToast()`. Guard SWR data. Use existing patterns.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - GET /api/assets/[id] returns sourceDeal with screeningResult
  - "Originated from" banner shows deal name as clickable link
  - AI Deal Intelligence section visible when screening data exists
  - Type-specific sections render when detail records exist
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced deals page summary cards + dedicated analytics page</name>
  <files>
    src/app/(gp)/deals/page.tsx,
    src/app/(gp)/analytics/page.tsx,
    src/app/api/analytics/pipeline/route.ts,
    src/lib/routes.ts
  </files>
  <action>
  **A. Enhanced summary cards on deals page:**

  In src/app/(gp)/deals/page.tsx, enhance the existing stat cards above the pipeline board:
  1. **Pipeline Value by Stage**: Show total $ value (sum of targetSize) per active stage. Display as a row of cards: "Screening: $X", "DD: $Y", "IC Review: $Z", "Closing: $W"
  2. **Active / Closed / Dead counts**: Already exist -- verify they work, enhance styling if needed
  3. **Conversion rates**: Already exist with Math.min(100,...) cap -- keep as-is
  4. Add a "View Full Analytics" link/button that navigates to /analytics

  **B. Pipeline Analytics API:**

  File: src/app/api/analytics/pipeline/route.ts

  GET handler:
  - Accept firmId query param
  - Compute and return:
    1. **Pipeline value by stage**: GROUP BY stage, SUM targetSize (parse string to number), COUNT per stage
    2. **Time-in-stage tracking**: For each deal in active stages, calculate days since last stage change (from DealActivity records where activityType contains "STAGE"). Compute average days per stage.
    3. **Deal velocity**: Count deals closed per month (last 6 months). Average time from SCREENING to CLOSED (for closed deals).
    4. **Conversion rates**: Deals that moved from stage N to stage N+1 / total that entered stage N. Already computed somewhere -- reuse or recompute.
    5. **Deal throughput**: Deals entering pipeline per month (created_at), deals exiting (CLOSED or DEAD) per month.
  - Return as structured JSON with all metrics

  Handle edge cases: no deals, no activities, targetSize as string (parse with parseFloat, default to 0).

  **C. Dedicated Analytics Page:**

  File: src/app/(gp)/analytics/page.tsx

  "use client" page with:
  1. **Header**: "Pipeline Analytics" title
  2. **Summary stats row**: Total pipeline value, active deal count, average time to close, overall conversion rate (use stat-card.tsx pattern)
  3. **Pipeline Value by Stage chart**: Bar chart using Recharts showing $ value per stage
  4. **Time in Stage chart**: Bar chart showing average days per stage (identify bottlenecks)
  5. **Deal Velocity**: Line chart showing deals closed per month over last 6 months
  6. **Conversion Funnel**: Horizontal funnel/bar showing how many deals pass each stage

  Fetch data from GET /api/analytics/pipeline?firmId=...

  **D. Register analytics route:**

  In src/lib/routes.ts, add to APP_ROUTES:
  ```
  { path: "/analytics", label: "Analytics", description: "Pipeline analytics and deal metrics", keywords: ["analytics", "metrics", "pipeline", "performance", "conversion"], icon: "BarChart3", sidebarIcon: "\u25A8", portal: "gp", priority: 83 }
  ```

  Place it between Accounting (82) and Meetings (80) in priority order.

  Use Recharts for all charts (already in the project). Use `const toast = useToast()`. Use `useFirm()`. Use `useSWR` for data fetching.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - Deals page shows enhanced summary cards with pipeline value by stage
  - "View Full Analytics" link navigates to /analytics
  - Analytics API returns pipeline value, time-in-stage, velocity, conversion metrics
  - Analytics page shows charts: value by stage, time in stage, velocity, funnel
  - /analytics appears in sidebar
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Asset from closed deal shows "Originated from" banner with deal link
- AI intelligence section visible on assets with source deal data
- Deals page summary cards show pipeline value by stage
- /analytics page loads with charts
- Analytics route appears in sidebar navigation
</verification>

<success_criteria>
ASSET-01: Asset detail polished with source deal attribution, AI intelligence carry-over, type-specific content, clean layout.
Pipeline Analytics: Enhanced deals page summary + dedicated analytics page with pipeline value, time-in-stage, velocity, conversion metrics. Analytics accessible from sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-07-SUMMARY.md`
</output>
