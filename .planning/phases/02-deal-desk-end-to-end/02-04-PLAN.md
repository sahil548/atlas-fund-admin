---
phase: 02-deal-desk-end-to-end
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/features/deals/deal-overview-tab.tsx
  - src/app/api/deals/[id]/route.ts
  - src/app/api/deals/[id]/extract-metadata/route.ts
autonomous: true
requirements:
  - DEAL-06

must_haves:
  truths:
    - "Deal overview tab displays as a 4-section dashboard (header card, key metrics, IC Memo summary, deal terms)"
    - "AI-extracted metadata fields are displayed in the deal terms section"
    - "Fields are editable where appropriate (user corrects AI extractions)"
    - "IC Memo summary shows executive summary excerpt with 'Read full memo' link"
    - "Deal terms section organizes fields differently per asset class"
    - "AI auto-extraction endpoint exists and can populate dealMetadata from uploaded docs"
  artifacts:
    - path: "src/components/features/deals/deal-overview-tab.tsx"
      provides: "4-section dashboard layout for deal overview"
    - path: "src/app/api/deals/[id]/extract-metadata/route.ts"
      provides: "AI metadata extraction endpoint"
  key_links:
    - from: "deal-overview-tab.tsx"
      to: "/api/deals/[id] GET"
      via: "fetch deal with screeningResult and dealMetadata"
      pattern: "dealMetadata"
    - from: "deal-overview-tab.tsx"
      to: "/api/deals/[id]/extract-metadata POST"
      via: "trigger AI extraction from deal documents"
      pattern: "extract-metadata"
---

<objective>
Redesign the deal overview tab as a 4-section dashboard with AI-extracted metadata fields. Build the AI metadata extraction endpoint that populates deal terms from uploaded documents.

Purpose: The deal overview is the first thing a GP sees when opening a deal. Currently it's a basic form. The user wants it to feel like a "deal dashboard" with structured data, AI-populated fields, and an IC Memo summary -- not a wall of text.
Output: Dashboard-style overview with header card, key metrics, IC Memo summary, asset-class-specific deal terms, and AI extraction endpoint.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md
@.planning/phases/02-deal-desk-end-to-end/02-01-SUMMARY.md

<interfaces>
<!-- Existing overview tab and related data -->

From src/components/features/deals/deal-overview-tab.tsx:
- Currently displays deal fields in a form-like layout
- Uses InlineEditField for editable fields
- Has DealEntitySection for entity linking
- Shows AI score badge if present

From prisma/schema.prisma Deal model (after Plan 01):
- dealMetadata: Json? -- AI-extracted structured data (new)
- screeningResult: AIScreeningResult? (existing) -- has memo, summary, strengths, risks, financials
- aiScore: Int?
- assetClass, participationStructure, capitalInstrument, targetSize, targetReturn, source, counterparty

From AIScreeningResult model:
- memo: Json? -- { summary, sections[], recommendation }
- version: Int
- previousVersions: Json?
- summary, strengths, risks, financials

From src/lib/ai-service.ts:
- Existing AI service with OpenAI/Anthropic SDK
- Used for deal screening and IC memo generation
- Pattern: user-supplied API key from AIConfiguration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign deal overview tab as 4-section dashboard</name>
  <files>src/components/features/deals/deal-overview-tab.tsx</files>
  <action>
  Rewrite the deal overview tab layout as a dashboard with 4 distinct sections. Read the existing file first to understand current structure, then redesign.

  **Section 1: Header Card** (full width, top):
  - Deal name (large, bold), AI score badge (color-coded: green >70, yellow 50-70, red <50)
  - Current stage badge (color-coded per stage)
  - Asset class badge, participation structure badge, capital instrument badge
  - Deal lead name + avatar/initials
  - Deal size (targetSize field)
  - Source and counterparty if present

  **Section 2: Key Metrics** (2-3 column grid below header):
  - Target Return (from deal.targetReturn or dealMetadata)
  - Target Check Size (from deal.targetCheckSize)
  - Projected Cash Flow (from dealMetadata if extractable, else "Not available")
  - Use stat-card.tsx component if available, or create small metric cards

  **Section 3: IC Memo Summary** (below metrics, left side):
  - If screeningResult.memo exists:
    - Show executive summary (first 300 chars of memo.summary, truncated with "...")
    - "Read full memo" link that switches to IC Review tab (or scrolls/navigates)
    - Show memo version (v1, v2, etc.)
  - If no memo: Show "IC Memo not yet generated" placeholder

  **Section 4: Deal Terms** (below metrics, right side or below memo):
  - Display AI-extracted structured terms from deal.dealMetadata JSON
  - Organize differently per asset class:
    - REAL_ESTATE: Property Type, Location, Cap Rate, NOI, Square Footage
    - Credit/Debt: Principal, Interest Rate, Maturity Date, LTV, Covenants summary
    - Equity/Operating Business: Revenue, EBITDA, Ownership %, Growth Rate
    - Default/Other: Show all extracted fields as key-value pairs
  - Each field should be editable (use InlineEditField pattern or a simple edit icon that opens the field)
  - "Extract from Documents" button that calls the AI extraction endpoint (Plan 04 Task 2)
  - If dealMetadata is empty, show "No deal terms extracted. Upload documents and click 'Extract from Documents'."

  Keep the DealEntitySection below the 4 sections (it's already there and will be enhanced by Plan 03).

  Layout approach: Use Tailwind CSS grid. On large screens: 2-column layout for sections 3 and 4. On mobile: stack vertically.

  Use `const toast = useToast()` (never destructure). Guard all data access with `if (!deal)` checks. Follow SWR patterns.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - Overview tab displays 4-section dashboard layout
  - Header card shows deal name, AI score, stage, badges
  - Key metrics section shows target return, check size
  - IC Memo summary shows excerpt with "Read full memo" link
  - Deal terms section organizes by asset class
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: AI metadata extraction endpoint</name>
  <files>
    src/app/api/deals/[id]/extract-metadata/route.ts,
    src/app/api/deals/[id]/route.ts
  </files>
  <action>
  **A. Create POST /api/deals/[id]/extract-metadata/route.ts:**

  This endpoint triggers AI extraction of structured deal metadata from uploaded documents.

  1. Extract dealId from params (await params)
  2. Fetch the deal with its documents (include documents with extractedText)
  3. If no documents have extractedText, return 400: "No document text available for extraction"
  4. Build a prompt for the AI:
     - System: "Extract structured deal metadata from the following document text. Return JSON with fields: dealSize, targetReturn, projectedCashFlow, investmentStructure, keyTerms, and asset-class-specific fields based on the deal's asset class: {deal.assetClass}"
     - Include extractedText from all deal documents (concatenate, truncate to reasonable limit)
  5. Call the AI service (follow existing pattern from ai-service.ts -- check how screening/DD analysis calls work)
  6. Parse the AI response as JSON
  7. Update deal.dealMetadata with the extracted data: `prisma.deal.update({ where: { id: dealId }, data: { dealMetadata: extractedJson } })`
  8. Return 200 with the extracted metadata

  Handle errors gracefully: if AI call fails, return 500 with message. Use 90-second timeout pattern (Promise.race) established in Phase 1.

  **B. Update GET /api/deals/[id]/route.ts:**
  - Ensure the GET response includes dealMetadata field (it should already since it's a direct Deal field, but verify the select/include doesn't exclude it)

  Use `import { prisma } from "@/lib/prisma"`. Use `await params` for route params.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - POST /api/deals/[id]/extract-metadata endpoint exists
  - Extracts structured metadata from deal documents via AI
  - Saves extracted data to deal.dealMetadata
  - GET /api/deals/[id] returns dealMetadata
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Deal overview tab shows 4-section dashboard layout
- Header card displays deal identity, badges, AI score
- IC Memo summary section shows memo excerpt when available
- Deal terms section shows "Extract from Documents" button
- Extract metadata API returns structured JSON from deal documents
</verification>

<success_criteria>
Deal overview feels like a "deal dashboard" -- not a form, not a wall of text. AI-extracted metadata populates deal terms. IC Memo summary is accessible. Asset-class-specific field organization works for RE, Credit, and Equity deals.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-04-SUMMARY.md`
</output>
