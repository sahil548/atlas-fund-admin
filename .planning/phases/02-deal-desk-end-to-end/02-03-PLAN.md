---
phase: 02-deal-desk-end-to-end
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/components/features/deals/deal-closing-tab.tsx
  - src/components/features/deals/close-deal-modal.tsx
  - src/components/features/deals/deal-entity-section.tsx
  - src/lib/deal-stage-engine.ts
  - src/app/api/deals/[id]/closing/route.ts
  - src/app/(gp)/deals/[id]/page.tsx
autonomous: true
requirements:
  - DEAL-02
  - DEAL-03
  - DEAL-04

must_haves:
  truths:
    - "Closing checklist supports custom items (GP can add beyond template)"
    - "Closing checklist items support per-item file attachments"
    - "Closing warns on incomplete items but allows override (does not block)"
    - "Deal close creates Asset with all metadata carried over (AI-extracted data, linked docs, counterparty, sourceDealId)"
    - "After close, user is auto-redirected to the new asset page"
    - "Entity creation available at any deal stage (DealEntitySection always visible)"
    - "Deal can be linked to multiple entities via junction table"
    - "Multi-entity allocation % visible on deal detail (not just in close modal)"
    - "Close modal pre-populates allocations from DealEntity junction records"
  artifacts:
    - path: "src/components/features/deals/deal-closing-tab.tsx"
      provides: "Enhanced closing tab with custom items and file attachments"
    - path: "src/components/features/deals/deal-entity-section.tsx"
      provides: "Multi-entity support via DealEntity junction table"
    - path: "src/lib/deal-stage-engine.ts"
      provides: "closeDeal with full metadata carryover and sourceDealId"
  key_links:
    - from: "deal-closing-tab.tsx"
      to: "/api/deals/[id]/closing POST/PATCH"
      via: "create custom items, update with file attachments"
      pattern: "closing"
    - from: "deal-stage-engine.ts closeDeal"
      to: "prisma.asset.create"
      via: "full metadata carryover including AI data and sourceDealId"
      pattern: "sourceDealId"
    - from: "deal-entity-section.tsx"
      to: "/api/deals/[id]/entities"
      via: "junction table CRUD"
      pattern: "dealEntit"
---

<objective>
Complete the closing workflow with checklist enhancements, implement deal-to-asset transition with full metadata carryover, and enable multi-entity deal participation with entity formation at any stage.

Purpose: DEAL-02 (closing workflow), DEAL-03 (deal-to-asset), and DEAL-04 (entity formation) form the core GP daily workflow. Closing a deal and getting a properly-formed asset is the primary value proposition.
Output: Enhanced closing checklist with attachments and custom items, seamless deal-to-asset transition with auto-redirect, multi-entity deal support with junction table.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md
@.planning/phases/02-deal-desk-end-to-end/02-01-SUMMARY.md

<interfaces>
<!-- After Plan 01 schema changes -->

From prisma/schema.prisma (after Plan 01):
- DealEntity: { id, dealId, entityId, allocationPercent?, role?, createdAt }
- ClosingChecklist now has: fileUrl?, fileName?, isCustom
- Asset now has: sourceDealId?
- Deal now has: dealMetadata? (JSON)

From src/lib/deal-stage-engine.ts closeDeal():
- Accepts dealId, opts: { costBasis, fairValue?, entryDate?, force?, allocations: CloseAllocation[] }
- Creates Asset with: name, assetClass, capitalInstrument, participationStructure, sector, costBasis, fairValue, entryDate
- Creates AssetEntityAllocation per allocation
- Links deal documents to asset via prisma.document.updateMany
- Does NOT carry over: screeningResult data, counterparty, gpName, description, thesisNotes, investmentRationale, dealMetadata

From src/components/features/deals/deal-closing-tab.tsx:
- Fetches closing checklist via GET /api/deals/[id]/closing
- Initialize via POST with { action: "INITIALIZE" }
- Update via PATCH with { id, status/assigneeId/dueDate/notes }
- No custom item creation, no file attachments

From src/components/features/deals/deal-entity-section.tsx:
- Uses deal.entityId (single FK) to show one linked entity
- link/unlink/create+link via PUT /api/deals/[id] with { entityId }

From /api/deals/[id]/entities (created in Plan 01):
- GET: returns DealEntity[] with entity details
- POST: creates DealEntity record
- DELETE: removes DealEntity record
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Closing tab enhancements with custom items and file attachments</name>
  <files>
    src/components/features/deals/deal-closing-tab.tsx,
    src/app/api/deals/[id]/closing/route.ts
  </files>
  <action>
  **A. Closing tab UI enhancements (deal-closing-tab.tsx):**

  1. **Add custom item button**: "Add Item" button below checklist. Opens an inline form: title input + "Add" button. On submit, POSTs to /api/deals/[id]/closing with { action: "ADD_CUSTOM", title: "..." }. After success, mutate SWR key to refresh list.

  2. **Per-item file attachment**: In each checklist item's expanded/detail section, add a file upload area. Use the FileUpload component pattern (FormData to API). Upload via PATCH to /api/deals/[id]/closing with { id: itemId, action: "ATTACH_FILE" } and FormData containing the file.

  3. **Show attached file**: When item has fileUrl, show filename as a clickable link with download icon. Show a small "Remove" button to clear the attachment.

  **B. Closing API route enhancements (/api/deals/[id]/closing/route.ts):**

  4. **POST handler additions**:
     - Add "ADD_CUSTOM" action that creates a new ClosingChecklist record with isCustom=true, status=NOT_STARTED, order = max existing order + 1

  5. **PATCH handler additions**:
     - Add "ATTACH_FILE" action: accept FormData with file. Upload to Vercel Blob (same pattern as document upload). Save fileUrl and fileName on the ClosingChecklist record.
     - Add "REMOVE_FILE" action: set fileUrl and fileName to null on the item

  Use `const toast = useToast()` (never destructure). Use existing FileUpload pattern for the upload UI.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - "Add Item" button creates custom checklist items
  - Each item can have a file attached via upload
  - Attached file shows as clickable link
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Deal-to-asset carryover + multi-entity UI + close modal + auto-redirect</name>
  <files>
    src/lib/deal-stage-engine.ts,
    src/components/features/deals/deal-entity-section.tsx,
    src/components/features/deals/close-deal-modal.tsx,
    src/app/(gp)/deals/[id]/page.tsx
  </files>
  <action>
  **A. Deal-to-asset metadata carryover (DEAL-03):**

  In deal-stage-engine.ts closeDeal():
  1. When creating the Asset, also set:
     - `sourceDealId: dealId`
     - `description: deal.description` (if Asset model has this field -- check first; if not, skip)
  2. Fetch deal.screeningResult and deal.dealMetadata. If the Asset model has no JSON metadata field, store key extracted data in notes or log the gap.
  3. Return the created asset's ID in the response so the client can redirect: `return { ...deal, asset: { id: newAsset.id } }`

  **B. Multi-entity deal support (DEAL-04):**

  In deal-entity-section.tsx:
  4. **Rewrite to use DealEntity junction table** instead of single entityId FK:
     - Fetch linked entities via GET /api/deals/[id]/entities (created in Plan 01)
     - Show all linked entities in a list with: entity name, type badge, formation status badge, allocation % (if set)
     - "Add Entity" button: opens a picker (select from useSWR entities list), POSTs to /api/deals/[id]/entities
     - "Remove" icon button per entity: DELETEs from junction table
     - Inline entity creation: same pattern as before (create entity via API, then add to junction)
  5. **Always visible**: Ensure DealEntitySection renders at every deal stage (verify it's not conditionally hidden behind a stage check)

  **C. Close modal pre-population:**

  In close-deal-modal.tsx:
  6. Instead of using deal.entityId to pre-populate, fetch DealEntity records via GET /api/deals/[id]/entities. Pre-populate the allocations array from DealEntity records with their allocationPercent values.

  **D. Auto-redirect after close:**

  In deals/[id]/page.tsx:
  7. After successful close response, use `router.push(\`/assets/\${data.asset.id}\`)` to redirect to the new asset page.
  8. Show toast: "Deal closed -- redirecting to asset..."

  Use `const toast = useToast()` (never destructure). Use `useFirm()` for firmId. Use `parseBody(req, schema)` for API validation.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - closeDeal carries sourceDealId to Asset
  - After close, auto-redirect to new asset page
  - DealEntitySection shows multiple entities with allocation %
  - Entities can be added/removed at any stage
  - Formation status badge shown per entity
  - CloseDealModal pre-populates from DealEntity junction records
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Create custom closing checklist item => appears in list
- Attach file to closing item => file link visible
- Close deal => asset created with sourceDealId set
- After close => auto-redirect to /assets/[newAssetId]
- Link multiple entities to deal at any stage => all visible with allocation %
- Close modal pre-populates from junction table allocations
</verification>

<success_criteria>
DEAL-02: Closing checklist fully functional with custom items, file attachments, template items focused on transactional mechanics.
DEAL-03: Deal close creates Asset with sourceDealId, auto-redirect to asset page.
DEAL-04: Multi-entity deal support via junction table, entity creation at any stage, formation status visible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-03-SUMMARY.md`
</output>
