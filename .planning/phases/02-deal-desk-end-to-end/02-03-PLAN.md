---
phase: 02-deal-desk-end-to-end
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/app/(gp)/assets/[id]/page.tsx
  - src/components/features/assets/asset-detail-tabs.tsx
  - src/components/features/assets/asset-originated-from.tsx
  - src/app/api/assets/[id]/route.ts
autonomous: true
requirements:
  - ASSET-01

must_haves:
  truths:
    - "Asset detail page shows 'Originated from: [Deal Name]' with clickable link back to deal"
    - "Asset detail displays all data carried from the deal (cost basis, fair value, AI-extracted metadata, documents)"
    - "Asset type-specific tabs auto-populate from deal DD analysis data (RE, Credit, Equity, Fund LP)"
    - "Asset detail page is polished and usable for GP daily work"
  artifacts:
    - path: "src/app/(gp)/assets/[id]/page.tsx"
      provides: "Enhanced asset detail page with source deal link and type-specific content"
    - path: "src/components/features/assets/asset-originated-from.tsx"
      provides: "Originated from banner component linking back to source deal"
  key_links:
    - from: "assets/[id]/page.tsx"
      to: "/api/assets/[id] GET"
      via: "fetch asset with sourceDeal include"
      pattern: "sourceDeal"
    - from: "asset-originated-from.tsx"
      to: "/deals/[dealId]"
      via: "Link component to source deal"
      pattern: "deals.*sourceDealId"
---

<objective>
Polish asset detail pages after deal-to-asset flow works, including source deal attribution, type-specific tabs populated from DD data, and a clean layout for daily GP use.

Purpose: ASSET-01 depends on DEAL-03 (deal-to-asset carryover) from Plan 02. Once deals properly create assets with metadata, the asset pages need to display that data effectively.
Output: Polished asset detail with "Originated from" link, type-specific tabs, and carried-over deal intelligence.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md

<interfaces>
<!-- From Plan 02 (dependency) — types that will exist after Plan 02 completes -->

Asset model (after Plan 02):
- sourceDealId: String? — links to source deal
- sourceDeal: Deal? — relation to source deal (includes screeningResult, workstreams, documents)
- Existing: name, assetClass, capitalInstrument, participationStructure, sector, costBasis, fairValue, entryDate
- Type-specific one-to-one relations: equityDetails, creditDetails, realEstateDetails, fundLPDetails

From src/app/api/assets/[id]/route.ts GET:
- Returns asset with includes: entityAllocations, documents, valuations, tasks, leases, creditAgreements
- Need to add: sourceDeal include (with screeningResult for AI data)

From prisma/schema.prisma Asset type-specific models:
- AssetEquityDetails: instrument, ownership, revenue, ebitda (currently seeded data)
- AssetCreditDetails: instrument, principal, rate, maturity, ltv
- AssetRealEstateDetails: propertyType, sqft, occupancy, noi, capRate
- AssetFundLPDetails: gpName, commitment, calledAmount, distributions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asset API enhancement and originated-from component</name>
  <files>
    src/app/api/assets/[id]/route.ts,
    src/components/features/assets/asset-originated-from.tsx
  </files>
  <action>
  1. **Update GET /api/assets/[id]** to include the sourceDeal relation:
     - Add to the Prisma include: `sourceDeal: { select: { id: true, name: true, assetClass: true, stage: true, screeningResult: true } }`
     - This gives the asset page access to the deal name (for the link) and AI screening data

  2. **Create AssetOriginatedFrom component** (new file):
     - Props: `{ dealId: string; dealName: string }`
     - Renders a banner/card: "Originated from: [Deal Name]" with a Next.js Link to `/deals/${dealId}`
     - Styled as a subtle info banner (bg-indigo-50 border-indigo-200, small text)
     - Include a small arrow/link icon

  This component will be consumed by the asset detail page in Task 2.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - GET /api/assets/[id] returns sourceDeal with id, name, screeningResult
  - AssetOriginatedFrom component created with proper Link to source deal
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish asset detail page with source deal link and type-specific content</name>
  <files>
    src/app/(gp)/assets/[id]/page.tsx
  </files>
  <action>
  Read the existing asset detail page first, then enhance:

  1. **Add "Originated from" banner**: If asset.sourceDeal exists, render AssetOriginatedFrom at the top of the page (below breadcrumb, above header). Shows deal name as clickable link.

  2. **Display carried-over deal data**: In the overview section, show:
     - Cost basis, fair value, entry date (already likely shown)
     - Sector, participation structure, capital instrument (badges)
     - If sourceDeal.screeningResult exists, show an "AI Deal Intelligence" expandable section with:
       - AI score (if available)
       - IC Memo executive summary (truncated with "Read full memo on deal page" link)
       - Key strengths and risks from screeningResult

  3. **Type-specific tabs**: Check if the asset detail page already has type-specific sections. If so, ensure they render when the one-to-one detail records exist:
     - Real Estate: show propertyType, sqft, occupancy, noi, capRate from AssetRealEstateDetails
     - Credit: show instrument, principal, rate, maturity, ltv from AssetCreditDetails
     - Equity: show instrument, ownership, revenue, ebitda from AssetEquityDetails
     - Fund LP: show gpName, commitment, calledAmount, distributions from AssetFundLPDetails
     - If no type-specific record exists (common for deal-created assets where these were seeded), show "No details available — add via valuation or manual entry"

  4. **Polish layout**: Ensure the page has a clean, dashboard-like feel:
     - Header with name, badges (asset class, status, instrument)
     - Key metrics row (cost basis, fair value, MOIC, IRR if available)
     - Entity allocations section
     - Documents section
     - Type-specific section

  Follow all existing patterns: `useSWR`, `useFirm()`, `const toast = useToast()`, loading guards.
  Do NOT restructure what already works well — only add the new features and polish rough edges.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - Asset detail shows "Originated from: [Deal Name]" link when sourceDealId exists
  - AI Deal Intelligence section shows screening data from source deal
  - Type-specific sections render when detail records exist
  - Clean, polished layout suitable for daily GP use
  - Build passes with zero errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- Asset created from deal close (Plan 02) shows "Originated from" link
- Clicking the link navigates back to the source deal
- AI data from deal screening visible on asset page
- Type-specific tabs display data when type detail records exist
</verification>

<success_criteria>
ASSET-01: Asset detail pages are polished with source deal attribution, AI intelligence carry-over, type-specific content display, and clean layout for GP daily use.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-03-SUMMARY.md`
</output>
