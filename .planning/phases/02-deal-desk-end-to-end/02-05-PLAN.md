---
phase: 02-deal-desk-end-to-end
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - src/components/features/deals/deal-dd-tab.tsx
  - src/app/api/deals/[id]/workstreams/[workstreamId]/route.ts
  - src/app/api/deals/[id]/workstreams/[workstreamId]/comments/route.ts
  - src/app/api/deals/[id]/workstreams/[workstreamId]/attachments/route.ts
  - src/components/features/deals/workstream-detail-panel.tsx
autonomous: true
requirements:
  - DEAL-07

must_haves:
  truths:
    - "DD workstream items display as PM-style tasks with assignee, status, priority, due date"
    - "Each workstream supports threaded comments (GP adds findings, back-and-forth)"
    - "Each workstream supports file attachments"
    - "Workstream status: Open -> In Progress -> Resolved"
    - "Re-analyze button triggers per-workstream AI analysis"
    - "IC Memo auto-updates (new version) when workstream is re-analyzed"
    - "Display is list view (not kanban/board)"
  artifacts:
    - path: "src/components/features/deals/deal-dd-tab.tsx"
      provides: "Redesigned DD tab with PM-style workstream list"
    - path: "src/components/features/deals/workstream-detail-panel.tsx"
      provides: "Expandable detail panel for a single workstream with comments and attachments"
    - path: "src/app/api/deals/[id]/workstreams/[workstreamId]/comments/route.ts"
      provides: "Threaded comments API for workstreams"
    - path: "src/app/api/deals/[id]/workstreams/[workstreamId]/attachments/route.ts"
      provides: "File attachment API for workstreams"
  key_links:
    - from: "deal-dd-tab.tsx"
      to: "/api/deals/[id]/workstreams GET"
      via: "fetch workstreams with comments and attachments"
      pattern: "workstreams"
    - from: "workstream-detail-panel.tsx"
      to: "/api/deals/[id]/workstreams/[id]/comments"
      via: "CRUD for threaded comments"
      pattern: "comments"
    - from: "deal-dd-tab.tsx"
      to: "/api/deals/[id]/workstreams/[id] PATCH"
      via: "update status, assignee, priority, due date"
      pattern: "workstream.*update"
---

<objective>
Redesign the DD workstreams tab from a basic status list into an interactive PM-style task management interface with assignees, statuses, threaded comments, file attachments, and per-workstream re-analysis with IC Memo auto-versioning.

Purpose: DD workstreams are where the GP team spends most of their time during deal evaluation. The user wants this to feel like Asana/Notion/Monday -- task-based with real collaboration features, not just a list of items with checkboxes.
Output: PM-style workstream list with detail panels, comment threads, file attachments, and re-analysis triggers.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-desk-end-to-end/02-CONTEXT.md
@.planning/phases/02-deal-desk-end-to-end/02-01-SUMMARY.md

<interfaces>
<!-- After Plan 01 schema changes -->

From prisma/schema.prisma DDWorkstream (after Plan 01):
- Existing: id, dealId, name, status (DDWorkstreamStatus), totalTasks, completedTasks, hasAI, description, customInstructions, aiGenerated, sortOrder, analysisType, analysisResult (Json), createdAt, updatedAt
- New: assigneeId (String?), priority (String? default "MEDIUM"), dueDate (DateTime?)
- New relation: comments (DDWorkstreamComment[]), attachments (DDWorkstreamAttachment[])

From prisma/schema.prisma DDWorkstreamComment (new, Plan 01):
- id, workstreamId, authorId, content, parentId (for threading), createdAt, updatedAt

From prisma/schema.prisma DDWorkstreamAttachment (new, Plan 01):
- id, workstreamId, fileName, fileUrl, fileSize, uploadedById, createdAt

From DDWorkstreamStatus enum:
- NOT_STARTED, IN_PROGRESS, COMPLETE

From src/components/features/deals/deal-dd-tab.tsx (current):
- Displays workstreams as expandable cards with status, AI analysis results
- Has "Analyze" button per workstream
- Shows overall DD progress %

From src/app/api/deals/[id]/workstreams/ (existing routes):
- Read existing routes to understand current patterns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workstream API enhancements (update, comments, attachments)</name>
  <files>
    src/app/api/deals/[id]/workstreams/[workstreamId]/route.ts,
    src/app/api/deals/[id]/workstreams/[workstreamId]/comments/route.ts,
    src/app/api/deals/[id]/workstreams/[workstreamId]/attachments/route.ts
  </files>
  <action>
  **A. Workstream PATCH endpoint** (create or update existing route):

  File: src/app/api/deals/[id]/workstreams/[workstreamId]/route.ts

  PATCH handler:
  - Accept JSON body with optional fields: { status, assigneeId, priority, dueDate }
  - Status values: "NOT_STARTED", "IN_PROGRESS", "COMPLETE" (map to DDWorkstreamStatus enum)
  - Validate workstream belongs to the deal
  - Update the DDWorkstream record
  - Return updated workstream

  GET handler (if not already present):
  - Return workstream with includes: { comments: { include: { author: true, replies: { include: { author: true } } }, where: { parentId: null }, orderBy: { createdAt: "asc" } }, attachments: true, assignee: true }

  **B. Comments API:**

  File: src/app/api/deals/[id]/workstreams/[workstreamId]/comments/route.ts

  GET handler:
  - Return all root comments (parentId is null) for the workstream with nested replies and author info
  - Include: author (name, initials), replies with their authors
  - Order by createdAt asc

  POST handler:
  - Accept { content: string, parentId?: string }
  - Get current user ID from auth context (check existing patterns for how other API routes identify the user -- likely from query params or session)
  - Create DDWorkstreamComment record
  - Return 201 with the created comment including author

  **C. Attachments API:**

  File: src/app/api/deals/[id]/workstreams/[workstreamId]/attachments/route.ts

  GET handler:
  - Return all DDWorkstreamAttachment records for the workstream

  POST handler:
  - Accept FormData with file
  - Upload to Vercel Blob (same pattern as document upload)
  - Create DDWorkstreamAttachment record with fileName, fileUrl, fileSize
  - Return 201 with the created attachment

  DELETE handler:
  - Accept attachment ID in query params
  - Delete the DDWorkstreamAttachment record
  - Return 200

  Use `parseBody(req, schema)` for JSON endpoints. Use `await params` for all route params.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - PATCH workstream updates status, assignee, priority, due date
  - Comments API: GET returns threaded comments, POST creates new comments
  - Attachments API: GET returns list, POST uploads file, DELETE removes
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign DD tab as PM-style task list with detail panels</name>
  <files>
    src/components/features/deals/deal-dd-tab.tsx,
    src/components/features/deals/workstream-detail-panel.tsx
  </files>
  <action>
  **A. Redesign deal-dd-tab.tsx as list view:**

  Read the existing file first. Then redesign the layout:

  1. **Overall progress header**: Keep the existing progress bar showing DD completion %. Add counts: "X of Y workstreams complete".

  2. **Workstream list (not kanban)**: Display each workstream as a row in a list:
     - Left: Status indicator (colored dot: gray=NOT_STARTED, blue=IN_PROGRESS, green=COMPLETE)
     - Name (clickable to expand detail panel)
     - Assignee avatar/initials (click to change -- dropdown of team members)
     - Priority badge (High=red, Medium=yellow, Low=green)
     - Due date (if set, show date; if overdue, show in red)
     - Comment count badge (shows thread count)
     - Attachment count badge (shows file count)
     - Status dropdown (NOT_STARTED / IN_PROGRESS / COMPLETE)
     - "Re-analyze" button (only shown if workstream has AI capability, i.e., hasAI=true)

  3. **Quick-update inline**: Clicking assignee/priority/due date opens inline editor. Changes PATCH to /api/deals/[id]/workstreams/[workstreamId]. Status dropdown changes trigger immediate PATCH.

  4. **Re-analyze behavior**: When "Re-analyze" is clicked:
     - Call the existing AI analysis endpoint for that workstream
     - Show loading indicator on that workstream row
     - After analysis completes, refresh the workstream data
     - Then trigger IC Memo regeneration (if memo exists): call the existing memo generation endpoint
     - The memo generation creates a new version (increments version, saves old to previousVersions)
     - Show toast: "Workstream re-analyzed. IC Memo updated to v{N}."

  **B. Create WorkstreamDetailPanel (new component):**

  File: src/components/features/deals/workstream-detail-panel.tsx

  An expandable panel (or slide-over) that shows when a workstream row is clicked:

  1. **Header**: Workstream name, status, assignee, priority, due date (all editable)
  2. **AI Analysis section**: If analysisResult exists, show it (reuse existing rendering logic from current DD tab)
  3. **Comments section**: Threaded comments list. Each comment shows: author initials, content, timestamp. Reply button that opens inline reply form. Post new comment form at bottom.
  4. **Attachments section**: List of attached files with download links. Upload button to add new attachment. Delete button per attachment.

  Fetch data: GET /api/deals/[id]/workstreams/[workstreamId] for full workstream with comments and attachments.

  Use `const toast = useToast()` (never destructure). Use `useFirm()` for firmId. Guard SWR data with loading checks.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Investor Portal Claude App/atlas" && npm run build</automated>
  </verify>
  <done>
  - DD tab shows workstreams as PM-style list with assignee, status, priority, due date
  - Clicking a workstream opens detail panel with comments and attachments
  - Inline editing of status, assignee, priority works
  - Re-analyze button triggers AI analysis and IC Memo version bump
  - Comments are threaded with reply support
  - File attachments can be uploaded and downloaded
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- DD tab displays workstreams as list with PM-style metadata (assignee, priority, due date, status)
- Click workstream => detail panel with comments and attachments
- Change status inline => PATCH saves, UI updates
- Post a comment => appears in thread
- Upload attachment => file link appears
- Re-analyze workstream => AI runs, memo version increments
</verification>

<success_criteria>
DD workstreams feel like Asana/Notion/Monday -- task-based with assignees, statuses, comments, attachments. Not just a list of items with checkboxes. Re-analysis triggers IC Memo versioning.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-desk-end-to-end/02-05-SUMMARY.md`
</output>
