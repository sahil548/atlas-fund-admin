---
phase: 04-asset-entity-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pagination.ts
  - src/lib/schemas.ts
  - src/components/ui/load-more-button.tsx
  - src/components/ui/search-filter-bar.tsx
  - src/components/ui/error-boundary.tsx
  - src/app/api/deals/route.ts
  - src/app/api/assets/route.ts
  - src/app/api/entities/route.ts
  - src/app/api/investors/route.ts
  - src/app/api/documents/route.ts
  - src/app/api/tasks/route.ts
  - src/app/api/transactions/route.ts
  - src/app/api/meetings/route.ts
  - src/app/(gp)/deals/page.tsx
  - src/app/(gp)/assets/page.tsx
  - src/app/(gp)/entities/page.tsx
  - src/app/(gp)/directory/page.tsx
  - src/app/(gp)/documents/page.tsx
  - src/app/(gp)/tasks/page.tsx
  - src/app/(gp)/transactions/page.tsx
  - src/app/(gp)/meetings/page.tsx
  - src/app/layout.tsx
  - src/app/api/ai/agents/route.ts
  - src/app/api/ai/search/route.ts
  - src/lib/rate-limit.ts
autonomous: true
requirements: [CORE-04, CORE-05, CORE-06]

must_haves:
  truths:
    - "All data lists support cursor-based pagination with Load More button (default 50 records)"
    - "All data lists have a search bar and relevant filters"
    - "Page-level error boundary catches unhandled errors and shows retry button"
    - "Section-level error boundaries wrap charts, computation displays, AI content, and financial tables"
    - "Loading states show spinner + descriptive text (not skeleton loaders)"
    - "Empty states show helpful message with action prompt"
    - "AI endpoints return 429 with Retry-After header when rate limit exceeded"
    - "Per-user rate limit shows 'Slow down' message in UI"
  artifacts:
    - path: "src/lib/pagination.ts"
      provides: "Cursor-based pagination helper for API routes"
      exports: ["paginatedQuery", "PaginationParams"]
    - path: "src/components/ui/load-more-button.tsx"
      provides: "Reusable Load More button component for paginated lists"
    - path: "src/components/ui/search-filter-bar.tsx"
      provides: "Reusable search + filter bar component"
    - path: "src/components/ui/error-boundary.tsx"
      provides: "Page-level and section-level error boundary components"
      exports: ["PageErrorBoundary", "SectionErrorBoundary"]
    - path: "src/lib/rate-limit.ts"
      provides: "In-memory rate limiter for AI endpoints"
      exports: ["rateLimit", "RateLimitConfig"]
  key_links:
    - from: "src/app/api/deals/route.ts"
      to: "src/lib/pagination.ts"
      via: "paginatedQuery for cursor-based results"
      pattern: "paginatedQuery"
    - from: "src/app/(gp)/deals/page.tsx"
      to: "src/components/ui/load-more-button.tsx"
      via: "LoadMore component at bottom of list"
      pattern: "LoadMoreButton"
    - from: "src/app/layout.tsx"
      to: "src/components/ui/error-boundary.tsx"
      via: "PageErrorBoundary wrapping children"
      pattern: "PageErrorBoundary"
    - from: "src/app/api/ai/agents/route.ts"
      to: "src/lib/rate-limit.ts"
      via: "rateLimit check at top of handler"
      pattern: "rateLimit"
---

<objective>
Add cursor-based pagination to all data lists, error boundaries at page and section levels, helpful loading/empty states, and rate limiting on AI endpoints. This is infrastructure hardening that makes Atlas reliable for daily GP use.

Purpose: Without pagination, pages load all records at once (fine for 9 entities, bad for 500 transactions). Without error boundaries, one broken component crashes the entire page. Without rate limiting, a single user can exhaust AI API budgets.

Output: Pagination system, error boundaries, rate limiter, search/filter bars on all list pages.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-asset-entity-polish/04-CONTEXT.md

<interfaces>
<!-- Existing SWR pattern used on all list pages -->
```typescript
// All list pages follow this pattern:
const { firmId } = useFirm();
const { data, isLoading } = useSWR(`/api/deals?firmId=${firmId}`, fetcher);
if (isLoading || !data) return <div className="text-sm text-gray-400">Loading...</div>;
```

<!-- Existing API pattern -->
```typescript
// All list APIs follow this pattern:
export async function GET(req: Request) {
  const { searchParams } = new URL(req.url);
  const firmId = searchParams.get("firmId");
  const items = await prisma.deal.findMany({ where: { firmId } });
  return NextResponse.json(items);
}
```

<!-- Filter specs from user decisions -->
Deals: by stage, asset class, deal lead, date range
Assets: by asset class, entity, status, performance range
Entities: by type, status, vintage
Investors: by entity, commitment range
Transactions: by type, date range, entity
Others: Claude's discretion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pagination infrastructure + Error boundaries + Rate limiting</name>
  <files>
    src/lib/pagination.ts,
    src/lib/schemas.ts,
    src/lib/rate-limit.ts,
    src/components/ui/load-more-button.tsx,
    src/components/ui/search-filter-bar.tsx,
    src/components/ui/error-boundary.tsx,
    src/hooks/use-paginated-list.ts,
    src/app/layout.tsx,
    src/app/api/ai/agents/route.ts,
    src/app/api/ai/search/route.ts
  </files>
  <action>
    1. Create src/lib/pagination.ts:
       - PaginationParams type: { cursor?: string, limit?: number, search?: string, filters?: Record<string, string> }
       - PaginatedResult<T> type: { data: T[], nextCursor: string | null, hasMore: boolean, total: number }
       - parsePaginationParams(searchParams: URLSearchParams): PaginationParams — extracts cursor, limit (default 50), search, and dynamic filter keys
       - buildPrismaArgs(params: PaginationParams, searchFields?: string[]): object — builds Prisma where/take/skip/cursor args
         - search: builds OR condition across searchFields using `contains` (case-insensitive)
         - cursor: uses Prisma cursor pagination (take: limit+1, cursor: { id: cursor }, skip: 1 if cursor present)
         - Returns { where, take, skip, cursor, orderBy }
    2. Add PaginationQuerySchema to schemas.ts: z.object({ cursor: z.string().optional(), limit: z.coerce.number().min(1).max(100).default(50), search: z.string().optional() })
    3. Create src/components/ui/load-more-button.tsx:
       - Props: { hasMore: boolean, loading: boolean, onLoadMore: () => void }
       - Shows "Load More" button when hasMore is true, spinner when loading
       - Tailwind styling consistent with existing UI (rounded-lg, text-sm, border)
    4. Create src/components/ui/search-filter-bar.tsx:
       - Props: { onSearch: (query: string) => void, filters: FilterConfig[], onFilterChange: (key: string, value: string) => void, activeFilters: Record<string, string> }
       - FilterConfig: { key: string, label: string, options: { value: string, label: string }[] }
       - Renders search input + filter dropdowns in a horizontal bar
       - Debounced search (300ms) using useState + useEffect
    5. Create src/hooks/use-paginated-list.ts:
       - Custom hook wrapping SWR for paginated data
       - usePaginatedList(baseUrl, params): { data: T[], isLoading, hasMore, loadMore, search, setSearch, filters, setFilter, total }
       - Manages cursor progression, appends new data to existing array
       - Resets data when search/filters change
    6. Create src/components/ui/error-boundary.tsx:
       - PageErrorBoundary: class component wrapping children, catches errors, shows "Something went wrong" with Retry button and error message
       - SectionErrorBoundary: same but smaller styling, for wrapping individual sections
       - Both use React ErrorBoundary pattern (componentDidCatch, getDerivedStateFromError)
       - Retry resets error state and re-renders children
    7. Update src/app/layout.tsx:
       - Wrap the main content area (children) with PageErrorBoundary
       - Do NOT wrap the entire layout (sidebar should remain visible on error)
    8. Create src/lib/rate-limit.ts:
       - In-memory rate limiter using Map<string, { count: number, resetAt: number }>
       - rateLimit(userId: string, config: { maxRequests: number, windowMs: number }): { allowed: boolean, remaining: number, retryAfter?: number }
       - Default config: 20 requests/minute per user, 200 requests/hour per user
       - Clean up expired entries on each call (simple GC)
    9. Update /api/ai/agents/route.ts and /api/ai/search/route.ts:
       - Add rate limit check at top of POST handler: if (!allowed) return 429 with Retry-After and X-RateLimit-Remaining headers
       - Get userId from getAuthUser()
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pagination helper, error boundaries (page + section), rate limiter, search/filter bar, load more button, and paginated list hook all exist and build passes. AI endpoints return 429 when rate limited.</done>
</task>

<task type="auto">
  <name>Task 2: Apply pagination + search/filters to all 8 list pages</name>
  <files>
    src/app/api/deals/route.ts,
    src/app/api/assets/route.ts,
    src/app/api/entities/route.ts,
    src/app/api/investors/route.ts,
    src/app/api/documents/route.ts,
    src/app/api/tasks/route.ts,
    src/app/api/transactions/route.ts,
    src/app/api/meetings/route.ts,
    src/app/(gp)/deals/page.tsx,
    src/app/(gp)/assets/page.tsx,
    src/app/(gp)/entities/page.tsx,
    src/app/(gp)/directory/page.tsx,
    src/app/(gp)/documents/page.tsx,
    src/app/(gp)/tasks/page.tsx,
    src/app/(gp)/transactions/page.tsx,
    src/app/(gp)/meetings/page.tsx
  </files>
  <action>
    For each of the 8 list APIs (deals, assets, entities, investors, documents, tasks, transactions, meetings):
    1. Update GET handler to use parsePaginationParams and buildPrismaArgs
    2. Return PaginatedResult shape: { data: [...], nextCursor, hasMore, total }
    3. Add search across relevant text fields (e.g., deals: name, assets: name, entities: name)
    4. Add filter support in where clause

    For each of the 8 list pages:
    1. Replace direct useSWR with usePaginatedList hook
    2. Add SearchFilterBar at top with appropriate filters:
       - Deals: stage (SCREENING/DD/IC/CLOSING/CLOSED), assetClass, dealLead
       - Assets: assetClass, status (ACTIVE/EXITED/WRITTEN_OFF), entity
       - Entities: entityType, status (ACTIVE/WINDING_DOWN/DISSOLVED)
       - Directory/Investors: entity, type
       - Documents: category
       - Tasks: status (TODO/IN_PROGRESS/DONE), priority
       - Transactions: type, entity
       - Meetings: source (FIREFLIES/MANUAL)
    3. Add LoadMoreButton at bottom of list
    4. Update loading state to show spinner + descriptive text: "Loading deals...", "Loading assets...", etc.
    5. Add helpful empty state when no results: "No deals found" with contextual action (e.g., "Create your first deal" button or "Try different filters")
    6. Wrap financial data sections with SectionErrorBoundary

    IMPORTANT: The deals page has a pipeline/kanban view — pagination applies to the underlying data fetch, not the visual board. Keep the board rendering as-is but feed it paginated data. If the deals page uses a different list view mode, paginate that.

    Pattern for each page update:
    ```tsx
    const { data, isLoading, hasMore, loadMore, search, setSearch, filters, setFilter, total } = usePaginatedList(`/api/deals?firmId=${firmId}`);
    if (isLoading && !data.length) return <div className="flex items-center gap-2 text-sm text-gray-400"><Spinner /> Loading deals...</div>;
    if (!data.length) return <EmptyState message="No deals found" action="Create your first deal" />;
    ```
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 list pages show search bar + filters, use cursor-based pagination with Load More, have spinner loading states, and show helpful empty states. APIs return paginated results with nextCursor and hasMore. Build passes.</done>
</task>

</tasks>

<verification>
- GET /api/deals?firmId=X&limit=10 returns { data: [...10], nextCursor: "abc", hasMore: true, total: N }
- GET /api/deals?firmId=X&limit=10&cursor=abc returns next page
- GET /api/deals?firmId=X&search=acme filters by name containing "acme"
- GET /api/deals?firmId=X&stage=SCREENING filters by stage
- POST /api/ai/agents with 21st request in 1 minute returns 429 with Retry-After header
- Navigating to /deals shows search bar, filter dropdowns, and Load More button
- Throwing an error in a chart component shows "Something went wrong - Retry" instead of full page crash
- Empty states show actionable messages
- Build passes with zero errors
</verification>

<success_criteria>
- All 8 list pages converted to cursor-based pagination (50 default, Load More)
- Search bar and contextual filters on every list page
- Page-level ErrorBoundary in layout, section-level on charts/financial/AI content
- Loading: spinner + text. Empty: message + action. Error: message + retry
- AI endpoints rate limited (20/min, 200/hr per user) with proper 429 response
</success_criteria>

<output>
After completion, create `.planning/phases/04-asset-entity-polish/04-03-SUMMARY.md`
</output>
