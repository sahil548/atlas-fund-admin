---
phase: 04-asset-entity-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/lib/auth.ts
  - src/lib/routes.ts
  - src/lib/permissions.ts
  - src/app/api/settings/permissions/route.ts
  - src/app/(gp)/settings/page.tsx
  - src/components/features/settings/permissions-tab.tsx
  - src/components/features/settings/service-provider-manager.tsx
  - src/app/api/users/[id]/entity-access/route.ts
autonomous: true
requirements: [CORE-02, CORE-03]

must_haves:
  truths:
    - "LP_INVESTOR users hitting GP routes (/(gp)/*) get redirected to /lp/dashboard"
    - "LP_INVESTOR users calling GP API routes get 403 Forbidden"
    - "GP_ADMIN has unrestricted access to everything"
    - "GP_TEAM permissions are configurable by GP_ADMIN per area (deals, entities, capital, settings)"
    - "SERVICE_PROVIDER users can only VIEW data for entities in their entityAccess[] array"
    - "Service providers can be managed from both Settings page (central view) and Entity detail (contextual)"
    - "Meaningful actions are logged to an audit trail (create, edit, delete, view sensitive data)"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Role-based routing: LP redirect, role check on GP routes"
      contains: "LP_INVESTOR"
    - path: "src/lib/permissions.ts"
      provides: "Permission definitions and check functions"
      exports: ["checkPermission", "PERMISSION_AREAS", "getEffectivePermissions"]
    - path: "src/app/api/settings/permissions/route.ts"
      provides: "API for reading/updating GP_TEAM permissions"
    - path: "src/components/features/settings/service-provider-manager.tsx"
      provides: "UI for managing service provider entity access"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/auth.ts"
      via: "getAuthUser() for role check in middleware"
      pattern: "getAuthUser|user\\.role"
    - from: "src/lib/permissions.ts"
      to: "prisma.user"
      via: "database query for user permissions"
      pattern: "prisma\\.user\\.findUnique"
    - from: "src/app/api/settings/permissions/route.ts"
      to: "src/lib/permissions.ts"
      via: "uses permission definitions"
      pattern: "PERMISSION_AREAS"
---

<objective>
Enforce role-based access control across the entire application. LP users must be hard-blocked from GP pages. GP_TEAM permissions must be configurable. Service providers get read-only, entity-scoped access. Add audit logging for accountability.

Purpose: Currently NO role enforcement exists — any authenticated LP can access GP pages. This is a production security requirement. The user called LP enforcement "non-negotiable for production security."

Output: Enhanced middleware with role routing, permissions system, service provider management, audit logging.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-asset-entity-polish/04-CONTEXT.md

<interfaces>
<!-- Current middleware — only checks authentication, no role routing -->
From src/middleware.ts:
```typescript
import { clerkMiddleware, createRouteMatcher } from "@clerk/nextjs/server";
const isPublicRoute = createRouteMatcher(["/sign-in(.*)", "/sign-up(.*)", "/api/webhooks(.*)", ...]);
export default clerkMiddleware(async (auth, req) => {
  if (!isPublicRoute(req)) { await auth.protect(); }
});
```

From src/lib/auth.ts:
```typescript
export async function getAuthUser(): Promise<{ id, name, email, role, firmId, initials, isActive, contactId } | null>;
export function unauthorized(): NextResponse;
export function forbidden(): NextResponse;
```

From src/lib/routes.ts:
```typescript
export interface AppRoute { path, label, description, keywords, icon, sidebarIcon, portal: "gp" | "lp", priority }
export const APP_ROUTES: AppRoute[];
```

UserRole enum: GP_ADMIN, GP_TEAM, SERVICE_PROVIDER, LP_INVESTOR
User model has entityAccess String[] field (exists but never populated or checked)
DealActivity model pattern: dealId, activityType, description, metadata JSON, createdAt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware role enforcement + permissions system</name>
  <files>
    src/middleware.ts,
    src/lib/auth.ts,
    src/lib/permissions.ts,
    src/lib/routes.ts,
    prisma/schema.prisma,
    src/lib/schemas.ts
  </files>
  <action>
    1. Update src/middleware.ts to add role-based routing AFTER auth.protect():
       - If user role is LP_INVESTOR and path matches /(gp)/* or /dashboard or /deals or /entities or /assets or /directory or /documents or /tasks or /accounting or /meetings or /transactions or /settings: redirect to /lp/dashboard
       - If user role is LP_INVESTOR and path matches /api/* (except /api/lp/*): return 403 forbidden JSON
       - If user role is SERVICE_PROVIDER and path matches write endpoints (POST/PUT/PATCH/DELETE except GET): return 403 forbidden
       - If user role is SERVICE_PROVIDER: check entityAccess[] against requested entity — block if not in list
       - Note: Middleware needs to look up user role from DB. Add a lightweight helper in auth.ts that caches the lookup per request using Clerk userId.
    2. Create src/lib/permissions.ts:
       - Define PERMISSION_AREAS: ['deals', 'entities', 'capital_activity', 'investors', 'documents', 'settings', 'reports'] as const
       - Define PermissionLevel: 'full' | 'read_only' | 'none'
       - UserPermissions type: Record<PermissionArea, PermissionLevel>
       - DEFAULT_PERMISSIONS: GP_ADMIN gets 'full' everywhere, GP_TEAM gets 'read_only' everywhere (configurable), LP_INVESTOR gets 'none' everywhere, SERVICE_PROVIDER gets 'read_only' on assigned entities
       - checkPermission(user, area, requiredLevel): boolean — checks user role + stored permissions
       - getEffectivePermissions(userId): Promise<UserPermissions> — reads from DB
    3. Add Permission model to schema.prisma (or use JSON field on User for simplicity — Claude's discretion, but keep it simple for 3 GP users):
       - If JSON approach: add `permissions Json?` field to User model
       - If model approach: UserPermission { userId, area, level }
    4. Add AuditLog model to schema.prisma:
       - id (cuid), firmId, userId, action (String — e.g., "CREATE_DEAL", "UPDATE_ENTITY", "DELETE_DOCUMENT"), targetType (String — "Deal", "Entity", "Asset", etc.), targetId (String), metadata (Json? — what changed), createdAt
       - Relation: belongsTo User, belongsTo Firm
    5. Add helper function logAudit(firmId, userId, action, targetType, targetId, metadata?) in auth.ts or a new audit.ts file
    6. Update routes.ts AppRoute interface to include optional `requiredRole?: UserRole` for route-level role hints (not enforcement — middleware handles enforcement)
    7. Run prisma db push --force-reset, generate, seed
    8. Add Zod schemas: UpdatePermissionsSchema, AuditLogQuerySchema
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx prisma validate && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Middleware redirects LP users away from GP routes, returns 403 on GP API calls. Permission system defined with area-level granularity. AuditLog model exists. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Permissions settings UI + Service provider management</name>
  <files>
    src/app/api/settings/permissions/route.ts,
    src/app/api/users/[id]/entity-access/route.ts,
    src/app/api/audit-log/route.ts,
    src/app/(gp)/settings/page.tsx,
    src/components/features/settings/permissions-tab.tsx,
    src/components/features/settings/service-provider-manager.tsx
  </files>
  <action>
    1. Create /api/settings/permissions/route.ts:
       - GET: list all GP_TEAM users with their current permissions (GP_ADMIN only)
       - PUT: update permissions for a specific user (body: { userId, permissions: Record<area, level> })
       - Must verify caller is GP_ADMIN before allowing changes
    2. Create /api/users/[id]/entity-access/route.ts:
       - GET: list entityAccess[] for a user (for SERVICE_PROVIDER management)
       - PUT: update entityAccess[] array (body: { entityIds: string[] })
       - Must verify caller is GP_ADMIN
    3. Create /api/audit-log/route.ts:
       - GET: list audit logs with filtering (query: firmId, targetType?, userId?, startDate?, endDate?, limit=50)
       - Paginated with cursor
    4. Add "Permissions" tab to settings page (src/app/(gp)/settings/page.tsx):
       - Only visible to GP_ADMIN users
       - Tab shows permissions-tab.tsx component
    5. Create permissions-tab.tsx:
       - Table showing each GP_TEAM user as a row
       - Columns: user name, then one column per PERMISSION_AREA
       - Each cell is a dropdown: Full / Read Only / None
       - Save button per user row (or auto-save on change)
       - Use SWR for data, fetch for mutations, mutate for cache update
    6. Create service-provider-manager.tsx:
       - Reusable component used in both Settings page and Entity detail
       - Props: { entityId?: string } — if entityId provided, scoped to that entity; if not, shows all service providers
       - Lists service providers with their assigned entities
       - "Add Entity Access" button with entity multi-select dropdown
       - "Remove" button per entity assignment
       - Calls /api/users/[id]/entity-access for mutations
    7. Wire audit logging into key API routes by calling logAudit() after successful mutations:
       - POST /api/deals (CREATE_DEAL)
       - PATCH /api/deals/[id] (UPDATE_DEAL, KILL_DEAL, CLOSE_DEAL)
       - POST /api/entities (CREATE_ENTITY)
       - PUT /api/entities/[id] (UPDATE_ENTITY)
       - POST /api/capital-calls (CREATE_CAPITAL_CALL)
       - POST /api/distributions (CREATE_DISTRIBUTION)
       - (Add to the most impactful 6-8 endpoints, not all 73)
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>GP_ADMIN can configure GP_TEAM permissions via Settings > Permissions tab. Service provider entity access manageable from Settings (central) and Entity detail (contextual). Audit log records key actions and is queryable by type/user/date. Build passes.</done>
</task>

</tasks>

<verification>
- LP user accessing /dashboard gets redirected to /lp/dashboard
- LP user calling POST /api/deals gets 403
- GP_ADMIN can access everything
- GP_TEAM with "read_only" on deals can GET /api/deals but cannot POST /api/deals
- SERVICE_PROVIDER can only see entities in their entityAccess[] array
- Permissions tab shows area x user matrix with dropdowns
- Audit log records deal creation, entity update, capital call creation
- Build passes with zero errors
</verification>

<success_criteria>
- LP_INVESTOR hard-blocked from all GP routes (redirect) and GP APIs (403)
- GP_TEAM permissions configurable per area by GP_ADMIN
- SERVICE_PROVIDER read-only and entity-scoped via entityAccess[]
- Audit log captures meaningful actions with who/what/when
- Service providers manageable from both Settings and Entity detail pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-asset-entity-polish/04-02-SUMMARY.md`
</output>
