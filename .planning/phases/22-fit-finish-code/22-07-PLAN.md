---
phase: 22-fit-finish-code
plan: 07
type: execute
wave: 3
depends_on: [1, 2, 3, 4]
files_modified:
  - src/app/api/waterfall-templates/[id]/calculate/route.ts
  - src/lib/computations/pref-accrual.ts
  - src/lib/computations/__tests__/waterfall.test.ts
  - prisma/seed.ts
  - src/lib/__tests__/deal-dd-progress.test.ts
autonomous: true
requirements: [FIN-02, FIN-08]
walkthrough_items: [Obs 5]

must_haves:
  truths:
    - "src/app/api/waterfall-templates/[id]/calculate/route.ts no longer inlines days360Exclusive, days360Inclusive, buildPicTimeline, walkPicTimeline — it imports them from pref-accrual.ts"
    - "All waterfall/pref/fee-engine tests pass after the refactor (baseline count preserved)"
    - "22-07-VERIFICATION.md contains a section for BUG-01 with one of two explicitly labeled outcomes: `[CLOSED: seed fix — <description>]` (with seed commit reference and screenshot of populated DD tab on IC Review deal) OR `[REOPENED: <reproduction steps> + <filed issue reference>]` (with exact repro and issue tracker link)"
    - "22-07-VERIFICATION.md contains a section for BUG-02 with one of two explicitly labeled outcomes: `[CLOSED: <description>]` (with screenshot of stat cards showing no broken >100% percentage) OR `[REOPENED: <reproduction steps> + <filed issue reference>]`"
    - "22-07-VERIFICATION.md contains a section for BUG-03 with one of two explicitly labeled outcomes: `[CLOSED: <description>]` (with screenshot of IC Memo rendering on a DD-stage deal without stuck spinner) OR `[REOPENED: <reproduction steps> + <filed issue reference>]`"
  artifacts:
    - path: "src/app/api/waterfall-templates/[id]/calculate/route.ts"
      provides: "Waterfall calculate orchestrator that imports pref math (no inlined duplicate)"
      contains: "from \"@/lib/computations/pref-accrual\""
    - path: "prisma/seed.ts"
      provides: "Seed with workstream records for at least one IC-Review stage deal (BUG-01 fix if seed-gap diagnosis applies)"
      contains: "dDWorkstream"
    - path: ".planning/phases/22-fit-finish-code/22-07-VERIFICATION.md"
      provides: "Plan VERIFICATION with one explicitly labeled [CLOSED: ...] or [REOPENED: ...] block per BUG-01/02/03"
      contains: "[CLOSED"
  key_links:
    - from: "src/app/api/waterfall-templates/[id]/calculate/route.ts"
      to: "src/lib/computations/pref-accrual.ts"
      via: "ES imports of days360Exclusive, days360Inclusive, buildPicTimeline, walkPicTimeline"
      pattern: "import.*\\{(days360Exclusive|days360Inclusive|buildPicTimeline|walkPicTimeline)"
---

<objective>
Close FIN-02 (waterfall route refactor to import from pref-accrual.ts) and FIN-08 (formally re-verify the three March-5 bugs). Plus Obs 5 (IC Review deal showing zero workstreams) — rolls under FIN-08 per CONTEXT.md.

Purpose: FIN-02 removes a ~120-line duplication between the API route and the canonical pref-accrual module — any future correctness fix lands in one place. FIN-08 is evidence-based closeout: the walkthrough spot-checked BUG-02 and BUG-03 as apparently-resolved; this plan makes that formal (screenshot + written "closed by observation" note) and handles the single remaining BUG-01 variant (post-DD deals showing zero workstreams, which per RESEARCH.md is a seed gap, not a code bug).

**FIN-08 evidence shape (per checker W5).** Each of BUG-01/02/03 MUST land in 22-07-VERIFICATION.md as a named section with one of exactly two header forms:
- `[CLOSED: <short description of what was found/fixed>]` followed by evidence (screenshot, terminal output, or commit reference).
- `[REOPENED: <exact reproduction steps>] — filed as <issue tracker ref>` followed by evidence.

This keeps the must_have truths unconditionally checkable: a reviewer greps the VERIFICATION file for `[CLOSED:` or `[REOPENED:` and sees exactly three matches (one per bug). No ambiguous "maybe fixed" or "looks resolved" language.

Output: A refactored waterfall route whose math lives in pref-accrual.ts, an expanded seed covering post-DD deal workstreams (if BUG-01 closes by seed fix), and a formal VERIFICATION document with explicitly labeled closure/reopen blocks for each of the three March-5 bugs.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-fit-finish-code/22-CONTEXT.md
@.planning/phases/22-fit-finish-code/22-RESEARCH.md
@.planning/phases/22-fit-finish-code/22-VALIDATION.md
@.claude/rules/coding-patterns.md

@src/app/api/waterfall-templates/[id]/calculate/route.ts
@src/lib/computations/pref-accrual.ts
@src/lib/computations/__tests__/waterfall.test.ts
@src/lib/computations/__tests__/capital-accounts.test.ts
@prisma/seed.ts
@src/lib/deal-stage-engine.ts

<interfaces>
<!-- FIN-02 refactor: the route has ~120 lines of inlined math that already
     exists as exports in pref-accrual.ts. This is a mechanical substitution. -->

From src/lib/computations/pref-accrual.ts (verified exports in RESEARCH.md):
```typescript
export function days360Exclusive(start: Date, end: Date): number;
export function days360Inclusive(start: Date, end: Date): number;
export function rocEffectiveDate(paidDate: Date): Date;
export interface PicEvent {
  date: Date;
  type: "CALL" | "ROC";
  amount: number;
}
export interface PrefSegment {
  startDate: Date;
  endDate: Date;
  pic: number;
  prefAmount: number;
}
export function buildPicTimeline(events: PicEvent[]): PicEvent[];
export function walkPicTimeline(
  timeline: PicEvent[],
  config: { prefRate: number; compounding?: "SIMPLE" | "COMPOUND" }
): PrefSegment[];
```

Route inlined functions to replace:
- `days360Exclusive` (route lines 221-232)
- `days360Inclusive` (route lines 233-239)
- Full PIC-timeline walk block (route lines 328-401)

After refactor, the route keeps:
- Database reads (fetch waterfall template, vehicle, investors, capital activity)
- Config construction (prefRate, feeEngine setup)
- `computeWaterfall` invocation
- Response shaping

VERIFICATION block template (for BUG-01/02/03 per-bug section):
```
### BUG-01 — DD tab 0% / zero workstreams on post-DD deals (Obs 5)

[CLOSED: <one-line description of what was fixed — e.g., "seed gap — added
dDWorkstream records for IC_REVIEW and CLOSING stage deals">]

Evidence:
- Seed change committed in <commit ref>
- Screenshot: IC Review deal DD tab showing populated workstream list
- Test: `deal-dd-progress.test.ts` new regression test passes

OR

[REOPENED: <exact reproduction steps — e.g., "Navigate to IC Review deal,
click DD tab, workstream list is empty despite seed containing dDWorkstream
records for this deal. See `prisma studio` confirming records exist but
component is not rendering them. Filed as issue #<N> in <tracker>">]

Evidence:
- Issue link: <URL>
- Screenshot of empty DD tab
- Prisma Studio screenshot showing records exist
```

Apply the same template shape to BUG-02 and BUG-03 sections.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Snapshot baseline test count before refactor</name>
  <files>(none — measurement only)</files>
  <action>
Before any refactor, run the vitest suite and record:
1. Total test count (the "87 tests" figure from STATE.md is our baseline expectation).
2. Which files contain waterfall/pref tests (grep through test files for `computeWaterfall`, `days360`, `buildPicTimeline`, `walkPicTimeline`, `prefAccrual`).

```bash
npx vitest run 2>&1 | tail -30
```

Record the exact count in a scratch note (to be copied into VERIFICATION.md later).

Also record: the exact functions inlined in `src/app/api/waterfall-templates/[id]/calculate/route.ts` by running:
```bash
grep -n "days360Exclusive\|days360Inclusive\|buildPicTimeline\|walkPicTimeline" src/app/api/waterfall-templates/\[id\]/calculate/route.ts
```

The output tells Task 2 exactly which definitions need to be replaced with imports.

No file edits in this task. It's a 3-minute measurement.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
    Task 2 can proceed only when baseline test count is recorded.
  </verify>
  <done>Baseline vitest count and inlined-function line numbers recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor waterfall calculate route to import from pref-accrual (FIN-02)</name>
  <files>
    src/app/api/waterfall-templates/[id]/calculate/route.ts
    src/lib/computations/__tests__/waterfall.test.ts
  </files>
  <behavior>
    - Test 1: All baseline tests pass after refactor (count preserved, none new-failing).
    - Test 2: Route no longer declares `days360Exclusive`, `days360Inclusive`, `buildPicTimeline`, `walkPicTimeline` — grep confirms only import statements match those names.
    - Test 3: An end-to-end calculate call against a seeded waterfall template returns numerically identical output before and after the refactor (byte-compare the JSON response).
    - Edge case: If the inlined code had any subtle deviation from pref-accrual (e.g. different rounding), the refactor must surface it — either harmonize to the canonical implementation OR document the divergence with a written justification. The canonical implementation in pref-accrual.ts is the reference; inlined drift is the bug.
  </behavior>
  <action>
**Mechanical refactor. Minimal-diff.**

1. **Add imports** at the top of `route.ts`:
   ```typescript
   import {
     days360Exclusive,
     days360Inclusive,
     buildPicTimeline,
     walkPicTimeline,
     type PicEvent,
     type PrefSegment,
   } from "@/lib/computations/pref-accrual";
   ```

2. **Delete the inlined function declarations** at the lines recorded in Task 1. Remove the local `PicEvent` type alias (line ~328 area) if one was declared inline — import it from pref-accrual instead.

3. **Verify the segment-walk loop usage** matches `walkPicTimeline`'s signature. If the route's inline walk took additional parameters (e.g. custom fee offsets) not exposed by `walkPicTimeline`, do ONE of:
   - Extend `walkPicTimeline` to accept those parameters (add optional argument; default matches existing behavior). This is preferable.
   - Keep a thin wrapper inline that calls `walkPicTimeline` then applies the extra logic. Document why.

4. **Run the full test suite** and compare to baseline. Any regression must be diagnosed before commit.

5. **End-to-end numerical smoke test:**
   - Before commit (in Task 1 actually, extend there): capture the response JSON of `/api/waterfall-templates/<seeded-id>/calculate` via a dev server.
   - After refactor: capture again. `diff` them. If identical → refactor is safe. If they differ → investigate (this is the "inlined drift" edge case above).

6. **Optional addition — new test** in `src/lib/computations/__tests__/waterfall.test.ts`: a regression-lock test that runs a seeded waterfall template through `/calculate` and asserts a snapshot of the response. This locks the behavior going forward.

Follow coding-patterns.md: `parseBody` for any body parsing; `prisma` singleton; no hardcoded firmId. No schema changes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/computations 2>&1 | tail -20</automated>
    <automated>npx vitest run 2>&1 | tail -10</automated>
    <automated>grep -cE "(days360Exclusive|days360Inclusive|buildPicTimeline|walkPicTimeline)\s*=" src/app/api/waterfall-templates/\[id\]/calculate/route.ts 2>&1</automated>
    (The last grep should output 0 — no local definitions remaining.)
    <automated>npm run build 2>&1 | tail -20</automated>
    Manual: Run `/api/waterfall-templates/<seeded-id>/calculate` in browser, save response. Confirm numbers match what came out before the refactor (Task 1 captured baseline).
  </verify>
  <done>Route imports pref math; zero local definitions remain; all baseline tests pass; calculate response is byte-identical before/after. `npm run build` clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: BUG-01 — add post-DD deal workstreams to seed (Obs 5 + FIN-08 partial)</name>
  <files>
    prisma/seed.ts
    src/lib/__tests__/deal-dd-progress.test.ts
  </files>
  <action>
**Seed fix + regression test.**

Per RESEARCH.md: the DD tab symptom the walkthrough described ("IC Review deal shows zero workstreams, empty list") is most likely a seed gap — post-DD deals were seeded without workstream records. Diagnose briefly:

1. Run `npx prisma studio` (or a quick script). Query: `DDWorkstream.findMany({ where: { deal: { stage: "IC_REVIEW" } } })`. If empty → seed gap confirmed.
2. If non-empty but the UI still shows zero → the bug is in the `AssetTasksTab` / DD tab component filtering. Trace from there.

**If seed gap (most likely):**

1. In `prisma/seed.ts`, find where DD workstreams are seeded for DD-stage deals (there must be a block creating `prisma.dDWorkstream.create({ ... })` or similar).
2. Add equivalent workstreams for at least one IC_REVIEW stage deal and at least one CLOSING stage deal. Use the same structure (workstream type, status, progress percentage) so the DD tab renders the same way it does for DD-stage deals.
3. Run `npx prisma db seed` and verify workstreams appear on the IC Review deal's DD tab.

**Regression test in `src/lib/__tests__/deal-dd-progress.test.ts`:**

Add a new test:
```typescript
it("BUG-01: post-DD stage deals have workstreams", async () => {
  const postDDDeals = await prisma.deal.findMany({
    where: { stage: { in: ["IC_REVIEW", "CLOSING", "CLOSED"] } },
  });
  for (const deal of postDDDeals) {
    const ws = await prisma.dDWorkstream.findMany({ where: { dealId: deal.id } });
    expect(ws.length).toBeGreaterThan(0);
  }
});
```

**Also: check `src/lib/deal-stage-engine.ts`** — does advancing a deal past DD stage ever `deleteMany` on its workstreams? If yes, that's the real root cause, not the seed. Read the advance-stage logic and confirm it does NOT delete workstreams. If it does, remove that delete call (deleting workstream history is wrong — workstreams are historical records of DD completion).

**BUG-01 VERIFICATION block:** Write the outcome into `22-07-VERIFICATION.md` using the labeled-block format from this plan's <interfaces> section. Either:
- `[CLOSED: seed fix — added dDWorkstream records for IC_REVIEW and CLOSING stage deals in prisma/seed.ts]` + evidence (screenshot, test pass, commit ref)
- `[REOPENED: <exact reproduction steps>] — filed as <issue tracker ref>` + evidence

The block MUST start with one of those two exact tags. The must_have truths key on this.

Follow coding-patterns.md for any code changes.
  </action>
  <verify>
    <automated>npx prisma db seed 2>&1 | tail -10</automated>
    <automated>npx vitest run src/lib/__tests__/deal-dd-progress.test.ts 2>&1 | tail -20</automated>
    <automated>npm run build 2>&1 | tail -20</automated>
    Manual: Open an IC Review stage deal → Due Diligence tab. Confirm workstreams list is populated (not empty).
  </verify>
  <done>Post-DD deals have workstream records in seed (if seed-gap diagnosis applies). New regression test passes. DD tab renders workstreams on IC Review deals. `npm run build` clean. VERIFICATION.md BUG-01 section begins with exactly `[CLOSED:` or `[REOPENED:`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: BUG-02 + BUG-03 formal closeout (FIN-08 remainder)</name>
  <files>(verification-only; no code edits expected)</files>
  <action>
**Evidence-gathering task. No code change unless a regression is discovered.**

**BUG-02 (pass rate 300%):**

Per RESEARCH.md Obs 1 walkthrough finding, "No pass-rate percentage shown above 100%." The percentage display has apparently been removed or fixed.

1. Go to `/deals` and screenshot the stat cards at the top. Confirm no >100% percentage is rendered anywhere.
2. If percentage is not displayed at all, record the finding as: `[CLOSED: percentage display removed — stat cards show raw numbers only]`.
3. If a percentage IS displayed and exceeds 100%, that's a regression. Investigate `src/app/(gp)/deals/page.tsx` and the calculation. Fix if found. If still broken after investigation, record `[REOPENED: ...]` with repro steps and file an issue.

**BUG-03 (IC Memo stuck spinner):**

Per RESEARCH.md Obs 4: "IC Memo visible on Overview — no stuck spinner."

1. Open a DD-stage deal → Overview tab → find the IC Memo section. Confirm it shows content (not a stuck "Generating..." spinner).
2. Try triggering IC Memo generation on a deal without one. Confirm the spinner eventually resolves (to success or to an actionable error).
3. If passing, record `[CLOSED: IC Memo renders correctly on DD-stage deal; generation resolves within N seconds on test deal]`.
4. If the spinner is stuck, investigate `src/components/features/deals/deal-overview-tab.tsx` per coding-patterns.md BUG-03 note. Fix if found. If still stuck, record `[REOPENED: ...]` with repro steps and file an issue.

**VERIFICATION.md shape — mandatory block headers:**

Each bug's section in `22-07-VERIFICATION.md` MUST begin with one of exactly two labeled headers:
- `[CLOSED: <short description>]`  — followed by evidence (screenshot, test, commit ref)
- `[REOPENED: <exact reproduction steps>] — filed as <issue tracker ref>`  — followed by evidence

Do NOT use ambiguous language like "seems fixed" or "looks resolved" outside a `[CLOSED: ...]` block. The must_have truths grep on these tags. If no issue tracker exists for a REOPENED bug, create a lightweight one (a new issue file at `.planning/issues/bug-XX-<date>.md` is acceptable) and link it.

Do NOT create new issue files for BUGs that are re-closed — the VERIFICATION `[CLOSED: ...]` note is the closeout record.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    Manual: Screenshots of `/deals` stat cards confirming no broken percentage; screenshots of IC Memo rendering on a DD-stage deal confirming no stuck spinner.
  </verify>
  <done>Both BUGs re-verified. VERIFICATION.md has one `[CLOSED: ...]` or `[REOPENED: ...]` header per bug (BUG-02 and BUG-03) with screenshot evidence.</done>
</task>

</tasks>

<verification>
Per VALIDATION.md: `npx vitest run src/lib/computations` after Task 2 (FIN-02 refactor risk surface); `npm run build` every commit; `npx vitest run` full suite at plan close.

Manual checks for VERIFICATION.md:
- Grep of route.ts shows zero local definitions of pref-accrual functions (FIN-02)
- Baseline test count preserved (FIN-02)
- Calculate endpoint produces numerically identical output (FIN-02)
- BUG-01 section begins with exactly `[CLOSED:` or `[REOPENED:` — evidence per label
- BUG-02 section begins with exactly `[CLOSED:` or `[REOPENED:` — evidence per label
- BUG-03 section begins with exactly `[CLOSED:` or `[REOPENED:` — evidence per label

Post-plan grep check: `grep -c "^\[CLOSED:\|^\[REOPENED:" 22-07-VERIFICATION.md` must return exactly 3.
</verification>

<success_criteria>
1. `src/app/api/waterfall-templates/[id]/calculate/route.ts` imports days360*/buildPicTimeline/walkPicTimeline from pref-accrual.ts and declares none of them locally.
2. All baseline waterfall/pref tests pass post-refactor.
3. Calculate endpoint output is numerically identical before and after.
4. Post-DD deals have workstream records in seed (if BUG-01 closes by seed fix); DD tab renders them.
5. `22-07-VERIFICATION.md` contains exactly 3 `[CLOSED:` or `[REOPENED:` headers (one per BUG-01/02/03), each followed by evidence.
6. `npm run build` clean. `npx vitest run` green.
</success_criteria>

<output>
After completion, create `.planning/phases/22-fit-finish-code/22-07-VERIFICATION.md` with: baseline vs. post-refactor test counts; grep output confirming no local pref-math in route.ts; calculate-endpoint response diff (before vs. after) showing zero numerical change; three labeled closeout blocks (one per bug, starting with either `[CLOSED: ...]` or `[REOPENED: ...]`) with screenshots and repro steps as applicable.
</output>
