---
plan_number: "22-13"
wave: 6
depends_on: [12]
autonomous: true
requirements: []
walkthrough_items: []
files_modified:
  - src/lib/schemas.ts
  - src/app/api/assets/[id]/route.ts
  - src/app/api/assets/[id]/valuations/[valuationId]/route.ts
  - src/components/features/assets/edit-valuation-form.tsx
  - src/app/(gp)/assets/[id]/page.tsx
gap_closure: true
---

# Plan 22-13: Valuation Audit Trail

<objective>
Close punch-list gap #6. The `Valuation` Prisma model already has `approvedBy` (String?) and `approvedAt` (DateTime?) columns, but they were never populated: the PUT route ignored the auth user, the GET route never resolved the approver's name, and the UI never displayed approval metadata.

Effect for an LP-reporting fund: when an approved valuation is sent or cited, you can't answer "who approved this and when?" without digging through git. That's an audit gap.

Small, targeted plan:
- On DRAFT → APPROVED transition, stamp `approvedBy` + `approvedAt`.
- On APPROVED → DRAFT revert, clear them (so re-approval re-stamps).
- Resolve the approver's display name + initials in the asset GET response (in-memory user lookup — no schema change, no Prisma relation add).
- Show "Approved by X on DATE" in the Edit Valuation modal when viewing an APPROVED valuation.
- Add "Approved By" + "Approved At" columns to the Valuation History table on the asset Overview tab.
</objective>

<must_haves>
<truths>
- `UpdateValuationSchema` accepts an optional `approvedBy` field so forms can pass the current userId (needed for the mock UserProvider in dev; Clerk session populates server-side in prod)
- `PUT /api/assets/[id]/valuations/[valuationId]` on status change DRAFT → APPROVED writes `approvedBy = authUser?.id ?? data.approvedBy ?? null` and `approvedAt = new Date()`
- Reverse transition APPROVED → DRAFT clears both fields back to null
- `GET /api/assets/[id]` attaches `valuation.approver = { id, name, initials } | null` for every valuation that has a resolvable `approvedBy`
- Edit Valuation modal header shows "Approved by <Name> on <Date>" when the valuation is APPROVED
- Valuation History table on `/assets/[id]` Overview tab has two new columns: "Approved By" (with initials badge + name) and "Approved At" (date)
- Reverting an approved valuation (status back to DRAFT) clears the stamp — table shows "—" for both columns again
- `npm run build` passes with zero TypeScript errors
</truths>
</must_haves>

<tasks>

<task id="22-13-01">
<action>
Extend `UpdateValuationSchema` in `src/lib/schemas.ts` with optional `approvedBy: z.string().optional()`. Comment explains why (dev-mode fallback).
</action>
</task>

<task id="22-13-02">
<action>
Update `PUT /api/assets/[id]/valuations/[valuationId]/route.ts`:
- Capture `authUser` return value (was discarded).
- Build `approvalUpdate` record based on status transition: DRAFT→APPROVED stamps, APPROVED→DRAFT clears, no-change no-op.
- Spread `approvalUpdate` into the Prisma `update.data` call.
</action>
</task>

<task id="22-13-03">
<action>
Update `GET /api/assets/[id]/route.ts` response build:
- Collect unique non-null `approvedBy` IDs from `asset.valuations`.
- Single `prisma.user.findMany({ id: { in: ids } })` to resolve name + initials.
- Attach `approver: {id, name, initials} | null` to each valuation in the response.
</action>
</task>

<task id="22-13-04">
<action>
Update `src/components/features/assets/edit-valuation-form.tsx`:
- Import `useUser` from user-provider.
- When submitting APPROVED status (transition), include `approvedBy: userId` in the payload.
- Show an "Approved by <Name> on <Date>" line inside the existing amber banner when viewing an APPROVED valuation.
</action>
</task>

<task id="22-13-05">
<action>
Update `/assets/[id]/page.tsx` Valuation History table:
- Add two columns between "Status" and "Notes": "Approved By" (initials badge + name), "Approved At" (date or "—").
- Handle the fallback case where `approvedBy` is set but `approver` lookup returned null (e.g., user was deleted) — render the raw ID as italic gray.
</action>
</task>

</tasks>

<verification>

**Build:** `npm run build` — PASSED, 116/116 pages, zero TS errors.

**API round-trip (script evidence in 22-13-SUMMARY.md):**
- Create a DRAFT valuation via POST.
- PUT status=APPROVED with `approvedBy: "user-jk"` in body.
- Response shows status=APPROVED, approvedBy="user-jk", approvedAt=<now>.
- GET asset shows valuation with `approver: { id: "user-jk", name: "James Kim", initials: "JK" }`.

**Browser (deferred to user on production):**
- After deploy, open an asset detail page in Chrome and verify the Valuation History table shows Approved By + Approved At columns.
- Log a new valuation, mark it APPROVED, confirm the columns populate with the current user's name and today's date.
- Open Edit Valuation on an APPROVED record — amber banner now shows "Approved by <Name> on <Date>".

</verification>

