---
plan_number: "22-14"
wave: 6
depends_on: [13]
autonomous: true
requirements: []
walkthrough_items: []
files_modified:
  - src/lib/schemas.ts
  - src/app/api/assets/route.ts
  - src/components/features/assets/edit-asset-form.tsx
  - src/components/features/assets/create-asset-form.tsx
gap_closure: true
---

# Plan 22-14: Projected Metrics JSON Blob

<objective>
Close punch-list gap #4. `Asset.projectedMetrics` is a `Json?` column whose shape is validated by `ProjectedMetricsSchema` — capRate, cashOnCash, yieldToMaturity, currentYield, revenueMultiple, ebitdaMultiple, plus generic targetIRR/targetMultiple (the latter two are redundant with the scalar projectedIRR/projectedMultiple added in 22-10, skipped).

Before this plan: schema exists, UpdateAssetSchema accepts it, zero UI references. Effectively dead.

After: per-class projected metrics editable in Add/Edit Asset. Same type-conditional rendering pattern as the other fieldsets in 22-10/11/12.
</objective>

<must_haves>
<truths>
- `CreateAssetSchema` accepts `projectedMetrics: ProjectedMetricsSchema.optional()`
- `POST /api/assets` persists the blob as Prisma `InputJsonValue` when supplied
- Edit + Add Asset render a "Projected Metrics" fieldset after the type-conditional section
- Fieldset fields switch on detected `kind`:
  - REAL_ESTATE → Projected Cap Rate (%), Projected Cash-on-Cash (%)
  - PRIVATE_CREDIT → Projected YTM (%), Projected Current Yield (%)
  - OPERATING → Projected Revenue Multiple (x), Projected EBITDA Multiple (x)
  - LP_INTEREST → fieldset hidden (no LP-specific projection fields in schema)
- Edit form prefills from `asset.projectedMetrics` on open
- Edit submit only sends fields relevant to the current kind, and only if at least one is populated (or if a previously-set blob is being cleared)
- `npm run build` passes
</truths>
</must_haves>

<tasks>

<task id="22-14-01">
<action>
Extend `CreateAssetSchema` in `src/lib/schemas.ts` with `projectedMetrics: ProjectedMetricsSchema.optional()`.
</action>
</task>

<task id="22-14-02">
<action>
Update `POST /api/assets/route.ts`:
- Destructure `projectedMetrics` from validated body.
- Spread into `prisma.asset.create.data` with `InputJsonValue | NullableJsonNullValueInput` cast (same pattern as the PUT route).
</action>
</task>

<task id="22-14-03">
<action>
Update `edit-asset-form.tsx`:
- Extend `Props.asset` to include `projectedMetrics` typed object.
- Add 6 new string form fields (`pmCapRate`, `pmCashOnCash`, `pmYieldToMaturity`, `pmCurrentYield`, `pmRevenueMultiple`, `pmEbitdaMultiple`).
- Prefill from `asset.projectedMetrics` on open.
- In submit: build a `pm` record with only the fields for the detected `kind`; send `payload.projectedMetrics = pm` if any field set, or always send when clearing previously-set values.
- Render "Projected Metrics" fieldset after the type-conditional section with per-kind fields.
</action>
</task>

<task id="22-14-04">
<action>
Update `create-asset-form.tsx`:
- Add the same 6 string form fields + reset logic.
- In submit: build `pm` record per-kind and include in payload if non-empty.
- Render "Projected Metrics" fieldset mirroring Edit.
</action>
</task>

</tasks>

<verification>

**Build:** `npm run build` — PASSED, 116/116 pages, zero TS errors.

**Browser (production):** To be verified via Claude-in-Chrome after Vercel deploy — open Edit Asset on an Operating Business asset, confirm "Projected Metrics" fieldset appears with "Projected Revenue Multiple (x)" and "Projected EBITDA Multiple (x)", set values, save, reload, confirm values persist in `asset.projectedMetrics`.

</verification>

