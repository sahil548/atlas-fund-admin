---
phase: 22-fit-finish-code
plan: 06
type: execute
wave: 3
depends_on: [1, 2, 3, 4]
files_modified:
  - src/app/(gp)/assets/page.tsx
  - src/app/(gp)/entities/page.tsx
  - src/app/(gp)/meetings/page.tsx
  - src/lib/pagination.ts
  - src/app/api/assets/route.ts
  - src/app/(gp)/assets/[id]/page.tsx
  - src/components/features/entities/tabs/entity-overview-tab.tsx
  - src/components/features/entities/tabs/entity-cap-table-tab.tsx
  - src/app/api/deals/[id]/route.ts
  - src/components/features/deals/deal-delete-button.tsx
  - src/app/api/ai/execute/route.ts
  - src/app/api/ai/suggest-tasks/route.ts
  - src/app/api/ai/draft-lp-update/route.ts
  - src/app/api/documents/[id]/extract/route.ts
  - src/lib/error-messages.ts
autonomous: true
requirements: [FIN-09, FIN-10, FIN-11]
walkthrough_items: [Obs 3, Obs 7, Obs 8, Obs 18, Obs 21, Obs 22, Obs 27, Obs 39, Obs 44]

must_haves:
  truths:
    - "Asset list class filter buttons filter the rendered list"
    - "Entity list has working column sort AND search input"
    - "Meetings list has working sort (by date and by title/type)"
    - "Clicking a task row on an asset's Tasks tab navigates to /tasks/[id]"
    - "Clicking a task row on an entity's Overview tab navigates to /tasks/[id]"
    - "Clicking an investor row on an entity's Cap Table tab navigates to /investors/[id]"
    - "Deleting a non-SCREENING / non-DEAD deal surfaces a descriptive stage-gate message to the user (not 'Unauthorized')"
    - "AI routes that fail due to missing keys or disabled AI access return actionable messages"
    - "Document AI summary returns an actionable message when prerequisites are missing"
    - "Deletion bypass finding (Obs 7) is diagnosed and documented — either the bypass is closed OR the flow is clarified in the UI"
  artifacts:
    - path: "src/lib/error-messages.ts"
      provides: "Centralized error-code map (planner's choice per CONTEXT.md discretion)"
      exports: ["ERR"]
    - path: "src/app/(gp)/entities/page.tsx"
      provides: "Entity list with sort + search"
      contains: "sortKey"
    - path: "src/app/(gp)/meetings/page.tsx"
      provides: "Meetings list with date/title sort"
      contains: "sortKey"
  key_links:
    - from: "src/app/(gp)/assets/page.tsx"
      to: "/api/assets (filter params)"
      via: "buildUrl appends activeFilters correctly so API receives assetClass/status"
      pattern: "assetClass"
    - from: "AssetTasksTab"
      to: "/tasks/[id]"
      via: "<Link href> wrap on task row"
      pattern: "tasks/\\$\\{.*\\.id"
    - from: "entity-cap-table-tab.tsx"
      to: "/investors/[id]"
      via: "<Link href> on investor name cell"
      pattern: "investors/\\$\\{.*\\.id"
---

<objective>
Close the polish batch: FIN-09 (error copy), FIN-10 (list controls), FIN-11 (record linkage), plus Obs 7 (deletion bypass investigation). These are all small, uniform wire-ups and copy fixes with high user-facing value.

Per CONTEXT.md Claude's Discretion on FIN-09 error copy: **use a centralized error-code map in `src/lib/error-messages.ts`.** Rationale: the three AI routes (ai/execute, ai/suggest-tasks, ai/draft-lp-update) all return the same "Unauthorized" literal for the same missing-key condition; centralizing avoids drift and makes Phase 23 docs retrofit easier. Deal stage-gate copy is already descriptive in the API; the client-side error surfacing is the actual fix needed there.

Purpose: This plan is the "1000 papercuts" fix — every item is small, none is a blocker, but together they represent most of the walkthrough's list of urgent items. Ship as one plan because they all touch list-control and linkage patterns, and the planner's time is better spent doing them in one sweep than fragmenting.

**Scope-density acknowledgment (per checker W3 feedback).** This plan touches 15 files across four tasks. That is above the "~5 files" soft guideline for a single plan but BELOW the "7+ files = must split" hard threshold — the warning was "borderline", not "violation". The planner chose to keep this as one plan rather than split into 22-06a (list controls + record linkage) and 22-06b (error copy + Obs 7) for three reasons:
1. **Uniform patterns.** Every item is one of: list-state hook, Link wrap, error-string swap, or grep-driven audit. Splitting would fragment the context without simplifying any single task.
2. **Preserved plan count.** CONTEXT.md targeted 7-8 plans nominally; Phase 22 is at 9 plans (8 execution + 1 SUMMARY). Splitting 22-06 would push that to 10 without changing what gets built — the number is indicative, not prescriptive, and 10 is still reasonable.
3. **No wave-shifting.** 22-06 is Wave 3. Splitting would either keep both halves in Wave 3 (no parallelism gain) or reorder dependencies (complicating the graph without benefit).

**Required compensation: per-item VERIFICATION evidence.** Because this plan is denser than most, VERIFICATION.md MUST contain a dedicated checklist entry per walkthrough item (Obs 3, 7, 8, 18, 21, 22, 27, 39, 44) AND per FIN-XX (FIN-09, FIN-10, FIN-11). Each entry gets its own evidence block — screenshot, terminal output, or written diagnostic. Do NOT roll multiple items into one "passed all checks" line. The density is OK; the evidence granularity must match.

Output: Working filters/sort on three list pages, links from task rows to tasks and investor rows to CRM, actionable error copy across five+ routes, and a written diagnosis of the Obs 7 deletion bypass.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-fit-finish-code/22-CONTEXT.md
@.planning/phases/22-fit-finish-code/22-RESEARCH.md
@.planning/phases/22-fit-finish-code/22-VALIDATION.md
@.claude/rules/coding-patterns.md

@src/app/(gp)/assets/page.tsx
@src/app/(gp)/entities/page.tsx
@src/app/(gp)/meetings/page.tsx
@src/lib/pagination.ts
@src/app/api/assets/route.ts
@src/app/(gp)/assets/[id]/page.tsx
@src/components/features/entities/tabs/entity-cap-table-tab.tsx
@src/app/api/deals/[id]/route.ts
@src/app/api/ai/execute/route.ts
@src/app/api/ai/suggest-tasks/route.ts
@src/app/api/ai/draft-lp-update/route.ts
@src/app/api/documents/[id]/extract/route.ts

<interfaces>
<!-- Centralized error messages. Matches the planner-discretion decision. -->

New file src/lib/error-messages.ts:
```typescript
export const ERR = {
  // Deal stage-gate — descriptive, tells user what to do next
  DEAL_STAGE_GATE: (stage: string) =>
    `Can't delete a deal in ${stage} stage. Move it to Dead first, then delete from there.`,

  // AI prerequisites
  AI_NO_KEY: "AI features require an API key. Go to Settings → AI Config to configure OpenAI or Anthropic.",
  AI_NO_ACCESS: "AI access is not enabled for your account. Contact your firm administrator.",
  AI_MODEL_UNAVAILABLE: "The configured AI model is currently unavailable. Try another model in Settings → AI Config.",

  // Document extraction
  DOC_EXTRACT_NO_AI: "Document AI summary requires AI access. Go to Settings → AI Config to enable it.",
  DOC_EXTRACT_NO_FILE: "This document has no attached file to extract from.",
} as const;
```

Sort state pattern (from src/app/(gp)/assets/page.tsx — reuse shape):
```typescript
type SortKey = "name" | "date" | /* type-specific */;
type SortDir = "asc" | "desc";
const [sortKey, setSortKey] = useState<SortKey>("name");
const [sortDir, setSortDir] = useState<SortDir>("asc");
const handleSort = (k: SortKey) => {
  if (sortKey === k) setSortDir((d) => (d === "asc" ? "desc" : "asc"));
  else { setSortKey(k); setSortDir("asc"); }
};
const sorted = useMemo(() =>
  [...items].sort((a, b) => {
    const [av, bv] = [a[sortKey], b[sortKey]];
    const cmp = av < bv ? -1 : av > bv ? 1 : 0;
    return sortDir === "asc" ? cmp : -cmp;
  }), [items, sortKey, sortDir]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix list sort/filter on assets, entities, and meetings (FIN-10; Obs 8, 21, 44)</name>
  <files>
    src/app/(gp)/assets/page.tsx
    src/app/(gp)/entities/page.tsx
    src/app/(gp)/meetings/page.tsx
    src/lib/pagination.ts
    src/app/api/assets/route.ts
  </files>
  <action>
**Three list pages, three fixes.**

**1. Assets — asset class filter (Obs 8).**

RESEARCH.md: filter state and URL-building are present; the bug is likely in `parsePaginationParams` failing to extract `assetClass` from the query OR the SearchFilterBar not calling `handleFilterChange` correctly. Diagnose in ~5 minutes:

- Open `/assets` in the browser, click the Real Estate filter button, watch the Network tab.
- Is the request URL `&assetClass=REAL_ESTATE`? If NO, the client-side filter submission is broken (fix `SearchFilterBar` or the handler).
- If YES, is the API response already filtered? If NO, `parsePaginationParams` doesn't know about `assetClass` — extend it in `src/lib/pagination.ts` to include `assetClass` and `status` in its known-params list.

Apply the fix per the diagnosis. RESEARCH.md confirmed the API already reads `params.filters?.assetClass` — so the fix is most likely on the `parsePaginationParams` side. Keep the change minimal.

**2. Entities — column sort + search (Obs 21).**

RESEARCH.md: no sort state at all. Add the standard sort pattern (see interfaces block):
- Sort keys: `name`, `entityType`, `vintageYear`, `status`
- Make table headers clickable with a chevron indicator showing `sortKey` + `sortDir`
- Client-side sort (compatible with Phase 25 pagination's "Load more" — sort accumulated array)

Also add search — RESEARCH.md notes search is absent. Add a `<SearchFilterBar>` (same component used on assets page) with a search input. Extend `buildUrl` to pass `search` param to `/api/entities`. Verify the API reads the search param.

**3. Meetings — sort (Obs 44).**

Already has `SearchFilterBar` with search + source filter. Add sort by date (default) and by title. Reuse the sort pattern. No new search/filter — existing are sufficient per RESEARCH.md.

**Shared constraint:** All sort is client-side so it survives Phase 25 pagination's "Load more" pattern (sort the accumulated array, not the current page).

Follow coding-patterns.md: SWR loading guards, `cn()` for conditional classes, keyed list items.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    Manual: (a) `/assets` — click Real Estate filter → list shows only RE assets; click Other Filters (status, etc.) → combinations work. (b) `/entities` — click Name column header → sorts A-Z, click again → Z-A. Repeat for entity type, vintage year, status. Search: type a partial name → list narrows. (c) `/meetings` — click Date column (or sort affordance) → reorders. Click Title → reorders alphabetically.
  </verify>
  <done>All three lists sort and filter as expected. No console errors. `npm run build` clean. Per-list evidence (screenshot or network-tab-log) recorded in VERIFICATION.md, one entry each for Obs 8, Obs 21, Obs 44.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire task + investor row links (FIN-11; Obs 18, 22, 27)</name>
  <files>
    src/app/(gp)/assets/[id]/page.tsx
    src/components/features/entities/tabs/entity-overview-tab.tsx
    src/components/features/entities/tabs/entity-cap-table-tab.tsx
  </files>
  <action>
**Three Link wraps. Uniform pattern.**

**1. Asset Tasks tab → /tasks/[id] (Obs 18).**

In `src/app/(gp)/assets/[id]/page.tsx`, find `AssetTasksTab`. Per RESEARCH.md, task rows are `<div key={t.id} className="flex items-center justify-between p-3 ...">`. Wrap the inner content (or the whole div):

```tsx
<Link key={t.id} href={`/tasks/${t.id}`} className="block ... hover:bg-gray-50 transition">
  {/* existing row content */}
</Link>
```

If the row has its own buttons (complete, delete), wrap only the informational area (title + date) in a `<Link>` and keep the action buttons as siblings with `e.stopPropagation()` on their onClick. Avoid nested interactive elements.

**2. Entity task widgets → /tasks/[id] (Obs 22).**

In `src/components/features/entities/tabs/entity-overview-tab.tsx` (or wherever the entity's task widget lives — verify by Grepping for "tasks" in the entity feature folder). Same Link wrap pattern.

**3. Cap Table investors → /investors/[id] (Obs 27).**

In `src/components/features/entities/tabs/entity-cap-table-tab.tsx`. Find the table body row that renders the investor name. Wrap the name cell:

```tsx
<td>
  <Link href={`/investors/${commitment.investor.id}`} className="text-blue-600 hover:underline">
    {commitment.investor.name}
  </Link>
</td>
```

Verify RESEARCH.md Open Question 5: does `Investor` have the data needed for the link? If the commitment.investor relationship exposes `.id`, the link is straightforward. If the investor is linked via `contactId` to `Contact`, use `/directory/[contactId]` instead. Check the route registry (`src/lib/routes.ts`) to confirm the investor detail route path; RESEARCH.md says `/investors/[id]`.

Follow coding-patterns.md: no nested `<button>`; keyed list items; `<Link>` is preferred over programmatic `router.push` for static navigations.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    Manual: (a) Open an asset with tasks → Tasks tab → click a task → `/tasks/[id]` loads. (b) Open an entity → Overview tab → click a task widget row → `/tasks/[id]` loads. (c) Open an entity → Cap Table tab → click an investor name → `/investors/[id]` loads.
  </verify>
  <done>All three link paths navigate correctly. No nested-button hydration errors. `npm run build` clean. Per-item screenshots recorded in VERIFICATION.md, one entry each for Obs 18, Obs 22, Obs 27.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Centralized error-message module + FIN-09 error-copy cleanup (Obs 3, 39)</name>
  <files>
    src/lib/error-messages.ts
    src/app/api/ai/execute/route.ts
    src/app/api/ai/suggest-tasks/route.ts
    src/app/api/ai/draft-lp-update/route.ts
    src/app/api/documents/[id]/extract/route.ts
    src/components/features/deals/deal-delete-button.tsx
    src/app/api/deals/[id]/route.ts
  </files>
  <action>
**Create the error-message module first, then swap every catch-all "Unauthorized" to use it.**

**1. `src/lib/error-messages.ts`** — exact content from the interfaces block above. Export `ERR` as `const`.

**2. AI routes — swap "Unauthorized" for actionable copy:**

In each of `src/app/api/ai/execute/route.ts`, `src/app/api/ai/suggest-tasks/route.ts`, `src/app/api/ai/draft-lp-update/route.ts`, find the check that returns `{ error: "Unauthorized" }` (RESEARCH.md confirmed these are NOT real auth failures — they check for AI key presence or `aiAccess` flag).

Replace with:
```typescript
import { ERR } from "@/lib/error-messages";

// If the check is "no AI key configured":
return NextResponse.json({ error: ERR.AI_NO_KEY }, { status: 400 });

// If the check is "aiAccess flag is false":
return NextResponse.json({ error: ERR.AI_NO_ACCESS }, { status: 403 });
```

Read each route carefully to determine which condition applies. If the check is a real auth failure (no session / invalid user), keep the 401 and use a proper unauthorized helper — do NOT touch real auth checks.

**3. Document AI summary — `src/app/api/documents/[id]/extract/route.ts`:**

Per RESEARCH.md, this route's `unauthorized()` call may be a real auth check but the downstream `extractDocumentFields` in `src/lib/document-extraction.ts` is where the missing-AI-key condition hides. Trace the failure path:
- If the AI key check is in `extractDocumentFields`, catch the specific error there and return `{ error: ERR.DOC_EXTRACT_NO_AI }` from the route rather than passing through a 401.
- If the check is in the route itself, same pattern.

**4. Deal delete — client-side error surfacing (Obs 3):**

Per RESEARCH.md, the API already returns a descriptive message (`"Cannot delete: deal is in X stage..."`). The user saw "Unauthorized" because the client-side delete handler shows a generic message. Find the component that calls DELETE on the deal (likely in `src/components/features/deals/` or the deals list page).

Apply the coding-patterns.md toast-string pattern:
```typescript
const res = await fetch(`/api/deals/${id}`, { method: "DELETE" });
if (!res.ok) {
  const data = await res.json().catch(() => ({}));
  const msg = typeof data.error === "string" ? data.error : "Failed to delete deal";
  toast.error(msg);
  return;
}
```

Also — optional refinement — update the API's deal-delete stage-gate error to use `ERR.DEAL_STAGE_GATE(deal.stage)` from the module so the copy is consistent. This is a Claude-discretion optimization; the current copy is already descriptive per RESEARCH.md.

**5. Audit sweep — `grep -rn "Unauthorized" src/app/api/`:**

Review every hit. Confirm each remaining one is a real auth failure (401 for missing session, 403 for explicit deny by role). Flag any remaining non-auth "Unauthorized" in VERIFICATION.md for follow-up.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -rn "Unauthorized\|Not authorized" src/app/api/ 2>&1 | head -30</automated>
    Manual: (a) Attempt to delete a deal in IC Review stage → toast shows the descriptive stage-gate message. (b) Open a document → AI Summary → (with no AI key configured) see actionable copy pointing to Settings → AI Config. (c) Trigger AI Execute with no key → see AI_NO_KEY message. For each, confirm copy matches the ERR module exactly.
  </verify>
  <done>All four ERR surfaces return actionable copy. Grep result shows only real auth failures remaining. Client-side deal delete shows API's descriptive message. `npm run build` clean. Per-item evidence in VERIFICATION.md, one entry each for Obs 3, Obs 39, plus one entry for each AI route covered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Diagnose Obs 7 deletion bypass + document finding</name>
  <files>
    (read-only) src/app/api/deals/[id]/route.ts
    (read-only) src/lib/deal-stage-engine.ts
    (possibly) src/components/features/deals/ delete flow component
  </files>
  <action>
**Investigation + documentation task — code change only if a real bypass is found.**

Per RESEARCH.md: the DELETE handler correctly enforces stage-gate (only SCREENING/DEAD allowed). The walkthrough user said they "killed an IC Review deal" — the most likely explanation is the two-step flow: PATCH `action:"kill"` moves the deal to DEAD, then DELETE is legitimate.

**Steps:**

1. **Reproduce the walkthrough scenario in dev:**
   - Create or identify an IC Review deal.
   - Click the three-dot menu → Delete (or whatever the UI affords).
   - Watch Network. Does a PATCH hit first, then DELETE? Or does DELETE succeed directly?

2. **If two-step (PATCH then DELETE):**
   - This is not a bypass — it's the intended flow. The fix is UI copy: the "Kill Deal" action should explicitly say "Move deal to Dead" (or similar) so users don't experience it as a hidden bypass. Apply this copy fix in the three-dot menu component.

3. **If DELETE succeeds directly on IC Review:**
   - There IS a bypass. Trace how: admin privilege bypass? `force=true` query param? Read the handler carefully. Fix by removing the bypass OR explicitly tightening the stage-gate.

4. **Document the finding in a diagnostic note** (copied into `22-06-VERIFICATION.md`):
   - Which scenario applied (1, 2, or 3 above)
   - Exact reproduction steps
   - What code change (if any) was made
   - What user-facing UX improvement (if any) was applied

Follow coding-patterns.md if applying a code change: `parseBody`, Zod, firm-scope.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    Manual: Attempt to delete an IC Review deal via the UI. Confirm either (a) the stage-gate blocks with a descriptive message, OR (b) if the flow is "Kill → Dead → Delete", each step is labeled explicitly so the user understands the sequence.
  </verify>
  <done>Obs 7 diagnosed. Written finding in VERIFICATION.md (dedicated "Obs 7" entry with reproduction steps + resolution). Either the bypass is closed with a code change OR the intended flow is clarified with UI copy. No surprise deletions of IC Review / Closing deals.</done>
</task>

</tasks>

<verification>
Per VALIDATION.md: `npm run build` every commit; the FIN-09 audit `grep` is itself an automated check. `npx vitest run` after Task 4 to confirm no regressions in deal-related tests.

**Scope-density rule (per W3 acknowledgment above):** VERIFICATION.md MUST contain a dedicated, named checklist entry for EACH of the items below — do NOT roll items together. The higher file count demands higher evidence granularity.

Manual checks for VERIFICATION.md (one entry each, explicit evidence):
- Obs 8 / FIN-10 — Asset class filter works: screenshot or network log
- Obs 21 / FIN-10 — Entity list sort + search work: screenshot per sort column
- Obs 44 / FIN-10 — Meetings sort works: screenshot
- Obs 18 / FIN-11 — Asset task row → /tasks/[id]: screenshot of URL after click
- Obs 22 / FIN-11 — Entity task widget → /tasks/[id]: screenshot of URL after click
- Obs 27 / FIN-11 — Cap Table investor → /investors/[id]: screenshot of URL after click
- Obs 3 / FIN-09 — Deal delete stage-gate copy shown: screenshot of toast message
- Obs 39 / FIN-09 — AI summary actionable copy: screenshot of message
- FIN-09 — AI execute actionable copy: test-harness or manual trigger evidence
- FIN-09 — AI suggest-tasks actionable copy: same
- FIN-09 — AI draft-lp-update actionable copy: same
- FIN-09 — Grep audit result: terminal output, one line per remaining "Unauthorized" in `src/app/api/` with justification (real auth failure vs. follow-up)
- Obs 7 — Deletion bypass diagnosis: dedicated diagnostic paragraph with reproduction steps
</verification>

<success_criteria>
1. `/assets` — Real Estate class filter renders only RE assets.
2. `/entities` — column headers sort; search narrows list.
3. `/meetings` — date and title sort.
4. Task rows on assets and entities navigate to `/tasks/[id]`.
5. Cap table investor names navigate to `/investors/[id]`.
6. Deal delete on non-SCREENING/DEAD shows the descriptive stage-gate message.
7. AI routes and document extraction return actionable copy, not "Unauthorized."
8. `src/lib/error-messages.ts` exports `ERR` with at least DEAL_STAGE_GATE, AI_NO_KEY, AI_NO_ACCESS, DOC_EXTRACT_NO_AI entries.
9. Grep audit finds no non-auth "Unauthorized" strings in `src/app/api/`.
10. Obs 7 deletion bypass diagnosed and documented.
11. VERIFICATION.md has 13+ distinct checklist entries (one per item listed above), each with its own evidence block — no rolled-up "passed all checks" lines.
12. `npm run build` clean. `npx vitest run` green.
</success_criteria>

<output>
After completion, create `.planning/phases/22-fit-finish-code/22-06-VERIFICATION.md` with: screenshots for each of the 10+ manual verification items (one dedicated section per item per the scope-density rule), grep output for FIN-09 audit with per-match justification, Obs 7 diagnostic paragraph with reproduction steps and resolution, list of any remaining "Unauthorized" strings deemed acceptable (with justification for each).
</output>
