---
phase: 13-deal-desk-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(gp)/deals/page.tsx
  - src/app/api/deals/route.ts
  - src/app/(gp)/deals/[id]/page.tsx
  - src/app/api/deals/[id]/route.ts
autonomous: true
requirements:
  - DEAL-11
  - DEAL-12
  - DEAL-13

must_haves:
  truths:
    - "Each kanban card displays the number of days the deal has been in its current stage"
    - "Each kanban column header shows the deal count and aggregate deal value for that stage"
    - "A closed deal's detail page has a visible 'View Asset' link that navigates to the asset created from that deal"
  artifacts:
    - path: "src/app/api/deals/route.ts"
      provides: "daysInStage computed field per deal + pipelineAnalytics.valueByStage already present"
      contains: "daysInStage"
    - path: "src/app/(gp)/deals/page.tsx"
      provides: "Kanban cards with days-in-stage badge + column headers with count and value"
      contains: "daysInStage"
    - path: "src/app/api/deals/[id]/route.ts"
      provides: "sourceAssets relation included in deal detail response"
      contains: "sourceAssets"
    - path: "src/app/(gp)/deals/[id]/page.tsx"
      provides: "View Asset link in closed deal banner"
      contains: "View Asset"
  key_links:
    - from: "src/app/api/deals/route.ts"
      to: "DealActivity records"
      via: "Prisma include + computation"
      pattern: "daysInStage.*Math\\.floor"
    - from: "src/app/(gp)/deals/page.tsx"
      to: "src/app/api/deals/route.ts"
      via: "SWR fetch, reads daysInStage and pipelineAnalytics"
      pattern: "daysInStage|valueByStage"
    - from: "src/app/(gp)/deals/[id]/page.tsx"
      to: "/assets/"
      via: "Link component using sourceAssets[0].id"
      pattern: "sourceAssets.*View Asset"
---

<objective>
Add pipeline intelligence to the deal kanban board and closed deal detail page.

Purpose: GPs need at-a-glance pipeline health — how long deals sit in each stage, total value per stage, and quick navigation from closed deals to their resulting assets.

Output: Enhanced kanban cards with days-in-stage, column headers with deal count + aggregate value, and a "View Asset" link on closed deal pages.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deal-desk-crm/13-CONTEXT.md
@.planning/phases/13-deal-desk-crm/13-RESEARCH.md

Key files to read before implementing:
- src/app/(gp)/deals/page.tsx (existing kanban board)
- src/app/api/deals/route.ts (deal list API)
- src/app/(gp)/deals/[id]/page.tsx (deal detail page with closed deal banner)
- src/app/api/deals/[id]/route.ts (deal detail API — needs sourceAssets include)
- src/app/api/analytics/pipeline/route.ts (has existing daysInStage computation to replicate)

<interfaces>
<!-- Key patterns from the codebase the executor needs -->

Days-in-stage computation (from src/app/api/analytics/pipeline/route.ts):
```typescript
const stageEntry = deal.activities.find(
  (a) => a.activityType.includes("STAGE") && (a.metadata as any)?.newStage === deal.stage
);
const enteredAt = stageEntry ? new Date(stageEntry.createdAt) : new Date(deal.createdAt);
const daysInStage = Math.max(0, Math.floor((now.getTime() - enteredAt.getTime()) / (1000 * 60 * 60 * 24)));
```

Pipeline analytics already in /api/deals response:
```typescript
pipelineAnalytics: {
  valueByStage: Record<string, number>,  // e.g., { SCREENING: 45000000, DUE_DILIGENCE: 120000000 }
  stageDistribution: Record<string, number>,  // deal counts per stage
}
```

Deal model relation for assets created from deal:
```prisma
model Deal {
  sourceAssets  Asset[]  // Assets created when deal closed — NOT currently included in /api/deals/[id]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add daysInStage to deal list API and render on kanban cards + column headers</name>
  <files>src/app/api/deals/route.ts, src/app/(gp)/deals/page.tsx</files>
  <action>
**API change (src/app/api/deals/route.ts):**
1. Add `activities: { where: { activityType: { contains: "STAGE" } }, select: { activityType: true, createdAt: true, metadata: true }, orderBy: { createdAt: "desc" } }` to the Prisma include block for the deal list query.
2. After fetching deals, compute `daysInStage` for each deal using the same formula from analytics/pipeline/route.ts (see interfaces above). Add `daysInStage` as a computed field on each deal object in the response. Use the current date (`new Date()`) for the calculation.
3. Do NOT return the raw `activities` array to the client — only return the computed `daysInStage` number. Delete the activities from each deal object before returning: `const { activities, ...rest } = deal; return { ...rest, daysInStage };`
4. The `pipelineAnalytics.valueByStage` and `pipelineAnalytics.stageDistribution` already exist in the response — confirm they are present and use them.

**UI change (src/app/(gp)/deals/page.tsx):**
1. On each kanban card, add a small badge or text showing the days-in-stage. Position it on the card (e.g., bottom-right or below the deal name). Use a muted style for low numbers (< 14 days), a warning style (amber) for 14-30 days, and a danger style (red) for > 30 days. Example: `<span className="text-xs text-gray-400">12d</span>` or with color coding.
2. On each column header, display the deal count and aggregate value. The count is already available (number of deals filtered to that stage). The value comes from `pipelineAnalytics.valueByStage[stageName]`. Format the value using `fmt()` from `@/lib/utils`. Example column header: `Screening (3) | $45M`.
3. Ensure dark mode compatibility with `dark:` variants on any new text/badge elements.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>Every kanban card shows "Xd" days-in-stage badge with color coding (gray < 14d, amber 14-30d, red > 30d). Each column header shows "StageName (count) | $value". Build passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add View Asset link to closed deal detail page</name>
  <files>src/app/api/deals/[id]/route.ts, src/app/(gp)/deals/[id]/page.tsx</files>
  <action>
**API change (src/app/api/deals/[id]/route.ts):**
1. Read the existing GET handler. Find the Prisma `include` block.
2. Add `sourceAssets: { select: { id: true, name: true } }` to the include block. This fetches any assets that were created from this deal (the relation already exists in schema.prisma).
3. No other changes needed.

**UI change (src/app/(gp)/deals/[id]/page.tsx):**
1. Find the closed deal banner — the component that shows "This deal has been closed — the asset has been created and booked." (likely a green/emerald banner with `isClosed` condition).
2. Add a "View Asset" link next to or within the banner text. Use Next.js `Link` component: `<Link href={/assets/${deal.sourceAssets?.[0]?.id}} className="text-emerald-800 font-semibold hover:underline">View Asset &rarr;</Link>`.
3. Only show the link if `deal.sourceAssets?.length > 0`. If no asset exists (deal closed but asset not yet created), do not show the link.
4. If multiple assets exist from a single deal, link to the first one (this is the common case — one deal creates one asset).
5. Ensure dark mode compatibility on the link styling.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>A closed deal's detail page shows a "View Asset" link in the closed deal banner that navigates to /assets/{assetId}. The link only appears when sourceAssets exist. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors
2. Navigate to /deals — kanban cards show days-in-stage badges; column headers show count + value
3. Navigate to a closed deal's detail page — "View Asset" link visible in banner, clicking navigates to the asset
</verification>

<success_criteria>
- Every active kanban card displays a days-in-stage counter
- Column headers show deal count and aggregate $ value per stage
- Closed deal detail pages link directly to their resulting asset
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-deal-desk-crm/13-01-SUMMARY.md`
</output>
