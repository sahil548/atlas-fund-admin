---
phase: 13-deal-desk-crm
plan: 04
type: execute
wave: 2
depends_on:
  - "13-01"
  - "13-02"
  - "13-03"
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/lib/schemas.ts
  - src/lib/constants.ts
  - src/lib/routes.ts
  - src/app/api/contacts/[id]/route.ts
  - src/app/api/contacts/[id]/interactions/route.ts
  - src/app/api/contacts/[id]/interactions/[interactionId]/route.ts
  - src/app/api/contacts/[id]/tags/route.ts
  - src/app/(gp)/contacts/[id]/page.tsx
  - src/components/features/contacts/contact-header-card.tsx
  - src/components/features/contacts/contact-activity-tab.tsx
  - src/components/features/contacts/contact-deals-tab.tsx
  - src/components/features/contacts/contact-connections-tab.tsx
  - src/app/(gp)/directory/page.tsx
autonomous: true
requirements:
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04

must_haves:
  truths:
    - "Contact detail page at /contacts/[id] shows header card with name, title, company, email, phone, contact type badge, relationship tags, quick stats, and initials avatar"
    - "Contact activity tab shows a unified chronological timeline of manual interactions and auto-linked deal events, filterable by type"
    - "GP can log interactions (Call, Email, Meeting, Note) via an inline form at the top of the activity timeline"
    - "Interactions are editable and deletable, showing who logged them"
    - "Contacts can be tagged with relationship types (predefined + custom) displayed as badges"
    - "Contact detail page shows all linked deals, entities, and assets"
    - "Directory table rows are clickable and navigate to /contacts/[id]"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ContactInteraction and ContactTag models with back-relations"
      contains: "model ContactInteraction"
    - path: "src/app/(gp)/contacts/[id]/page.tsx"
      provides: "Contact detail page with header + tabbed sections"
      min_lines: 80
    - path: "src/components/features/contacts/contact-header-card.tsx"
      provides: "Header card component with name, company link, badges, stats, avatar"
      min_lines: 40
    - path: "src/components/features/contacts/contact-activity-tab.tsx"
      provides: "Unified timeline with inline log form + type filter"
      min_lines: 80
    - path: "src/app/api/contacts/[id]/route.ts"
      provides: "GET contact detail with all relations (interactions, tags, deals, entities, assets)"
      exports: ["GET"]
    - path: "src/app/api/contacts/[id]/interactions/route.ts"
      provides: "CRUD endpoints for ContactInteraction"
      exports: ["GET", "POST"]
    - path: "src/app/api/contacts/[id]/interactions/[interactionId]/route.ts"
      provides: "PUT and DELETE endpoints for individual ContactInteraction"
      exports: ["PUT", "DELETE"]
    - path: "src/app/api/contacts/[id]/tags/route.ts"
      provides: "Add/remove contact relationship tags"
      exports: ["POST", "DELETE"]
    - path: "src/components/features/contacts/contact-deals-tab.tsx"
      provides: "Linked deals, entities, and assets sections for contact detail"
      contains: "Assets|assets"
  key_links:
    - from: "src/app/(gp)/contacts/[id]/page.tsx"
      to: "src/app/api/contacts/[id]/route.ts"
      via: "SWR fetch for contact detail data"
      pattern: "api/contacts.*useSWR"
    - from: "src/components/features/contacts/contact-activity-tab.tsx"
      to: "src/app/api/contacts/[id]/interactions/route.ts"
      via: "SWR fetch for timeline + POST for logging"
      pattern: "api/contacts.*interactions"
    - from: "src/components/features/contacts/contact-deals-tab.tsx"
      to: "src/app/api/contacts/[id]/route.ts"
      via: "Reads deals, entities, and assets from contact detail response"
      pattern: "linkedAssets|assets"
    - from: "src/app/(gp)/directory/page.tsx"
      to: "/contacts/"
      via: "Row onClick navigates to contact detail"
      pattern: "router\\.push.*contacts"
---

<objective>
Build the CRM foundation — new Prisma models, contact detail page with activity timeline, interaction logging, relationship tags, and linked deals/entities/assets display.

Purpose: The GP needs a single view of each contact's full relationship context — interactions, deals, entities, assets, and team connections — turning the flat directory into a relationship intelligence tool.

Output: ContactInteraction and ContactTag models, /contacts/[id] detail page with header card + 4 tabs, interaction CRUD APIs, tag management, and clickable directory rows.

Note: This plan is Wave 2 because it runs `prisma db push --force-reset` which resets the database. Pipeline enhancement plans (01-03) must complete first so they can be verified against the current database before the reset.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deal-desk-crm/13-CONTEXT.md
@.planning/phases/13-deal-desk-crm/13-RESEARCH.md

Key files to read before implementing:
- prisma/schema.prisma (Contact model, Deal model, User model — add new models + back-relations)
- prisma/seed.ts (add seed data for interactions and tags)
- src/app/(gp)/companies/[id]/page.tsx (reference pattern for detail page with header + stats)
- src/app/(gp)/deals/[id]/page.tsx (reference pattern for tabbed detail page)
- src/components/features/deals/deal-activity-tab.tsx (reference pattern for activity timeline + inline log form)
- src/app/(gp)/directory/page.tsx (existing directory — add row click navigation)
- src/lib/routes.ts (add contact detail route)
- src/lib/constants.ts (add RELATIONSHIP_TAGS)

<interfaces>
<!-- Existing Contact model fields (from schema.prisma — verify exact fields): -->
<!-- Contact has: id, firmId, firstName, lastName, email, phone, title, company (relation), contactType, etc. -->
<!-- Verify exact field names before implementing -->

ContactInteraction model to add:
```prisma
model ContactInteraction {
  id        String   @id @default(cuid())
  firmId    String
  contactId String
  authorId  String
  type      String   // CALL, EMAIL, MEETING, NOTE
  notes     String
  date      DateTime @default(now())
  dealId    String?
  entityId  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  firm      Firm     @relation(fields: [firmId], references: [id])
  contact   Contact  @relation(fields: [contactId], references: [id], onDelete: Cascade)
  author    User     @relation(fields: [authorId], references: [id])
  deal      Deal?    @relation(fields: [dealId], references: [id])
  entity    Entity?  @relation(fields: [entityId], references: [id])
  @@index([contactId])
  @@index([firmId])
  @@index([authorId])
}
```

ContactTag model to add:
```prisma
model ContactTag {
  id        String  @id @default(cuid())
  firmId    String
  contactId String
  tag       String
  contact   Contact @relation(fields: [contactId], references: [id], onDelete: Cascade)
  firm      Firm    @relation(fields: [firmId], references: [id])
  @@unique([contactId, tag])
  @@index([firmId])
}
```

Predefined relationship tags:
```typescript
export const RELATIONSHIP_TAGS = [
  "Broker", "Co-Investor", "LP (current)", "LP Prospect",
  "Advisor", "Board Member", "Service Provider"
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CRM Prisma models, seed data, schemas, and API routes</name>
  <files>prisma/schema.prisma, prisma/seed.ts, src/lib/schemas.ts, src/lib/constants.ts, src/app/api/contacts/[id]/route.ts, src/app/api/contacts/[id]/interactions/route.ts, src/app/api/contacts/[id]/interactions/[interactionId]/route.ts, src/app/api/contacts/[id]/tags/route.ts</files>
  <action>
**Schema changes (prisma/schema.prisma):**
1. Read the full schema to understand existing Contact, Deal, User, Entity, Firm models.
2. Add `ContactInteraction` model (see interfaces above). Ensure back-relations are added to: Contact (interactions ContactInteraction[]), User (contactInteractions ContactInteraction[]), Deal (contactInteractions ContactInteraction[]), Entity (contactInteractions ContactInteraction[]), Firm (contactInteractions ContactInteraction[]).
3. Add `ContactTag` model (see interfaces above). Ensure back-relations: Contact (tags ContactTag[]), Firm (contactTags ContactTag[]).
4. IMPORTANT: Check if adding relations to User, Deal, Entity, and Firm models causes naming conflicts with existing relations. Use explicit relation names if needed (e.g., `@relation("ContactInteractionAuthor")`).
5. Run schema validation in your head before proceeding — every foreign key must have a back-relation on the referenced model.

**After schema changes, run:**
```bash
PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION="yes" npx prisma db push --force-reset
npx prisma generate
npx prisma db seed
```

**Seed data (prisma/seed.ts):**
1. Add seed interactions for 3-5 contacts (2-4 interactions each): mix of CALL, EMAIL, MEETING, NOTE types. Use existing seeded user IDs as authorId.
2. Add seed tags for 5+ contacts with 1-3 tags each from the predefined list.
3. Place these seeds AFTER contact creation in the seed script execution order.

**Constants (src/lib/constants.ts):**
1. Add `RELATIONSHIP_TAGS` array: `["Broker", "Co-Investor", "LP (current)", "LP Prospect", "Advisor", "Board Member", "Service Provider"]`.
2. Add `INTERACTION_TYPES` array: `["CALL", "EMAIL", "MEETING", "NOTE"]`.
3. Add `INTERACTION_TYPE_LABELS` and `INTERACTION_TYPE_COLORS` Record maps for UI display.

**Zod schemas (src/lib/schemas.ts):**
1. Add `CreateInteractionSchema`:
```typescript
z.object({
  type: z.enum(["CALL", "EMAIL", "MEETING", "NOTE"]),
  notes: z.string().min(1, "Notes required"),
  date: z.string().optional(),
  dealId: z.string().optional(),
  entityId: z.string().optional(),
})
```
2. Add `UpdateInteractionSchema` (same fields, all optional except id).
3. Add `ContactTagSchema`: `z.object({ tag: z.string().min(1) })`.

**API: Contact detail (src/app/api/contacts/[id]/route.ts):**
1. Check if this file already exists. If so, update the GET handler to include all new relations. If not, create it.
2. GET handler: Fetch contact by id + firmId with includes:
   - `company: true` (linked company)
   - `tags: true` (relationship tags)
   - `interactions: { include: { author: true, deal: { select: { id: true, name: true } }, entity: { select: { id: true, name: true } } }, orderBy: { date: "desc" } }` (recent interactions)
   - Include deals this contact is linked to (check how contacts are linked to deals — via DealTeam? via company? Verify in schema).
   - Include entities this contact is linked to.
   - **CRITICAL for CRM-04 — Include linked assets:** Assets are linked to contacts through deals. Query assets via deals the contact is associated with. Use one of these approaches (verify which works with the schema):
     - Option A: If deals have a `sourceAssets` relation, include `sourceAssets` on each linked deal so the contact detail response contains `deal.sourceAssets[]`.
     - Option B: Separately query `prisma.asset.findMany({ where: { sourceDeal: { OR: [{ team: { some: { userId: ... } } }, other contact-deal link] } } })` and attach as `linkedAssets`.
     - Option C (simplest): When including linked deals, also include `{ sourceAssets: { select: { id: true, name: true, type: true } } }` on each deal include. The frontend can then flatten `contact.linkedDeals.flatMap(d => d.sourceAssets)` to display assets.
   - Choose the approach that best fits the existing schema. The key requirement is that the API response contains enough data to render an "Assets" section on the contact page.
3. Also compute quick stats: `dealCount`, `entityCount`, `assetCount` (count of assets linked through deals), `lastInteractionDate` (from most recent interaction).
4. Return enriched contact object.

**API: Interactions CRUD (src/app/api/contacts/[id]/interactions/route.ts):**
1. GET: List interactions for contactId, ordered by date desc. Include author (for avatar/name display). Filter by firmId.
2. POST: Create interaction using `CreateInteractionSchema`. Set `authorId` from authenticated user (use `getAuthUser()` or similar pattern from existing routes). Set `firmId` and `contactId` from params/query.

**API: Individual Interaction (src/app/api/contacts/[id]/interactions/[interactionId]/route.ts):**
1. PUT: Update an individual interaction. Accept partial update using `UpdateInteractionSchema`. Verify the interaction belongs to the correct contactId and firmId.
2. DELETE: Delete an individual interaction. Verify ownership (contactId + firmId) before deleting.

**API: Tags (src/app/api/contacts/[id]/tags/route.ts):**
1. POST: Add a tag to a contact. Use `ContactTagSchema`. Create `ContactTag` record with firmId, contactId, tag. Handle P2002 (duplicate) gracefully — return 409 "Tag already exists".
2. DELETE: Remove a tag. Accept `{ tag: string }` in body or query param. Delete the ContactTag record matching contactId + tag.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>ContactInteraction and ContactTag models exist in schema.prisma with all back-relations. Seed data populates sample interactions and tags. API routes for contact detail (including linked assets), interaction CRUD (list/create + edit/delete), and tag management all work. Zod schemas validate input. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build contact detail page with header card, activity timeline, and deals/connections tabs</name>
  <files>src/app/(gp)/contacts/[id]/page.tsx, src/components/features/contacts/contact-header-card.tsx, src/components/features/contacts/contact-activity-tab.tsx, src/components/features/contacts/contact-deals-tab.tsx, src/components/features/contacts/contact-connections-tab.tsx, src/lib/routes.ts, src/app/(gp)/directory/page.tsx</files>
  <action>
**Route registration (src/lib/routes.ts):**
1. Add contact detail route to `APP_ROUTES`:
```typescript
{ path: "/contacts/:id", label: "Contact Detail", description: "Contact relationship intelligence and activity history", keywords: ["contact", "crm", "interactions", "relationship"], icon: "User", portal: "gp", priority: 0 }
```

**Contact detail page (src/app/(gp)/contacts/[id]/page.tsx):**
1. Create page with `"use client"` directive.
2. Fetch contact data via SWR: `useSWR(\`/api/contacts/${id}?firmId=${firmId}\`, fetcher)`.
3. Add loading guard: `if (isLoading || !data) return <Loading />`.
4. Layout: ContactHeaderCard at top, then tab bar below with 4 tabs:
   - **Activity** (default) — unified timeline + inline log form
   - **Deals** — sourced deals + co-investments (Plan 05 will populate co-investments)
   - **Entities** — linked entities
   - **Team Connections** — which GP members have interacted
5. Use the same tab pattern as deals/[id]/page.tsx: `useState` for active tab, tab buttons with active/inactive styling, conditional panel rendering.
6. Add a "Back to Directory" breadcrumb/link at the top.

**Contact header card (src/components/features/contacts/contact-header-card.tsx):**
1. Initials avatar: circle with first letter of first + last name, colored background.
2. Name (large, bold) + title + company name (as clickable Link to `/companies/${companyId}`).
3. Email (mailto link) + phone.
4. Contact type badge (from existing contactType field).
5. Relationship tags as badges — map `contact.tags` to Badge components. Include an "Add Tag" button that opens a dropdown with predefined RELATIONSHIP_TAGS + custom text input.
6. Quick stats row: Deal count, Entity count, Asset count, Last interaction date.
7. Tag management: clicking "Add Tag" shows dropdown of predefined tags (excluding already-applied ones) + "Custom..." option. Tags are removable (X button on each badge). Add/remove calls POST/DELETE to `/api/contacts/${id}/tags`.
8. Dark mode: all elements have dark: variants.

**Activity tab (src/components/features/contacts/contact-activity-tab.tsx):**
1. **Inline log form at top** (same pattern as deal-activity-tab.tsx):
   - Type selector (Call, Email, Meeting, Note) — use radio buttons or segmented control.
   - Notes textarea.
   - Optional deal/entity link dropdowns (fetch deals and entities via SWR for the dropdowns).
   - Date picker (defaults to today).
   - Submit button: POST to `/api/contacts/${id}/interactions`.
   - On success: revalidate interactions SWR key, clear form, show toast.
2. **Type filter** below form: "All", "Calls", "Emails", "Meetings", "Notes", "Deal Events" filter buttons.
3. **Unified timeline** below filter:
   - Fetch manual interactions from `/api/contacts/${id}/interactions`.
   - Auto-linked deal events: The contact detail API should return relevant DealActivity records for deals this contact is linked to. If the API does not return these yet, fetch them separately or add to the contact detail endpoint. Normalize both sources to a common `{ timestamp, source, type, description, author?, deal?, entity? }` shape and sort by timestamp desc.
   - Each timeline item shows: type icon/badge, description/notes, author initials + timestamp, linked deal/entity name (clickable).
   - Manual interactions show edit (pencil icon) and delete (trash icon) buttons. Edit opens inline edit mode or modal. Delete shows ConfirmDialog.
   - Auto-linked events are read-only (no edit/delete).
   - Group by day with date headers (e.g., "March 8, 2026") for readability.
4. Empty state: "No interactions yet. Log your first interaction above."

**Deals tab (src/components/features/contacts/contact-deals-tab.tsx):**
1. "Deals Sourced" section: List deals where `deal.sourcedByContactId === contact.id`. For now, this will be empty until Plan 05 adds the `sourcedByContactId` field. Show a placeholder or "No sourced deals" message.
2. "Linked Deals" section: Show deals this contact is associated with (via DealTeam, company association, or co-investor — check how contacts link to deals in the existing schema). Show deal name (clickable link to /deals/[id]), stage badge, deal value.
3. **"Linked Assets" section (CRM-04):** Show assets linked to this contact through their associated deals. The contact detail API response includes asset data on linked deals (e.g., `deal.sourceAssets`). Flatten all assets across the contact's linked deals and display them in a table/list. Each row shows: asset name (clickable link to /assets/[id]), asset type badge, and the deal it came from (clickable link to /deals/[id]). Empty state: "No linked assets."
4. "Entities" section: Show entities this contact is linked to. Show entity name (clickable link to /entities/[id]), type badge.

**Team connections tab (src/components/features/contacts/contact-connections-tab.tsx):**
1. Aggregate which GP team members have logged interactions with this contact.
2. Group interactions by `authorId` — count interactions per team member.
3. Display each team member with: avatar, name, interaction count, last interaction date.
4. Sort by interaction count descending (most engaged team member first).
5. If no interactions exist, show "No team connections recorded yet."

**Directory page update (src/app/(gp)/directory/page.tsx):**
1. Find the contacts tab/table in the directory page.
2. Make each contact row clickable: add `onClick={() => router.push(\`/contacts/${contact.id}\`)}` and `className="cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-800"` to each `<tr>`.
3. Use `e.stopPropagation()` on any existing action buttons (Edit, Delete) in the row to prevent navigation when clicking those buttons.
4. Import `useRouter` from `next/navigation` if not already imported.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>Contact detail page exists at /contacts/[id] with header card (avatar, name, company link, badges, tags, stats including asset count) + 4 tabs (Activity, Deals, Entities, Team Connections). Activity tab has inline log form + filterable timeline. Tags are manageable via badges. Deals tab shows linked deals, linked assets, and entities. Directory rows are clickable. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors
2. Navigate to /directory — click a contact row — navigates to /contacts/[id]
3. Contact detail page shows header card with all info + relationship tag badges
4. Activity tab: log an interaction (select type, enter notes, submit) — appears in timeline
5. Activity tab: edit and delete an interaction — works correctly
6. Activity tab: filter by type — timeline filters correctly
7. Tag management: add a predefined tag — badge appears; remove a tag — badge disappears
8. Deals tab: shows linked deals, linked assets (via deals), and entities
9. Team Connections tab: shows which GP members have logged interactions
</verification>

<success_criteria>
- /contacts/[id] page renders with full header card and 4 functional tabs
- Interaction logging works end-to-end (create, edit, delete)
- Timeline merges manual interactions and auto-linked events chronologically
- Relationship tags can be added (predefined + custom) and removed
- Contact detail page shows linked assets through deal associations (CRM-04)
- Directory contact rows navigate to detail page on click
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-deal-desk-crm/13-04-SUMMARY.md`
</output>
