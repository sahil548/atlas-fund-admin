---
phase: 13-deal-desk-crm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pdf/ic-memo.tsx
  - src/app/(gp)/deals/[id]/page.tsx
  - src/app/api/analytics/pipeline/route.ts
  - src/app/(gp)/analytics/page.tsx
  - src/app/(gp)/deals/page.tsx
autonomous: true
requirements:
  - DEAL-14
  - DEAL-15

must_haves:
  truths:
    - "GP can download the IC memo as a professionally formatted PDF from the deal detail page"
    - "Dead deal analytics chart shows kill reason breakdown on the /analytics page"
    - "Top 3 kill reasons are summarized on the /deals pipeline page"
  artifacts:
    - path: "src/lib/pdf/ic-memo.tsx"
      provides: "IC memo PDF template using @react-pdf/renderer"
      min_lines: 60
    - path: "src/app/(gp)/deals/[id]/page.tsx"
      provides: "Export PDF button on deal detail page (IC Review or Overview tab)"
      contains: "Download.*PDF|Export.*Memo"
    - path: "src/app/api/analytics/pipeline/route.ts"
      provides: "Kill reason breakdown in pipeline analytics response"
      contains: "killReason"
    - path: "src/app/(gp)/analytics/page.tsx"
      provides: "Dead deal kill reason chart (horizontal bar chart)"
      contains: "killReason|Kill Reason"
    - path: "src/app/(gp)/deals/page.tsx"
      provides: "Mini summary of top 3 kill reasons in pipeline stats area"
      contains: "killReason|Kill Reason"
  key_links:
    - from: "src/app/(gp)/deals/[id]/page.tsx"
      to: "src/lib/pdf/ic-memo.tsx"
      via: "Client-side pdf() call to generate blob"
      pattern: "pdf.*ICMemoPDF|toBlob"
    - from: "src/app/(gp)/analytics/page.tsx"
      to: "src/app/api/analytics/pipeline/route.ts"
      via: "SWR fetch, reads killReasonBreakdown from response"
      pattern: "killReason"
---

<objective>
Add IC memo PDF export and dead deal analytics to the deal pipeline.

Purpose: GPs need to share IC memos with external parties (co-investors, advisors) as professional PDFs, and need visibility into why deals fail to identify pipeline patterns.

Output: A downloadable IC memo PDF from deal detail pages, a kill reason breakdown chart on /analytics, and a top-3 kill reason mini-summary on /deals.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-deal-desk-crm/13-CONTEXT.md
@.planning/phases/13-deal-desk-crm/13-RESEARCH.md

Key files to read before implementing:
- src/lib/pdf/quarterly-report.tsx (reference PDF template pattern)
- src/lib/pdf/shared-styles.ts (shared PDF styles to reuse)
- src/components/features/deals/deal-overview-tab.tsx (IC memo display — understand memo JSON structure)
- src/app/api/deals/[id]/dd-analyze/route.ts (memo generation — understand screeningResult.memo structure)
- src/app/(gp)/analytics/page.tsx (existing analytics charts — add kill reason chart)
- src/app/api/analytics/pipeline/route.ts (pipeline analytics API — add kill reason breakdown)
- src/app/(gp)/deals/page.tsx (pipeline page — add mini kill reason summary)

<interfaces>
<!-- IC memo data comes from deal.screeningResult.memo -->
<!-- Read deal-overview-tab.tsx to understand exact JSON structure before building PDF -->
<!-- The PDF must be generated client-side using @react-pdf/renderer pdf() function -->

Existing PDF pattern (from quarterly-report.tsx):
```typescript
import { Document, Page, Text, View } from "@react-pdf/renderer";
import { styles as s, formatDate } from "./shared-styles";

export function QuarterlyReport({ data }: { data: QuarterlyReportData }) {
  return (
    <Document title={`Report Title`} author="Fund Administration Platform">
      <Page size="A4" style={s.page}>
        {/* Content */}
      </Page>
    </Document>
  );
}
```

Client-side PDF download pattern:
```typescript
import { pdf } from "@react-pdf/renderer";
const blob = await pdf(<ICMemoPDF data={memoData} />).toBlob();
const url = URL.createObjectURL(blob);
const a = document.createElement("a");
a.href = url;
a.download = `IC_Memo_${deal.name}.pdf`;
a.click();
URL.revokeObjectURL(url);
```

Existing Recharts pattern (from analytics/page.tsx):
```typescript
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from "recharts";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IC memo PDF template and add export button to deal detail</name>
  <files>src/lib/pdf/ic-memo.tsx, src/app/(gp)/deals/[id]/page.tsx</files>
  <action>
**Step 1: Understand memo structure.**
Read `src/components/features/deals/deal-overview-tab.tsx` to understand how the IC memo is currently displayed. Find the structure of `deal.screeningResult.memo` — it likely contains sections with titles and content. Also read `src/app/api/deals/[id]/dd-analyze/route.ts` to confirm the memo JSON structure.

**Step 2: Create PDF template (src/lib/pdf/ic-memo.tsx).**
1. Import from `@react-pdf/renderer`: Document, Page, Text, View, StyleSheet.
2. Import shared styles: `import { styles as s, formatDate } from "./shared-styles"`.
3. Define `ICMemoData` interface based on the memo JSON structure discovered in step 1. At minimum it should include: `dealName: string`, `recommendation: string`, `executiveSummary: string`, `sections: Array<{ title: string; content: string }>`, `generatedAt: string`.
4. Create the `ICMemoPDF` component following the quarterly-report.tsx pattern:
   - Page 1: Header with deal name (large, bold), recommendation badge (APPROVE/REJECT/DEFER with colored background), generation date.
   - Executive summary section.
   - Each memo section rendered with section title (bold) + content (body text).
   - Footer with "Confidential" notice.
5. Style using `StyleSheet.create()` — NOT Tailwind classes (react-pdf does not support Tailwind). Reference shared-styles.ts for consistent typography, margins, colors.
6. Professional formatting: appropriate margins (30-40pt), readable font sizes (10-12pt body, 16-20pt headers), section spacing.

**Step 3: Add export button to deal detail page (src/app/(gp)/deals/[id]/page.tsx).**
1. Find where the IC memo is displayed (likely on the overview tab or IC review tab).
2. Add a "Download PDF" or "Export Memo" button near the memo display area. Use a button with a download icon (FileDown from Lucide).
3. On click, extract the memo data from `deal.screeningResult.memo`, construct the `ICMemoData` object, call `pdf(<ICMemoPDF data={memoData} />).toBlob()`, then trigger download using `URL.createObjectURL`.
4. Only show the button when `deal.screeningResult?.memo` exists (memo has been generated).
5. Add loading state on the button while PDF is generating.
6. IMPORTANT: The `pdf()` import from `@react-pdf/renderer` must be dynamic-imported or lazy-loaded if it causes SSR issues. If build fails with SSR errors, wrap the PDF generation in a `typeof window !== 'undefined'` check or use `dynamic` import.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>IC memo PDF template exists at src/lib/pdf/ic-memo.tsx. Deal detail page has an "Export PDF" button that generates and downloads a professional PDF of the IC memo. Button only appears when memo data exists. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add dead deal analytics — kill reason chart and mini-summary</name>
  <files>src/app/api/analytics/pipeline/route.ts, src/app/(gp)/analytics/page.tsx, src/app/(gp)/deals/page.tsx</files>
  <action>
**API change (src/app/api/analytics/pipeline/route.ts):**
1. Read the existing pipeline analytics route to understand its response structure.
2. Add kill reason breakdown computation to the response:
   - Filter deals where `stage === "DEAD"` (or equivalent dead/killed status).
   - Group by `killReason` field. Count deals per reason. Handle null/undefined killReason as "Unknown".
   - Sort by count descending.
   - Add to response object: `killReasonBreakdown: Array<{ reason: string; count: number }>`.
3. This data is consumed by both the /analytics page (full chart) and /deals page (top 3 summary).

**Analytics page chart (src/app/(gp)/analytics/page.tsx):**
1. Read the existing analytics page to understand its layout and chart patterns.
2. Add a new section/card: "Dead Deal Analysis" or "Kill Reason Breakdown".
3. Add a horizontal bar chart using Recharts `BarChart` with `layout="vertical"`:
   - XAxis: type="number" (count of deals)
   - YAxis: type="category" dataKey="reason" width={120} tick={{ fontSize: 11 }}
   - Bar: dataKey="count" fill="#ef4444" (red) radius={[0, 4, 4, 0]}
   - Wrap in ResponsiveContainer with height={Math.max(200, killReasonData.length * 40)}
4. Show empty state if no dead deals exist: "No dead deals to analyze."
5. Add total dead deal count as a header stat.
6. Dark mode: ensure chart text colors work with dark background.

**Pipeline page mini-summary (src/app/(gp)/deals/page.tsx):**
1. The /deals page likely has a stats/summary area above or beside the kanban. Find the pipeline stats section.
2. Add a small "Top Kill Reasons" summary showing the top 3 reasons with counts. Format as a compact list or inline badges. Example: "Pricing (5) | Risk (3) | Timing (2)".
3. The data comes from the same SWR response that fetches deals (if pipelineAnalytics includes killReasonBreakdown) OR from a separate SWR call to `/api/analytics/pipeline`. Check which approach the existing code uses and be consistent.
4. If the deals list API response already includes pipelineAnalytics, add `killReasonBreakdown` to that response instead of duplicating the computation. If not, use the analytics API call.
5. Only show the mini-summary if dead deals exist.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>Kill reason breakdown chart visible on /analytics page as a horizontal bar chart. Top 3 kill reasons shown in the pipeline stats area on /deals page. API returns killReasonBreakdown data. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors
2. Navigate to a deal with an IC memo — "Export PDF" button visible, downloads a professional PDF
3. Navigate to /analytics — dead deal kill reason chart visible with horizontal bars
4. Navigate to /deals — top 3 kill reasons shown in pipeline stats area
</verification>

<success_criteria>
- IC memo downloads as a clean, professional PDF document suitable for external sharing
- Dead deal analytics chart groups deals by kill reason on /analytics page
- Top 3 kill reasons are summarized on the /deals pipeline page
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-deal-desk-crm/13-02-SUMMARY.md`
</output>
