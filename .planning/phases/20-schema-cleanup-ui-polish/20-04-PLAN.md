---
phase: 20-schema-cleanup-ui-polish
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-03]
files_modified:
  - prisma/schema.prisma
  - src/lib/json-schemas.ts
autonomous: true
requirements: [SCHEMA-03, SCHEMA-04]

must_haves:
  truths:
    - "Database indexes exist on all high-query FK columns (firmId, entityId, dealId, status)"
    - "Orphaned enum values and dead fields are documented with comments"
    - "Schema changes applied successfully via db push"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Optimized schema with indexes and cleanup"
      contains: "@@index"
  key_links:
    - from: "prisma/schema.prisma"
      to: "database"
      via: "prisma db push"
      pattern: "@@index"
---

<objective>
Add missing database indexes on high-query columns and audit the schema for orphaned or mistyped fields.

Purpose: Performance optimization (queries on firmId, status, entityId without indexes are full table scans) and data integrity (dead enum values and orphaned fields accumulate confusion).

Output: Updated schema.prisma with new indexes. Seed data re-applied after force-reset.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
@.planning/phases/20-schema-cleanup-ui-polish/20-RESEARCH.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing database indexes</name>
  <files>prisma/schema.prisma</files>
  <action>
1. Read schema.prisma and catalog all existing @@index and @@unique declarations.

2. Add indexes on these identified high-query columns (from RESEARCH.md analysis):
   - `Asset`: Add `@@index([firmId])` and `@@index([firmId, status])` — assets always queried with firmId filter
   - `CapitalCall`: Add `@@index([status])` and `@@index([entityId, status])` — overdue call queries filter by status
   - `Deal`: Add `@@index([firmId, stage])` — composite for common pipeline query pattern
   - `Document`: Add `@@index([firmId])` — documents always queried with firmId filter
   - `DealActivity`: Add `@@index([dealId, type])` if not present — activity feed filters
   - `AuditLog`: Add `@@index([firmId, createdAt])` if not present — audit log time-range queries
   - `Task`: Add `@@index([firmId])` and `@@index([contextType, contextId])` if not present — task context queries
   - `Meeting`: Add `@@index([firmId])` if not present

3. Do NOT add indexes that already exist — check each model's existing @@index declarations first.

4. Do NOT remove any existing indexes.

5. Add comments above each new index explaining what query pattern it optimizes:
   ```prisma
   // Optimizes: GET /api/assets?firmId=xxx&status=ACTIVE
   @@index([firmId, status])
   ```
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx prisma validate 2>&1 | tail -5</automated>
  </verify>
  <done>All new indexes added to schema.prisma. Prisma validates successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Audit orphaned fields + apply schema migration</name>
  <files>prisma/schema.prisma</files>
  <action>
1. Schema audit — check for orphaned/dead fields:
   - `SyncStatus.SYNCING` — verify no code sets this value: `grep -rn "SYNCING" src/ --include="*.ts"`. If unused, add comment: `// SYNCING — no code path sets this value; retained for backward compatibility`
   - `InviteStatus.PENDING` — verify usage: `grep -rn "PENDING" src/ --include="*.ts"`. If only referenced in schema, add comment: `// PENDING — invite workflow not yet implemented; retained for future use`
   - `DealCategory` — per RESEARCH.md: already removed (confirmed by comment at line 97). Verify the comment exists.
   - Check for any nullable fields that are ALWAYS null in code (never written to). Document with comments if found.

2. Per CONTEXT.md locked decision: "Orphaned fields/models: Claude's discretion — remove clearly dead fields via migration, document ambiguous ones for future cleanup"
   - Document (add comments to) ambiguous fields rather than removing enum values that may have data
   - If a field is clearly unused AND never persisted data (e.g., an optional field added but never populated), it can be removed
   - Use conservative judgment — prefer documenting over removing

3. Apply the schema changes:
   ```bash
   PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION="yes" npx prisma db push --force-reset
   npx prisma generate
   npx prisma db seed
   ```

4. After seed completes, verify the dev server can start and serve data:
   - `npm run build` must succeed
   - All tests must pass

IMPORTANT: After force-reset, the AiConfig table is wiped. This is expected and documented in CLAUDE.md. The tenant AI key must be re-entered in Settings after migration.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -5 && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>Schema migrated with indexes. Orphaned fields documented with comments. Seed data applied. Build and tests pass. Note: AiConfig wiped — re-enter tenant AI key in Settings.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` — schema valid
- `npm run build` — zero errors
- `npm run test` — all tests pass
- New indexes visible in schema.prisma
- Orphaned fields documented with comments
</verification>

<success_criteria>
- All high-query FK columns have database indexes
- Orphaned fields documented with inline comments
- Schema migration applied (force-reset + seed)
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-04-SUMMARY.md`
</output>
