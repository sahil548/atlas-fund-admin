---
phase: 20-schema-cleanup-ui-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/logger.ts
  - src/lib/json-schemas.ts
  - src/lib/__tests__/logger.test.ts
  - src/lib/__tests__/json-schemas.test.ts
  - src/lib/__tests__/bug03-timeout.test.ts
  - src/app/api/lp/__tests__/dashboard.test.ts
autonomous: true
requirements: [INTEG-03, INTEG-04, INTEG-06, SCHEMA-01]

must_haves:
  truths:
    - "Logger utility exists with error/warn/info/debug levels"
    - "All 39 JSON blob fields have Zod schemas defined"
    - "LP dashboard test mock is fixed and all 7 tests pass"
    - "Bug-03 timeout test verifies API returns 504 on timeout"
  artifacts:
    - path: "src/lib/logger.ts"
      provides: "Structured logger utility"
      exports: ["logger"]
    - path: "src/lib/json-schemas.ts"
      provides: "Zod schemas for all JSON blob fields"
      min_lines: 100
    - path: "src/lib/__tests__/logger.test.ts"
      provides: "Logger unit tests"
    - path: "src/lib/__tests__/json-schemas.test.ts"
      provides: "JSON schema validation tests"
    - path: "src/lib/__tests__/bug03-timeout.test.ts"
      provides: "Timeout behavior test"
    - path: "src/app/api/lp/__tests__/dashboard.test.ts"
      provides: "Fixed LP dashboard tests"
  key_links:
    - from: "src/lib/logger.ts"
      to: "console.*"
      via: "wrapper functions"
      pattern: "logger\\.(error|warn|info|debug)"
    - from: "src/lib/json-schemas.ts"
      to: "prisma/schema.prisma"
      via: "Zod schemas matching Json fields"
      pattern: "Schema.*z\\.object"
---

<objective>
Wave 0 foundation: Create the logger utility, define Zod schemas for all 39 JSON blob fields, fix the LP dashboard test mock, and write test scaffolds for bug-03 timeout behavior.

Purpose: These are prerequisite artifacts that all subsequent plans depend on. The logger must exist before console.log migration (Plan 02). The JSON schemas must exist before API route Zod validation (Plan 04). The LP dashboard test fix unblocks a clean test baseline.

Output: 4 new files + 1 fixed test file. All tests green after this plan.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
@.planning/phases/20-schema-cleanup-ui-polish/20-RESEARCH.md
@prisma/schema.prisma
@src/app/api/lp/__tests__/dashboard.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create logger utility + JSON blob Zod schemas with tests</name>
  <files>src/lib/logger.ts, src/lib/json-schemas.ts, src/lib/__tests__/logger.test.ts, src/lib/__tests__/json-schemas.test.ts</files>
  <action>
1. Create `src/lib/logger.ts` — zero-dependency structured logger:
   - `const isDev = process.env.NODE_ENV !== "production";`
   - Export `logger` object with `error(msg, meta?)`, `warn(msg, meta?)`, `info(msg, meta?)`, `debug(msg, meta?)` methods
   - `error` and `warn` always log (both dev and prod)
   - `info` and `debug` only log when `isDev === true`
   - Use `console.error` for error, `console.warn` for warn, `console.log` for info/debug
   - Prefix with `[ERROR]`, `[WARN]`, `[INFO]`, `[DEBUG]` respectively
   - Type `meta` as `Record<string, unknown> | string | number | undefined`
   - Keep it simple — follow the exact pattern from RESEARCH.md code example

2. Create `src/lib/json-schemas.ts` — Zod schemas for ALL 39 Json/Json? fields in schema.prisma:
   - Read schema.prisma to identify every field with type `Json` or `Json?`
   - For each, create a named Zod schema. Use `z.object({...}).nullable()` for nullable fields
   - Group by model with comments (e.g., `// === Deal model ===`)
   - Key schemas to define (from RESEARCH.md + schema inspection):
     - `DealMetadataSchema` — Deal.dealMetadata
     - `NavProxyConfigSchema` — Entity.navProxyConfig
     - `RegulatoryFilingsSchema` — Entity.regulatoryFilings (array of filing objects)
     - `MeetingDecisionsSchema` — Meeting.decisions
     - `ExtractedFieldsSchema` — Document.extractedFields
     - `AppliedFieldsSchema` — Document.appliedFields
     - `ProjectedMetricsSchema` — Asset.projectedMetrics
     - `WaterfallConfigSchema` — WaterfallTemplate config fields
     - `SideLetterAdjustmentsSchema` — SideLetter.adjustments
     - `UserPermissionsSchema` — User.permissions
     - All remaining Json fields from schema.prisma
   - Use `z.record(z.string(), z.unknown())` for genuinely dynamic fields (per Zod 4 syntax requirement)
   - Export all schemas as named exports

3. Create `src/lib/__tests__/logger.test.ts`:
   - Test that `logger.error` and `logger.warn` call console.error/warn in both dev and prod
   - Test that `logger.info` and `logger.debug` only call console.log when NODE_ENV !== "production"
   - Mock console methods with vi.spyOn
   - Test with and without meta parameter

4. Create `src/lib/__tests__/json-schemas.test.ts`:
   - For each JSON schema: test that valid data parses successfully, test that invalid data fails
   - Test nullable schemas accept null
   - Test at least 10 of the 39 schemas explicitly (prioritize DealMetadata, NavProxyConfig, MeetingDecisions, ExtractedFields, UserPermissions)
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>logger.ts exports logger with 4 methods. json-schemas.ts exports Zod schemas for all 39 JSON blob fields. Both test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix LP dashboard test mock + create bug-03 timeout test</name>
  <files>src/app/api/lp/__tests__/dashboard.test.ts, src/lib/__tests__/bug03-timeout.test.ts</files>
  <action>
1. Fix `src/app/api/lp/__tests__/dashboard.test.ts`:
   - Read the existing test file to find the Prisma mock object
   - Add `findMany: vi.fn().mockResolvedValue([])` to the `metricSnapshot` mock object
   - This fixes the 7 failing tests where `prisma.metricSnapshot.findMany is not a function`
   - Do NOT change any other test logic — only fix the mock
   - Verify all 7 tests now pass

2. Create `src/lib/__tests__/bug03-timeout.test.ts`:
   - Test the timeout pattern that will be used to fix BUG-03 (IC memo spinner hang)
   - Create a test utility function `raceWithTimeout(promise, timeoutMs)` that wraps Promise.race
   - Test cases:
     - Promise resolves before timeout: returns resolved value
     - Promise exceeds timeout: throws "TIMEOUT" error
     - Promise rejects: throws original error (not timeout)
   - This test validates the pattern before applying it in Plan 02
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All LP dashboard tests pass (7 previously failing). Bug-03 timeout test passes. Full test suite green (741+ passing, 0 failing).</done>
</task>

</tasks>

<verification>
- `npm run test` — all tests pass (0 failures)
- `src/lib/logger.ts` exists and exports `logger`
- `src/lib/json-schemas.ts` exists and exports schemas for all 39 JSON fields
- LP dashboard tests pass without error
</verification>

<success_criteria>
- Full test suite green (0 failing tests)
- Logger utility ready for migration in Plan 02
- JSON schemas ready for wiring into API routes in Plan 04
- Bug-03 timeout pattern tested and ready for application in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-01-SUMMARY.md`
</output>
