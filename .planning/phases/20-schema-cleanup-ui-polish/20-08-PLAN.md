---
phase: 20-schema-cleanup-ui-polish
plan: 08
type: execute
wave: 5
depends_on: [20-06]
files_modified:
  - src/components/ui/modal.tsx
  - src/components/ui/select.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/button.tsx
autonomous: true
requirements: [UIPOL-01, UIPOL-02]

must_haves:
  truths:
    - "Modal component has smooth open/close animation and focus management"
    - "Select component renders custom dropdown instead of native HTML select"
    - "Both components work correctly in dark mode"
    - "Tabs active state has dark mode styling"
    - "Button has ghost variant"
  artifacts:
    - path: "src/components/ui/modal.tsx"
      provides: "Upgraded modal with animation"
      min_lines: 40
    - path: "src/components/ui/select.tsx"
      provides: "Custom select dropdown"
      min_lines: 60
    - path: "src/components/ui/tabs.tsx"
      provides: "Tabs with dark mode active state"
    - path: "src/components/ui/button.tsx"
      provides: "Button with ghost variant"
  key_links:
    - from: "src/components/ui/modal.tsx"
      to: "all pages using Modal"
      via: "import { Modal }"
      pattern: "import.*Modal.*from.*ui/modal"
    - from: "src/components/ui/select.tsx"
      to: "all pages using Select"
      via: "import { Select }"
      pattern: "import.*Select.*from.*ui/select"
---

<objective>
Upgrade the 4 weakest UI primitives to shadcn/ui-inspired designs: modal (animation + focus), select (custom dropdown), tabs (dark mode), button (ghost variant).

Purpose: Per locked CONTEXT.md decision: "Replace the weakest UI primitives (modals, selects, dropdowns, date pickers) with shadcn/ui-inspired alternatives." The current modal has no animation or focus trap. The select is a native HTML element. Both fail the $50K SaaS visual quality bar.

Output: 4 upgraded UI components. Zero functionality regressions. All existing consumers work without changes (backward-compatible API).
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
@.planning/phases/20-schema-cleanup-ui-polish/20-RESEARCH.md
@src/components/ui/modal.tsx
@src/components/ui/select.tsx
@src/components/ui/tabs.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade modal with animation + focus management</name>
  <files>src/components/ui/modal.tsx</files>
  <action>
1. Read the current `src/components/ui/modal.tsx` to understand its props interface and existing consumers.

2. Upgrade the modal while maintaining the EXACT same props interface (backward compatible):
   - **Backdrop animation:** Fade in/out with CSS transitions. Use Tailwind: `transition-opacity duration-200` on the backdrop div. Start with `opacity-0`, animate to `opacity-100`.
   - **Panel animation:** Slide up + fade in with CSS transitions. Use Tailwind: `transition-all duration-200 ease-out` on the content div. Start with `opacity-0 translate-y-4 scale-95`, animate to `opacity-100 translate-y-0 scale-100`.
   - **Focus management:** On open, auto-focus the first focusable element inside the modal (input, button, or textarea). Use `useEffect` with `ref.current?.querySelector("input, button, [tabindex], textarea")?.focus()`.
   - **Escape key close:** Add `useEffect` with keydown listener for Escape key. Call `onClose()` on Escape press.
   - **Click outside close:** onClick handler on backdrop calls `onClose()` (may already exist — verify and keep).
   - **Dark mode:** Ensure the modal panel has `bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700`.
   - **Close button:** Style the X button with `hover:bg-gray-100 dark:hover:bg-gray-800 rounded-lg p-1 transition-colors`.
   - **Shadow:** Add `shadow-xl` to the content panel.
   - **Animation implementation:** Use React state with `useEffect` to toggle between closed/open states with a small delay for CSS transitions. Pattern:
     ```tsx
     const [visible, setVisible] = useState(false);
     useEffect(() => { if (isOpen) requestAnimationFrame(() => setVisible(true));
       else setVisible(false); }, [isOpen]);
     ```
   - Keep the `createPortal` pattern for rendering in document body.

3. Do NOT change the props interface. Any component importing Modal must work without changes.

4. Verify with `npm run build` — no type errors.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Modal has smooth open/close animation, auto-focuses first element, closes on Escape, has dark mode support. All existing Modal consumers work without changes.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade select to custom dropdown + tabs dark mode + button ghost</name>
  <files>src/components/ui/select.tsx, src/components/ui/tabs.tsx, src/components/ui/button.tsx</files>
  <action>
1. **Select upgrade** — replace native HTML select with custom dropdown:
   - Read current `select.tsx` to understand its props interface
   - Build a custom dropdown component following the RESEARCH.md code example:
     - Trigger button shows selected value with ChevronDown icon
     - Dropdown renders below trigger with `absolute z-50` positioning
     - Options list with hover states (`hover:bg-gray-50 dark:hover:bg-gray-700`)
     - Selected option highlighted with indigo text
     - Close on outside click (useRef + mousedown event listener)
     - Close on Escape key
     - Keyboard navigation: ArrowDown/ArrowUp to move between options, Enter to select
     - Dark mode: `bg-white dark:bg-gray-800`, `border-gray-200 dark:border-gray-600`
     - Disabled state with `opacity-50 cursor-not-allowed`
     - Focus ring: `focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-400`
   - CRITICAL: Maintain backward compatibility with existing props interface. If the old Select used `<option>` children, the new one must accept the same props OR accept `options` array. Read the current interface carefully.
   - If existing consumers pass children (native select pattern), the new Select may need to accept both `options` prop AND children. Or, update all consumers if the old API was simple enough. Prefer maintaining backward compatibility.

2. **Tabs upgrade** — add dark mode to active state:
   - Read current `tabs.tsx`
   - Add `dark:bg-indigo-500 dark:text-white` (or equivalent) to the active tab state
   - Ensure inactive tabs also have dark mode: `dark:text-gray-400 dark:hover:text-gray-200`
   - Ensure the tabs container has dark mode background: `dark:bg-gray-800`

3. **Button ghost variant** — add ghost variant:
   - Read current `button.tsx` to understand variant system
   - Add a `ghost` variant: transparent background, text color only, hover with subtle background
   - Light mode: `text-gray-600 hover:bg-gray-100 hover:text-gray-900`
   - Dark mode: `dark:text-gray-300 dark:hover:bg-gray-800 dark:hover:text-white`
   - This is useful for icon-only buttons and toolbar actions

4. Run `npm run build` — verify no type errors. All existing component consumers must still work.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Select renders custom dropdown with keyboard nav. Tabs active state works in dark mode. Button has ghost variant. All existing consumers work. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` — zero errors
- All Modal, Select, Tabs, Button consumers compile without changes
- Modal has animation and focus management
- Select renders custom dropdown, not native HTML
- Tabs active state visible in dark mode
- Button has ghost variant
</verification>

<success_criteria>
- 4 UI primitives upgraded to shadcn/ui-inspired design
- All upgrades backward-compatible (no consumer changes needed)
- Dark mode works on all upgraded components
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-08-SUMMARY.md`
</output>
