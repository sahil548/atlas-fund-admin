---
phase: 20-schema-cleanup-ui-polish
plan: 10
type: execute
wave: 6
depends_on: [20-07, 20-08, 20-09]
files_modified: []
autonomous: false
requirements: [INTEG-01, INTEG-02, INTEG-03, INTEG-04, INTEG-05, INTEG-06, INTEG-07, INTEG-08, INTEG-09, SCHEMA-01, SCHEMA-02, SCHEMA-03, SCHEMA-04, UIPOL-01, UIPOL-02, UIPOL-03, UIPOL-04, UIPOL-05, UIPOL-06]

must_haves:
  truths:
    - "All 3 bugs are fixed and verified by human"
    - "UI components look polished in both light and dark mode"
    - "All LP pages render correctly in dark mode"
    - "Modal animates smoothly on open/close"
    - "Select renders custom dropdown, not native HTML"
    - "Build passes with zero errors"
    - "All tests pass with zero failures"
  artifacts: []
  key_links: []
---

<objective>
Final human verification checkpoint for Phase 20. The user tests all bug fixes, UI upgrades, and dark mode across the application.

Purpose: This is the v2.0 quality gate. Every change from Plans 01-09 is verified by the user in the browser before declaring Phase 20 complete.

Output: User approval or list of issues for gap closure.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Final Phase 20 human verification</name>
  <files></files>
  <action>
Present the user with a comprehensive verification checklist for all Phase 20 changes.

What was built across Plans 01-09:

Track 1 — Integration Audit:
- BUG-01: DD progress no longer shows 0% for post-DD deals
- BUG-02: Pipeline conversion rate capped at 100%
- BUG-03: IC memo generation returns error on timeout (no hanging spinner)
- Structured logger replaces all 210+ console.log/warn/error calls
- Dead code cleaned up (unused imports, commented-out code)
- 4 end-to-end workflows verified (deal->asset, capital calls->LP metrics, AI command bar, dashboard)

Track 2 — Schema Hardening:
- Zod schemas defined for all 39 JSON blob fields
- All API routes use parseBody with Zod validation (no raw req.json())
- Database indexes added on high-query columns (firmId, status, entityId composites)
- Orphaned fields documented with comments
- pdf-parse properly typed, as any count reduced

Track 3 — UI Polish:
- Modal: smooth animation, focus management, Escape close
- Select: custom dropdown with keyboard nav (not native HTML)
- Tabs: dark mode active state
- Button: ghost variant
- Full dark mode audit on all LP and GP pages
- SectionErrorBoundary on data-fetching sections

How to verify:

Start the dev server: npm run dev

1. Bug fixes (5 min):
- Navigate to /deals, open any deal past DD stage (IC_REVIEW or CLOSING)
- Click the DD tab — progress should NOT show 0% or NOT_STARTED (BUG-01)
- Go to /analytics — pipeline conversion rates should never exceed 100% (BUG-02)
- Open any deal, click "Generate IC Memo" — if it times out, you should see an error toast (not an infinite spinner) (BUG-03)

2. UI component upgrades (3 min):
- Open any modal (e.g., create a deal, kill a deal) — verify smooth animation on open/close
- Press Escape — modal should close
- Click any dropdown/select — verify it shows a custom styled dropdown, not native browser select
- Verify the dropdown has hover states and closes on click-outside

3. Dark mode full pass (5 min):
- Toggle dark mode (top-right icon or system preference)
- Navigate to each LP page: /lp-dashboard, /lp-activity, /lp-portfolio, /lp-settings, /lp-documents, /lp-tax — every page should have dark backgrounds, light text, no jarring white rectangles
- Navigate to GP pages: /dashboard, /deals, /assets, /entities, /tasks, /contacts, /documents, /meetings, /analytics, /transactions, /settings, /profile, /reports — spot-check for hardcoded white backgrounds
- Open a deal detail page in dark mode — check each tab

4. Build verification:
- Run npm run build — should complete with zero errors
- Run npm run test — should show all tests passing, zero failures

Resume signal: Type "approved" to complete Phase 20, or describe any issues found.
  </action>
  <verify>User types "approved" or describes issues for gap closure</verify>
  <done>User confirms Phase 20 complete. All 3 tracks verified. v2.0 quality gate passed.</done>
</task>

</tasks>

<verification>
- User confirms all bug fixes working
- User confirms UI polish meets quality bar
- User confirms dark mode working across all pages
- Build and tests pass
</verification>

<success_criteria>
- User types "approved"
- Zero known bugs remaining
- v2.0 ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-10-SUMMARY.md`
</output>
