---
phase: 20-schema-cleanup-ui-polish
plan: 07
type: execute
wave: 3
depends_on: [20-02, 20-04, 20-05]
files_modified:
  - src/lib/document-extraction.ts
  - src/types/pdf-parse.d.ts
  - src/lib/deal-stage-engine.ts
  - src/lib/capital-activity-engine.ts
  - src/lib/ai-service.ts
  - src/app/api/dashboard/stats/route.ts
  - src/app/api/dashboard/entity-cards/route.ts
  - src/app/api/dashboard/portfolio-aggregates/route.ts
  - src/app/api/deals/[id]/route.ts
autonomous: true
requirements: [INTEG-05, INTEG-06, INTEG-07, INTEG-08, UIPOL-05, UIPOL-06]

must_haves:
  truths:
    - "Deal to asset transition works end-to-end"
    - "Capital call lifecycle produces correct LP metrics"
    - "AI command bar queries return relevant results"
    - "Dashboard aggregation numbers are accurate"
    - "pdf-parse import is properly typed"
    - "as any count reduced to minimum necessary"
  artifacts:
    - path: "src/lib/document-extraction.ts"
      provides: "Typed pdf-parse import"
    - path: "src/types/pdf-parse.d.ts"
      provides: "Type stub for pdf-parse"
  key_links:
    - from: "deal close"
      to: "asset creation"
      via: "sourceDealId relation"
      pattern: "sourceDealId"
    - from: "capital call funding"
      to: "LP dashboard metrics"
      via: "capital account computation"
      pattern: "computeCapitalAccount"
---

<objective>
End-to-end workflow verification across all major cross-module interactions, plus type safety cleanup (fix pdf-parse import, reduce `as any` usage).

Purpose: Per locked CONTEXT.md decision: "Full cross-module verification — trace key workflows end-to-end." After all bug fixes and schema changes, verify that the 4 critical workflows still work correctly. Also fix the pdf-parse typing and reduce `as any` count.

Output: All 4 workflows verified working. pdf-parse properly typed. `as any` count minimized.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
@.planning/phases/20-schema-cleanup-ui-polish/20-RESEARCH.md
@.planning/phases/20-schema-cleanup-ui-polish/20-02-SUMMARY.md
@.planning/phases/20-schema-cleanup-ui-polish/20-04-SUMMARY.md
@.planning/phases/20-schema-cleanup-ui-polish/20-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pdf-parse typing + reduce as any count</name>
  <files>src/lib/document-extraction.ts, src/types/pdf-parse.d.ts</files>
  <action>
1. Fix the pdf-parse untyped import (UIPOL-05):
   - Check if `@types/pdf-parse` exists: `npm ls @types/pdf-parse 2>&1`
   - If not available, create a type stub at `src/types/pdf-parse.d.ts`:
     ```typescript
     declare module "pdf-parse" {
       interface PDFData {
         numpages: number;
         numrender: number;
         info: Record<string, unknown>;
         metadata: Record<string, unknown> | null;
         text: string;
         version: string;
       }
       function pdfParse(dataBuffer: Buffer | ArrayBuffer | Uint8Array): Promise<PDFData>;
       export = pdfParse;
     }
     ```
   - Update `src/lib/document-extraction.ts` to remove the `as any` cast on the pdf-parse import
   - Verify the import works: `import pdfParse from "pdf-parse";` without `as any`

2. Reduce `as any` count (UIPOL-06):
   - First count current `as any` occurrences:
     ```bash
     grep -rn "as any" src/ --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "node_modules" | wc -l
     ```
   - Categorize each `as any` into:
     a. **Can fix now:** Type is known, just needs proper interface (API response shapes, Prisma includes)
     b. **Legitimate:** Truly dynamic content (AI responses, third-party API payloads) — per CONTEXT.md "allow runtime Zod validation for genuinely dynamic content"
     c. **Already fixed by Plan 05:** Document category `as any` removed by Zod migration
   - Fix category (a): Create explicit TypeScript interfaces for the most common `as any` usages
   - Leave category (b) with a comment: `// eslint-disable-next-line @typescript-eslint/no-explicit-any -- dynamic AI/external response`
   - Target: Reduce `as any` count by at least 50%

3. Run `npm run build` to confirm all type fixes compile correctly.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && echo "as any count:" && grep -rn "as any" src/ --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "node_modules" | wc -l && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>pdf-parse import is properly typed (no as any). Overall as any count reduced by at least 50%. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify 4 end-to-end workflows</name>
  <files>src/lib/deal-stage-engine.ts, src/lib/capital-activity-engine.ts, src/lib/ai-service.ts, src/app/api/dashboard/stats/route.ts, src/app/api/dashboard/entity-cards/route.ts, src/app/api/dashboard/portfolio-aggregates/route.ts, src/app/api/deals/[id]/route.ts</files>
  <action>
This is a verification task — minimal code changes expected. Trace each workflow through code to confirm correctness. Fix any issues found in the listed files.

1. **INTEG-05: Deal to asset transition**
   - Read `src/lib/deal-stage-engine.ts` — find the CLOSING -> CLOSED transition
   - Verify asset creation uses `sourceDealId` to link back to the deal
   - Read the close deal API (PATCH /api/deals/[id]) to confirm it creates the asset
   - Read the deal detail page to verify "View Asset" link uses `sourceAssets` relation
   - If any issue found: fix it. If working correctly: document verification in SUMMARY.

2. **INTEG-06: Capital call lifecycle -> LP metrics**
   - Read `src/lib/capital-activity-engine.ts` — trace: capital call creation -> line items -> mark funded -> capital account recomputation
   - Verify LP dashboard API reads capital accounts correctly (the test mock was fixed in Plan 01)
   - Read the LP dashboard page to verify metrics display
   - Check: does funding a capital call properly trigger capital account recalculation?
   - If any issue found: fix it. If working correctly: document verification in SUMMARY.

3. **INTEG-07: AI command bar -> all data queries**
   - Read `src/lib/ai-service.ts` — verify the searchAndAnalyze function queries across deals, assets, entities, capital calls
   - Verify the page context injection from Phase 18 is working (setPageContext on relevant pages)
   - Check that AI response rendering handles all response types
   - If any issue found: fix it. If working correctly: document verification in SUMMARY.

4. **INTEG-08: Dashboard aggregation accuracy**
   - Read dashboard API endpoints: `/api/dashboard/stats`, `/api/dashboard/entity-cards`, `/api/dashboard/portfolio-aggregates`
   - Verify pipeline summary uses correct count aggregation (Deal.targetSize is String, so totalValue=0 is documented quirk)
   - Verify alerts aggregate from correct sources (overdue capital calls, covenant breaches, lease expirations)
   - Verify activity feed works with entity and type filters
   - If any issue found: fix it. If working correctly: document verification in SUMMARY.

For each workflow, record in SUMMARY:
- Status: VERIFIED / FIXED / KNOWN_LIMITATION
- What was checked
- Any issues found and fixed
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build 2>&1 | tail -3 && npm run test 2>&1 | tail -3</automated>
  </verify>
  <done>All 4 workflows traced and verified. Any issues discovered are fixed. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` — zero errors
- `npm run test` — all tests pass
- pdf-parse import typed (no `as any`)
- `as any` count reduced significantly
- 4 workflow verifications documented in SUMMARY
</verification>

<success_criteria>
- All 4 end-to-end workflows verified working or fixed
- pdf-parse properly typed
- `as any` count reduced by at least 50%
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-07-SUMMARY.md`
</output>
