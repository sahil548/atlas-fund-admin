---
phase: 20-schema-cleanup-ui-polish
plan: 09
type: execute
wave: 3
depends_on: [20-06]
files_modified:
  - src/app/(lp)/lp-activity/page.tsx
  - src/app/(lp)/lp-portfolio/page.tsx
  - src/app/(lp)/lp-settings/page.tsx
  - src/app/(lp)/lp-dashboard/page.tsx
  - src/app/(lp)/lp-documents/page.tsx
  - src/app/(lp)/lp-tax/page.tsx
  - src/app/(gp)/deals/page.tsx
  - src/app/(gp)/deals/[id]/page.tsx
  - src/app/(gp)/assets/page.tsx
  - src/app/(gp)/assets/[id]/page.tsx
  - src/app/(gp)/entities/page.tsx
  - src/app/(gp)/entities/[id]/page.tsx
  - src/app/(gp)/dashboard/page.tsx
  - src/app/(gp)/tasks/page.tsx
  - src/app/(gp)/contacts/page.tsx
  - src/app/(gp)/contacts/[id]/page.tsx
  - src/app/(gp)/documents/page.tsx
  - src/app/(gp)/meetings/page.tsx
  - src/app/(gp)/analytics/page.tsx
  - src/app/(gp)/transactions/page.tsx
  - src/app/(gp)/settings/page.tsx
  - src/app/(gp)/profile/page.tsx
  - src/app/(gp)/reports/page.tsx
  - src/components/ui/error-boundary.tsx
autonomous: true
requirements: [UIPOL-03, UIPOL-04]

must_haves:
  truths:
    - "All LP pages render correctly in dark mode with no hardcoded bg-white"
    - "All GP pages render correctly in dark mode"
    - "Charts, badges, forms, and tables look correct in dark mode"
    - "SectionErrorBoundary wraps data-fetching sections on all pages"
  artifacts:
    - path: "src/app/(lp)/**/*.tsx"
      provides: "Dark mode LP pages"
    - path: "src/app/(gp)/**/*.tsx"
      provides: "Dark mode GP pages"
  key_links:
    - from: "src/app/(lp)/*.tsx"
      to: "dark mode classes"
      via: "Tailwind dark: prefix"
      pattern: "dark:"
    - from: "src/app/(gp)/**/*.tsx"
      to: "src/components/ui/error-boundary.tsx"
      via: "SectionErrorBoundary import"
      pattern: "SectionErrorBoundary"
---

<objective>
Full dark mode audit across all 26 GP pages and 6-8 LP pages, plus wire SectionErrorBoundary to data-fetching sections that lack it.

Purpose: Per locked CONTEXT.md decision: "Full dark mode audit — every page verified in dark mode, fix all contrast/readability issues." RESEARCH.md confirmed LP pages (lp-activity, lp-portfolio, lp-settings) have hardcoded `bg-white` without `dark:` variants. Also per CONTEXT.md: "Error boundaries on every page route" — PageErrorBoundary already wraps all pages via app-shell.tsx (per Pitfall 7), so the gap is SectionErrorBoundary on individual data sections.

Output: All pages dark-mode compatible. SectionErrorBoundary on data-fetching sections.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
@.planning/phases/20-schema-cleanup-ui-polish/20-RESEARCH.md
@src/components/ui/error-boundary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark mode audit and fix on LP pages</name>
  <files>src/app/(lp)/lp-activity/page.tsx, src/app/(lp)/lp-portfolio/page.tsx, src/app/(lp)/lp-settings/page.tsx, src/app/(lp)/lp-dashboard/page.tsx, src/app/(lp)/lp-documents/page.tsx, src/app/(lp)/lp-tax/page.tsx</files>
  <action>
1. For EACH LP page file, find all hardcoded light-mode classes and add dark: variants:

   **Background colors:**
   - `bg-white` -> `bg-white dark:bg-gray-900`
   - `bg-gray-50` -> `bg-gray-50 dark:bg-gray-800`
   - `bg-gray-100` -> `bg-gray-100 dark:bg-gray-800`

   **Text colors:**
   - `text-gray-900` -> `text-gray-900 dark:text-white`
   - `text-gray-700` -> `text-gray-700 dark:text-gray-300`
   - `text-gray-600` -> `text-gray-600 dark:text-gray-400`
   - `text-gray-500` -> `text-gray-500 dark:text-gray-400`
   - `text-gray-400` -> `text-gray-400 dark:text-gray-500`

   **Borders:**
   - `border-gray-200` -> `border-gray-200 dark:border-gray-700`
   - `border-gray-100` -> `border-gray-100 dark:border-gray-700`
   - `border-gray-50` -> `border-gray-50 dark:border-gray-700`

   **Hover states:**
   - `hover:bg-gray-50` -> `hover:bg-gray-50 dark:hover:bg-gray-800`
   - `hover:bg-gray-100` -> `hover:bg-gray-100 dark:hover:bg-gray-700`

   **Focus rings (keep light ring — they work in both):**
   - `focus:ring-indigo-500` — already works in dark mode, no change needed

2. Check for hardcoded colors in inline styles (unlikely but verify):
   ```bash
   grep -n "style=" src/app/\(lp\)/ --include="*.tsx" -r
   ```
   If any inline styles set colors, convert to Tailwind classes with dark: variants.

3. Check LP component imports — if LP pages import feature components, those may also need dark mode fixes. Check:
   - Components imported from `src/components/features/` by LP pages
   - Verify those components have dark: variants (they likely do from Phase 11 foundation work)

4. Run `npm run build` to verify no type errors from class changes.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && echo "bg-white without dark:" && grep -rn "bg-white\b" src/app/\(lp\)/ --include="*.tsx" | grep -v "dark:" | wc -l && echo "bg-gray-50 without dark:" && grep -rn "bg-gray-50\b" src/app/\(lp\)/ --include="*.tsx" | grep -v "dark:" | wc -l && echo "bg-gray-100 without dark:" && grep -rn "bg-gray-100\b" src/app/\(lp\)/ --include="*.tsx" | grep -v "dark:" | wc -l && echo "text-gray-900 without dark:" && grep -rn "text-gray-900\b" src/app/\(lp\)/ --include="*.tsx" | grep -v "dark:" | wc -l</automated>
  </verify>
  <done>All LP pages have dark: variants on every background, text, and border class. Zero hardcoded light-only classes without dark: variant.</done>
</task>

<task type="auto">
  <name>Task 2: Dark mode verification on GP pages + SectionErrorBoundary wiring</name>
  <files>src/app/(gp)/**/*.tsx, src/components/features/**/*.tsx</files>
  <action>
1. **GP pages dark mode audit:**
   Scan all GP pages for missing dark: variants:
   ```bash
   grep -rn "bg-white\b" src/app/\(gp\)/ --include="*.tsx" | grep -v "dark:" | head -30
   ```
   Fix any instances found using the same pattern as Task 1:
   - `bg-white` -> `bg-white dark:bg-gray-900`
   - `text-gray-900` without `dark:text-white` -> add it
   - `border-gray-200` without `dark:border-gray-700` -> add it

   Priority pages (most likely to have gaps):
   - `src/app/(gp)/profile/page.tsx` — added in Phase 15, may not have full dark mode
   - `src/app/(gp)/reports/page.tsx` — added in Phase 19, may have gaps
   - `src/app/(gp)/settings/page.tsx` — has multiple tabs added across phases
   - All `[id]/page.tsx` detail pages — check dark mode on tab content areas

2. **SectionErrorBoundary wiring:**
   - Read `src/components/ui/error-boundary.tsx` to understand the SectionErrorBoundary component API
   - Per RESEARCH Pitfall 7: `PageErrorBoundary` already wraps all pages via `app-shell.tsx:71`. The gap is `SectionErrorBoundary` on individual data-fetching sections.
   - For each major GP page, wrap data-fetching sections (charts, data panels, SWR-driven content) in `SectionErrorBoundary`:
     - Dashboard: Wrap each of the 7 sections (SummaryBar, NeedsAttentionPanel, DealPipelineFunnel, entity cards, charts, TopBottomPerformers, ActivityFeed)
     - Entity detail: Wrap tab content sections
     - Asset detail: Wrap tab content sections
     - Deal detail: Wrap tab content sections
     - Analytics: Wrap chart sections
   - Pattern:
     ```tsx
     import { SectionErrorBoundary } from "@/components/ui/error-boundary";
     // Wrap data section:
     <SectionErrorBoundary>
       <DataFetchingSection />
     </SectionErrorBoundary>
     ```
   - Do NOT wrap every single tiny component — only sections that fetch data or do complex rendering where a JS error could crash
   - Check which pages already have SectionErrorBoundary and skip those

3. Run `npm run build` to verify.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && echo "GP bg-white without dark:" && grep -rn "bg-white\b" src/app/\(gp\)/ --include="*.tsx" | grep -v "dark:" | wc -l && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All GP pages have dark mode variants. SectionErrorBoundary wraps data-fetching sections on key pages. Build passes.</done>
</task>

</tasks>

<verification>
- Zero `bg-white` without `dark:` variant in LP pages
- Minimal `bg-white` without `dark:` variant in GP pages (some may be intentional within dark containers)
- `npm run build` — zero errors
- SectionErrorBoundary on dashboard, detail pages, analytics
</verification>

<success_criteria>
- All LP and GP pages render correctly in dark mode
- SectionErrorBoundary on all major data-fetching sections
- Build passes
- No functionality regressions
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-09-SUMMARY.md`
</output>
