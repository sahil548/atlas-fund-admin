---
phase: 20-schema-cleanup-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/app/api/deals/[id]/extract-metadata/route.ts
  - src/app/api/deals/[id]/dd-analyze/route.ts
  - src/components/features/deals/deal-overview-tab.tsx
  - src/components/features/deals/deal-dd-tab.tsx
  - src/app/api/deals/route.ts
autonomous: true
requirements: [INTEG-01, INTEG-02, INTEG-03]

must_haves:
  truths:
    - "IC memo generation returns 504 on timeout instead of hanging"
    - "Frontend catches 504 and shows toast error, clears spinner"
    - "DD progress never shows 0% for deals past DD stage"
    - "Pipeline conversion rate is capped at 100% and guard is verified"
  artifacts:
    - path: "src/app/api/deals/[id]/extract-metadata/route.ts"
      provides: "Timeout-protected AI extraction"
      contains: "Promise.race"
    - path: "src/app/api/deals/[id]/dd-analyze/route.ts"
      provides: "Timeout-protected DD analysis"
      contains: "Promise.race"
    - path: "src/components/features/deals/deal-overview-tab.tsx"
      provides: "IC memo error recovery UI"
      contains: "504"
    - path: "src/components/features/deals/deal-dd-tab.tsx"
      provides: "Fixed DD progress calculation"
  key_links:
    - from: "src/components/features/deals/deal-overview-tab.tsx"
      to: "src/app/api/deals/[id]/extract-metadata/route.ts"
      via: "fetch with 504 handling"
      pattern: "status.*504"
    - from: "src/components/features/deals/deal-dd-tab.tsx"
      to: "workstream status fallback"
      via: "progress calculation"
      pattern: "Math\\.min\\(100"
---

<objective>
Fix all 3 documented bugs (BUG-01, BUG-02, BUG-03) with proper error handling and defensive guards.

Purpose: Zero known bugs policy for v2.0 ship. These bugs were documented in CLAUDE.md and CONCERNS.md. BUG-03 is user-facing (spinner hangs forever). BUG-01 confuses users (0% DD on completed deals). BUG-02 is a data anomaly guard.

Output: 3 bugs fixed with regression protection. All existing tests continue to pass.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-schema-cleanup-ui-polish/20-CONTEXT.md
@.planning/phases/20-schema-cleanup-ui-polish/20-RESEARCH.md
@.planning/codebase/CONCERNS.md
@src/app/api/deals/[id]/extract-metadata/route.ts
@src/app/api/deals/[id]/dd-analyze/route.ts
@src/components/features/deals/deal-overview-tab.tsx
@src/components/features/deals/deal-dd-tab.tsx
@src/app/api/deals/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BUG-03 — IC memo timeout with 504 error handling</name>
  <files>src/app/api/deals/[id]/extract-metadata/route.ts, src/app/api/deals/[id]/dd-analyze/route.ts, src/components/features/deals/deal-overview-tab.tsx</files>
  <action>
1. In `src/app/api/deals/[id]/extract-metadata/route.ts`:
   - Wrap the AI call in `Promise.race` with a 55-second timeout (leaving 5s buffer before Vercel's 60s maxDuration)
   - On timeout, return `NextResponse.json({ error: "AI generation timed out. Try again with a smaller document." }, { status: 504 })`
   - On AI error (non-timeout), return `NextResponse.json({ error: "AI extraction failed" }, { status: 500 })`
   - Keep the `maxDuration = 60` export

2. In `src/app/api/deals/[id]/dd-analyze/route.ts`:
   - Apply the same Promise.race timeout pattern (55-second timeout)
   - Return 504 on timeout, 500 on AI error
   - Keep the `maxDuration = 60` export

3. In `src/components/features/deals/deal-overview-tab.tsx` (or wherever the IC memo generate button lives):
   - Find the fetch call that triggers IC memo generation
   - Add explicit handling for `res.status === 504`:
     - Clear the "Generating..." state (setGenerating(false) or equivalent)
     - Show toast: `toast.error("AI generation timed out. Try again with a smaller document.")`
     - Return early (don't try to parse response as JSON)
   - Also add generic error handling for non-ok responses:
     - Clear generating state
     - Show toast with the error message from the response body
   - Ensure the spinner NEVER gets stuck — every code path must clear the loading state

4. Apply the same 504-handling pattern to the DD analysis trigger in `deal-dd-tab.tsx` if it has a similar generate/analyze button
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both API routes return 504 on timeout. Frontend catches 504, clears spinner, shows toast. No spinner can hang indefinitely.</done>
</task>

<task type="auto">
  <name>Task 2: Fix BUG-01 (DD 0% progress) + verify BUG-02 (conversion rate guard)</name>
  <files>src/components/features/deals/deal-dd-tab.tsx, src/app/api/deals/route.ts</files>
  <action>
1. BUG-01 fix in `src/components/features/deals/deal-dd-tab.tsx`:
   - Read the progress calculation logic (around lines 67-100)
   - Verify the fallback path: when a deal is past DD stage (IC_REVIEW, CLOSING, CLOSED) and has no tasks, the progress should show based on workstream statuses, NOT show 0%
   - Ensure `Math.min(100, calculatedProgress)` is applied to BOTH the task-based AND the status-based progress paths
   - For deals in stages AFTER DD (IC_REVIEW, CLOSING, CLOSED): if ALL workstreams are complete OR the deal is CLOSED, show 100% progress
   - Add a stage-aware fallback: `if (deal.stage !== "SCREENING" && deal.stage !== "DUE_DILIGENCE" && totalTasks === 0) { return 100; }` — a deal that advanced past DD necessarily completed DD
   - Test: The existing test in `deal-dd-progress.test.ts` should continue to pass

2. BUG-02 verification in `src/app/api/deals/route.ts`:
   - Read the pipeline conversion rate calculation (around lines 90-103)
   - Verify the `Math.min(100, ...)` guard exists on ALL conversion rate computations
   - If any computation path is missing the guard, add it
   - The existing test in `pipeline-conversion-rates.test.ts` must continue to pass
   - This is primarily a verification task — the guard was reportedly already in place

3. Run `npm run build` to verify no TypeScript errors introduced.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run test -- --reporter=verbose 2>&1 | tail -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DD progress shows correct percentage for all deal stages including post-DD. Pipeline conversion rate capped at 100%. All existing tests pass. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run test` — all tests pass including dd-progress and pipeline-conversion-rates
- `npm run build` — zero errors
- BUG-01: DD tab no longer shows 0% for post-DD deals
- BUG-02: Pipeline conversion rate never exceeds 100%
- BUG-03: IC memo timeout returns error instead of hanging
</verification>

<success_criteria>
- All 3 known bugs documented in CLAUDE.md and CONCERNS.md are fixed
- Existing regression tests continue to pass
- Build succeeds with zero errors
- No new `any` types introduced
</success_criteria>

<output>
After completion, create `.planning/phases/20-schema-cleanup-ui-polish/20-02-SUMMARY.md`
</output>
