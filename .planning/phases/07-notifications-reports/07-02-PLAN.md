---
phase: 07-notifications-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/excel-export.ts
  - src/components/ui/search-filter-bar.tsx
  - src/components/ui/export-button.tsx
  - src/app/(gp)/deals/page.tsx
  - src/app/(gp)/assets/page.tsx
  - src/app/(gp)/entities/page.tsx
  - src/app/(gp)/directory/page.tsx
  - src/app/(gp)/documents/page.tsx
  - src/app/(gp)/tasks/page.tsx
  - src/app/(gp)/meetings/page.tsx
  - src/app/(gp)/transactions/page.tsx
  - src/app/(lp)/lp-documents/page.tsx
  - src/app/(lp)/lp-activity/page.tsx
  - src/app/(lp)/lp-account/page.tsx
autonomous: true
requirements: [REPORT-03]

must_haves:
  truths:
    - "Every GP data table has an Excel export download button"
    - "Export downloads XLSX file with all available fields for the record type"
    - "Export respects current filter selections (exports filtered data, not all)"
    - "Search bars removed from all list pages (AI command bar Cmd+K is universal search)"
    - "Filter dropdowns preserved on all list pages"
    - "LP-facing tables (documents, activity, account) also have export buttons"
  artifacts:
    - path: "src/lib/excel-export.ts"
      provides: "Generic XLSX generation utility using xlsx package"
      exports: ["exportToExcel", "downloadExcel"]
    - path: "src/components/ui/export-button.tsx"
      provides: "Reusable download icon button component"
      exports: ["ExportButton"]
    - path: "src/components/ui/search-filter-bar.tsx"
      provides: "Modified to remove search input, keep filters only, add export slot"
  key_links:
    - from: "src/components/ui/export-button.tsx"
      to: "src/lib/excel-export.ts"
      via: "calls downloadExcel with current table data"
      pattern: "downloadExcel\\("
    - from: "src/app/(gp)/deals/page.tsx"
      to: "src/components/ui/export-button.tsx"
      via: "ExportButton rendered in table header area"
      pattern: "<ExportButton"
---

<objective>
Add Excel export buttons to all data tables across GP and LP portals. Remove search bars from list pages (AI command bar Cmd+K is the universal search per user decision). Keep filter dropdowns. Uses the already-installed xlsx package (^0.18.5).

Purpose: GPs and LPs can export any data table to XLSX for offline analysis or sharing.
Output: excel-export utility, ExportButton component, all list pages updated.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/ui/search-filter-bar.tsx
@src/app/(gp)/deals/page.tsx
@src/app/(gp)/assets/page.tsx
@package.json (xlsx ^0.18.5 already installed)

<interfaces>
<!-- xlsx is already in package.json -->
From package.json: "xlsx": "^0.18.5"

From src/components/ui/search-filter-bar.tsx:
```typescript
interface SearchFilterBarProps {
  onSearch: (query: string) => void;
  filters?: FilterConfig[];
  onFilterChange?: (key: string, value: string) => void;
  activeFilters?: Record<string, string>;
  placeholder?: string;
}
export function SearchFilterBar({ onSearch, filters, onFilterChange, activeFilters, placeholder }: SearchFilterBarProps)
```

SearchFilterBar is used on: deals, assets, entities, directory, documents, tasks, meetings pages (7 GP pages)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Excel export utility + ExportButton component + SearchFilterBar modification</name>
  <files>
    src/lib/excel-export.ts,
    src/components/ui/export-button.tsx,
    src/components/ui/search-filter-bar.tsx
  </files>
  <action>
    1. Create `src/lib/excel-export.ts`:
       - Import `* as XLSX from "xlsx"`
       - Export `exportToExcel(data: Record<string, unknown>[], sheetName: string, fileName: string)`:
         a. Create workbook with XLSX.utils.book_new()
         b. Create worksheet from data array with XLSX.utils.json_to_sheet(data)
         c. Append sheet to workbook
         d. Write to binary string and trigger browser download
       - Export `downloadExcel(data: Record<string, unknown>[], fileName: string)`:
         a. Wrapper that calls exportToExcel with default sheet name "Data"
         b. Generates blob and creates download link (document.createElement('a'), URL.createObjectURL)
         c. fileName should include .xlsx extension automatically if not present

    2. Create `src/components/ui/export-button.tsx`:
       - "use client" component
       - Props: `{ data: Record<string, unknown>[]; fileName: string; disabled?: boolean }`
       - Renders a small download icon button (SVG download arrow icon, same sizing as filter dropdowns)
       - onClick: calls downloadExcel(data, fileName)
       - Shows brief "Exported!" toast on success via useToast()
       - If data is empty, button is disabled with tooltip "No data to export"
       - Styled to sit next to filter dropdowns: `text-xs border border-gray-200 rounded-lg bg-white px-2.5 py-1.5`

    3. Modify `src/components/ui/search-filter-bar.tsx`:
       - Rename to `FilterBar` (keep old export name for backward compat: `export { FilterBar as SearchFilterBar }`)
       - REMOVE the search input entirely (the entire `<div className="relative flex-1 min-w-[180px]">` block with the search icon and input)
       - REMOVE the `onSearch` prop requirement (make it optional and ignored, or remove)
       - KEEP filter dropdowns exactly as-is
       - KEEP "Clear filters" button exactly as-is
       - Add optional `children?: React.ReactNode` prop — rendered at the end of the flex row (slot for ExportButton)
       - Update SearchFilterBarProps: make `onSearch` optional (for backward compat) but the search input is gone
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>
    - excel-export.ts can generate and download XLSX files from any data array
    - ExportButton renders a download icon with toast feedback
    - SearchFilterBar no longer renders a search input
    - Filter dropdowns and clear button preserved
    - Build passes (backward compat maintained via optional onSearch)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add export buttons to all GP and LP list pages</name>
  <files>
    src/app/(gp)/deals/page.tsx,
    src/app/(gp)/assets/page.tsx,
    src/app/(gp)/entities/page.tsx,
    src/app/(gp)/directory/page.tsx,
    src/app/(gp)/documents/page.tsx,
    src/app/(gp)/tasks/page.tsx,
    src/app/(gp)/meetings/page.tsx,
    src/app/(gp)/transactions/page.tsx,
    src/app/(lp)/lp-documents/page.tsx,
    src/app/(lp)/lp-activity/page.tsx,
    src/app/(lp)/lp-account/page.tsx
  </files>
  <action>
    For each GP list page (deals, assets, entities, directory, documents, tasks, meetings, transactions):

    1. Import ExportButton from `@/components/ui/export-button`
    2. Remove the `onSearch` prop from SearchFilterBar usage (since search is removed)
    3. Add ExportButton as a child of SearchFilterBar (or adjacent in the same flex row):
       - Pass the current filtered/displayed data array
       - fileName: "[PageName]_Export" (e.g., "Deals_Export", "Assets_Export")
    4. Data passed to ExportButton should be the FILTERED data currently displayed (not raw API response)
       - Map each item to a flat object with all relevant fields (no nested objects)
       - For deals: id, name, assetClass, stage, targetReturn, targetSize, dealLead, status, createdAt
       - For assets: id, name, assetClass, entityName, costBasis, fairValue, irr, createdAt
       - For entities: id, name, type, totalCommitted, totalCalled, nav, irr, tvpi
       - For directory (contacts/companies/investors): id, name, email, type, company, role
       - For documents: id, name, category, entityName, uploadDate, fileSize
       - For tasks: id, title, status, priority, assignee, dueDate, dealName
       - For meetings: id, title, date, source, attendeeCount
       - For transactions: id, type, entityName, amount, status, date

    For LP pages (lp-documents, lp-activity, lp-account):
    5. Add ExportButton directly (these pages may not use SearchFilterBar)
       - Position: top-right of the content area, before the table
       - Same pattern: pass filtered data, appropriate fileName

    IMPORTANT:
    - Use `const toast = useToast()` — never destructure
    - Keep all existing SWR data fetching, loading guards, and filter logic
    - Only changes are: remove search bar, add export button
    - Deals page: kanban board stays, export button exports the filtered allDeals array (flat list, not kanban columns)
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>
    - All 8 GP list pages have export buttons and no search bars
    - 3 LP pages have export buttons
    - Export downloads XLSX with all fields for the record type
    - Export respects current filter selections
    - Deals kanban board preserved, export button exports flat list
    - Build passes with zero errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- No SearchFilterBar renders a search input anywhere
- Every list page renders an ExportButton
- Clicking export downloads a valid .xlsx file
- xlsx package used (already installed, no new dependency needed)
</verification>

<success_criteria>
- All data tables (GP: deals, assets, entities, directory, documents, tasks, meetings, transactions; LP: documents, activity, account) have functional Excel export
- Export downloads XLSX with all available fields, respecting current filters
- Search bars removed from all list pages
- Filter dropdowns preserved and functional
</success_criteria>

<output>
After completion, create `.planning/phases/07-notifications-reports/07-02-SUMMARY.md`
</output>
