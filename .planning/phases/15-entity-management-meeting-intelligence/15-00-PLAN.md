---
phase: 15-entity-management-meeting-intelligence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/phase15-entity-hierarchy.test.ts
  - src/lib/__tests__/phase15-fireflies-sync.test.ts
autonomous: true
requirements:
  - ENTITY-01
  - ENTITY-03
  - ENTITY-04
  - MTG-01
  - MTG-03
  - MTG-05

must_haves:
  truths:
    - "Two Phase 15 test stub files exist and run green with all tests skipped"
    - "Subsequent plans can run npx jest --testPathPattern=phase15 without file-not-found errors"
  artifacts:
    - path: "src/lib/__tests__/phase15-entity-hierarchy.test.ts"
      provides: "Test stubs for ENTITY-01, ENTITY-03, ENTITY-04"
      contains: "describe"
    - path: "src/lib/__tests__/phase15-fireflies-sync.test.ts"
      provides: "Test stubs for MTG-01, MTG-03, MTG-05"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test scaffold files for Phase 15 Nyquist compliance.

Purpose: All subsequent plans reference these test files in their verify commands. Without them, automated verification commands fail with file-not-found errors. This plan creates stub test files with skipped test blocks that will be filled in by execution plans.
Output: Two test files with describe/it.skip blocks covering all testable Phase 15 requirements.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-entity-management-meeting-intelligence/15-CONTEXT.md
@.planning/phases/15-entity-management-meeting-intelligence/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 15 test stub files</name>
  <files>
    src/lib/__tests__/phase15-entity-hierarchy.test.ts,
    src/lib/__tests__/phase15-fireflies-sync.test.ts
  </files>
  <action>
**Create `src/lib/__tests__/phase15-entity-hierarchy.test.ts`:**

```typescript
/**
 * Phase 15 — Entity Hierarchy & Vehicle Management Tests
 * Wave 0 stubs — filled in by Plans 01-03, 05
 */

describe("Phase 15: Entity Hierarchy", () => {
  describe("ENTITY-01: Vehicle view modes", () => {
    it.skip("buildTree nests child entities under parent by parentEntityId", () => {
      // Plan 02 fills this in
    });

    it.skip("buildTree returns entities without parentEntityId as roots", () => {
      // Plan 02 fills this in
    });
  });

  describe("ENTITY-03: Regulatory filings data", () => {
    it.skip("EntityRegulatoryDataSchema validates complete filing record", () => {
      // Plan 03 fills this in
    });

    it.skip("EntityRegulatoryDataSchema rejects invalid filing status", () => {
      // Plan 03 fills this in
    });

    it.skip("JurisdictionRecordSchema validates jurisdiction record", () => {
      // Plan 03 fills this in
    });
  });

  describe("ENTITY-04: Status transitions", () => {
    it.skip("valid transitions: ACTIVE -> WINDING_DOWN is allowed", () => {
      // Plan 01 fills this in
    });

    it.skip("valid transitions: WINDING_DOWN -> DISSOLVED is allowed", () => {
      // Plan 01 fills this in
    });

    it.skip("valid transitions: DISSOLVED -> any is rejected", () => {
      // Plan 01 fills this in
    });

    it.skip("valid transitions: ACTIVE -> DISSOLVED directly is rejected", () => {
      // Plan 01 fills this in
    });
  });
});
```

**Create `src/lib/__tests__/phase15-fireflies-sync.test.ts`:**

```typescript
/**
 * Phase 15 — Fireflies Sync & Meeting Intelligence Tests
 * Wave 0 stubs — filled in by Plans 04, 06
 */

describe("Phase 15: Fireflies Sync", () => {
  describe("MTG-01: Fireflies API key management", () => {
    it.skip("encryptApiKey produces encrypted string, IV, and tag", () => {
      // Plan 04 fills this in
    });

    it.skip("decryptApiKey recovers original plaintext", () => {
      // Plan 04 fills this in
    });
  });

  describe("MTG-03: Action item parsing", () => {
    it.skip("parseActionItems splits newline-delimited text into array", () => {
      // Plan 04 fills this in
    });

    it.skip("parseActionItems strips numbered prefixes (1. or 1))", () => {
      // Plan 04 fills this in
    });

    it.skip("parseActionItems filters out lines shorter than 4 chars", () => {
      // Plan 04 fills this in
    });

    it.skip("parseActionItems returns empty array for null input", () => {
      // Plan 04 fills this in
    });
  });

  describe("MTG-05: Meeting deduplication", () => {
    it.skip("meetings with same firefliesId are not created twice", () => {
      // Plan 04 fills this in — integration-level, may remain skipped
    });
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npx jest --testPathPattern="phase15" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>Both test files exist in src/lib/__tests__/. Running `npx jest --testPathPattern="phase15"` finds them and reports skipped tests (no failures). Subsequent plans can reference these files in their verify commands.</done>
</task>

</tasks>

<verification>
1. `npx jest --testPathPattern="phase15" --no-coverage` runs without file-not-found errors
2. All tests show as skipped (pending), zero failures
3. Both files importable by later plans
</verification>

<success_criteria>
- Two test files exist at the expected paths
- Jest discovers and runs them without errors
- All test blocks are skipped/pending (none fail)
</success_criteria>

<output>
After completion, create `.planning/phases/15-entity-management-meeting-intelligence/15-00-SUMMARY.md`
</output>
