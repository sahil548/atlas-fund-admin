---
phase: 15-entity-management-meeting-intelligence
plan: 05
type: execute
wave: 2
depends_on: ["15-00", "15-01"]
files_modified:
  - src/app/(gp)/entities/[id]/page.tsx
autonomous: true
requirements:
  - ENTITY-05

must_haves:
  truths:
    - "Side letter management (create, read, add rules) works end-to-end on the entity detail page"
    - "No new features added -- only verification of existing Phase 4 implementation"
  artifacts:
    - path: "src/app/(gp)/entities/[id]/page.tsx"
      provides: "Side letter section accessible and functional"
    - path: "src/components/features/side-letters/create-side-letter-form.tsx"
      provides: "Side letter creation form component"
      contains: "CreateSideLetterForm"
    - path: "src/components/features/side-letters/side-letter-rules-panel.tsx"
      provides: "Side letter rules management panel"
      contains: "SideLetterRulesPanel"
    - path: "src/app/api/side-letters/route.ts"
      provides: "Side letter list and create API"
      exports: ["GET", "POST"]
    - path: "src/app/api/side-letters/[id]/route.ts"
      provides: "Side letter CRUD by ID"
      exports: ["GET", "PUT", "DELETE"]
    - path: "src/app/api/side-letters/[id]/rules/route.ts"
      provides: "Side letter rules API"
      exports: ["GET", "POST"]
    - path: "src/lib/computations/side-letter-engine.ts"
      provides: "Side letter computation logic"
  key_links:
    - from: "src/app/(gp)/entities/[id]/page.tsx"
      to: "/api/side-letters"
      via: "CreateSideLetterForm and SideLetterRulesPanel components"
      pattern: "side-letter"
---

<objective>
Verify side letter management works end-to-end -- no new features, just confirmation the existing implementation is functional.

Purpose: ENTITY-05 requires end-to-end verification that the Phase 4 side letter implementation works: create side letter on entity investors tab, add rules, verify API response, confirm GP can see it in entity detail. Fix any issues found.
Output: Verified side letter flow or minimal fixes if broken.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-entity-management-meeting-intelligence/15-CONTEXT.md
@.planning/phases/15-entity-management-meeting-intelligence/15-01-SUMMARY.md

<interfaces>
<!-- Existing side letter code from Phase 4 -->

Files to verify:
- src/components/features/side-letters/create-side-letter-form.tsx
- src/components/features/side-letters/side-letter-rules-panel.tsx
- src/app/api/side-letters/route.ts (POST, GET)
- src/app/api/side-letters/[id]/route.ts (GET, PUT, DELETE)
- src/app/api/side-letters/[id]/rules/route.ts (POST, GET)
- src/app/api/side-letters/[id]/mfn/route.ts
- src/lib/computations/side-letter-engine.ts
- src/lib/schemas.ts (side letter schemas)

From prisma/schema.prisma:
```prisma
model SideLetter {
  // exists with investor, entity relations
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace and verify side letter end-to-end flow</name>
  <files>
    src/app/(gp)/entities/[id]/page.tsx
  </files>
  <action>
**Verification procedure (not building new features):**

1. **Read all side letter source files** to understand the current implementation:
   - `src/components/features/side-letters/create-side-letter-form.tsx` -- check it renders and submits correctly
   - `src/components/features/side-letters/side-letter-rules-panel.tsx` -- check it renders rules and allows add/edit
   - `src/app/api/side-letters/route.ts` -- check POST creates, GET lists with filters
   - `src/app/api/side-letters/[id]/route.ts` -- check GET/PUT/DELETE work
   - `src/app/api/side-letters/[id]/rules/route.ts` -- check POST creates rules, GET lists
   - `src/lib/schemas.ts` -- check side letter Zod schemas exist and are valid
   - `src/lib/computations/side-letter-engine.ts` -- check computation logic is present

2. **Verify the entity detail page surfaces side letters:**
   - Check that the entity detail page's Investors tab (or wherever side letters are shown) renders `CreateSideLetterForm` and `SideLetterRulesPanel`
   - The entity GET includes `sideLetters: { include: { investor: true, entity: true } }` -- this is already confirmed from reading the route

3. **Check for common issues:**
   - Missing imports or broken component references
   - API route parameter handling (Next.js 16 `await params`)
   - Zod schema validation matching what the form sends
   - `firmId` filtering in GET queries
   - Auth/permission checks

4. **If everything works:** Mark ENTITY-05 as verified with no changes needed. Document in the summary what was checked and confirmed working.

5. **If issues found:** Fix them minimally. Common issues to fix:
   - If `await params` is missing on API routes (Next.js 16 requirement) -- add it
   - If side letter section is hidden or not rendered on entity detail -- wire it in
   - If Zod schema is mismatched with form data -- align them
   - If the entity detail page doesn't import side letter components -- add imports

Do NOT add new features. Only fix what's broken in the existing flow.
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Investor\ Portal\ Claude\ App/atlas && npm run build</automated>
  </verify>
  <done>Side letter end-to-end flow verified: create side letter on entity investors tab, add rules via rules panel, data persists and is visible in entity detail. Any broken wiring fixed. Build passes. ENTITY-05 complete.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Side letter components import without errors
3. Side letter API routes handle Next.js 16 params pattern correctly
4. Entity detail page shows side letters in the appropriate tab
</verification>

<success_criteria>
- Side letter create, read, add-rules flow works without errors
- No new features added (verification only)
- Any discovered issues are fixed minimally
</success_criteria>

<output>
After completion, create `.planning/phases/15-entity-management-meeting-intelligence/15-05-SUMMARY.md`
</output>
